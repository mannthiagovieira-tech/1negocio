<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>1Negócio · Avaliação</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=DM+Mono:wght@300;400;500&family=Cabinet+Grotesk:wght@400;500;700;800;900&display=swap" rel="stylesheet">
<style>
:root{
  --bg:#080d0a;--bg1:#0e1510;--bg2:#131a14;--bg3:#1a2218;
  --em:#3dff7a;--em-d:rgba(61,255,122,.08);--em-l:rgba(61,255,122,.25);
  --roxo:#a78bfa;--roxo-d:rgba(167,139,250,.08);--roxo-l:rgba(167,139,250,.25);
  --am:#ffb03a;--am-d:rgba(255,176,58,.10);--am-l:rgba(255,176,58,.25);
  --rd:#ff5c5c;--rd-d:rgba(255,92,92,.08);
  --ink:#f0f5f1;--ink2:#a8b8ac;--ink3:#5a7060;--ink4:#3a4e3e;
  --line:rgba(255,255,255,.05);--line2:rgba(255,255,255,.10);
  --r:8px;--r2:14px;--r3:20px;
}
*{box-sizing:border-box;margin:0;padding:0}
html,body{background:var(--bg);color:var(--ink);font-family:'Cabinet Grotesk',sans-serif;overflow-x:hidden}

body::before{content:'';position:fixed;inset:0;pointer-events:none;z-index:0;
  background-image:url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");opacity:.02}

/* ── TOPBAR ── */
.topbar{position:sticky;top:0;z-index:100;height:50px;
  display:flex;align-items:center;justify-content:space-between;padding:0 20px;
  background:rgba(8,13,10,.85);backdrop-filter:blur(20px);
  border-bottom:1px solid var(--line)}
.logo{font-family:'Syne',sans-serif;font-weight:800;font-size:15px;letter-spacing:-.02em}
.logo em{color:var(--em);font-style:normal}
.step-dots{display:flex;gap:6px;align-items:center}
.dot{width:28px;height:4px;border-radius:2px;background:var(--line2);transition:all .3s}
.dot.ativa{background:var(--em)}
.dot.roxo{background:var(--roxo)}
.dot.am{background:var(--am)}
.dot.passada{background:rgba(61,255,122,.3)}

/* ── TELAS ── */
.tela{display:none;position:relative;z-index:1;min-height:100vh;padding:0 0 80px}
.tela.ativa{display:block;animation:fadeIn .35s ease}
@keyframes fadeIn{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:translateY(0)}}

/* ── HERO TELA ── */
.hero{padding:40px 20px 32px;max-width:600px;margin:0 auto}
.step-tag{display:inline-flex;align-items:center;gap:7px;
  font-family:'DM Mono',monospace;font-size:9px;letter-spacing:.16em;
  text-transform:uppercase;border-radius:20px;padding:4px 12px;
  margin-bottom:18px}
.step-tag.verde{color:var(--em);background:var(--em-d);border:1px solid var(--em-l)}
.step-tag.roxo{color:var(--roxo);background:var(--roxo-d);border:1px solid var(--roxo-l)}
.step-tag.am{color:var(--am);background:var(--am-d);border:1px solid var(--am-l)}
.step-tag .dot-pulse{width:5px;height:5px;border-radius:50%;flex-shrink:0;animation:pulse 2s ease infinite}
.step-tag.verde .dot-pulse{background:var(--em)}
.step-tag.roxo .dot-pulse{background:var(--roxo)}
.step-tag.am .dot-pulse{background:var(--am)}
@keyframes pulse{0%,100%{opacity:1;transform:scale(1)}50%{opacity:.3;transform:scale(.6)}}

.hero-cod{font-family:'DM Mono',monospace;font-size:10px;color:var(--ink4);
  letter-spacing:.14em;margin-bottom:8px}
.hero-nome{font-family:'Syne',sans-serif;font-weight:800;
  font-size:clamp(28px,7vw,40px);letter-spacing:-.03em;line-height:1.05;
  color:var(--ink);margin-bottom:4px}
.hero-local{font-size:12px;color:var(--ink3);display:flex;align-items:center;gap:6px;margin-bottom:20px}
.hero-local::before{content:'◎';font-size:10px;color:var(--ink4)}

/* ── SECTION WRAPPER ── */
.sec{max-width:600px;margin:0 auto;padding:0 20px 28px}
.sec-lbl{font-family:'DM Mono',monospace;font-size:9px;letter-spacing:.18em;
  text-transform:uppercase;color:var(--ink4);margin-bottom:16px;
  display:flex;align-items:center;gap:10px}
.sec-lbl::after{content:'';flex:1;height:1px;background:var(--line)}

/* ── DESCRICAO IA ── */
.desc-ia{font-size:13px;color:var(--ink2);line-height:1.8;
  padding:16px;background:var(--bg1);border:1px solid var(--line);border-radius:var(--r2);
  margin-bottom:6px}
.desc-tags{display:flex;flex-wrap:wrap;gap:6px;margin-bottom:28px}
.tag{font-family:'DM Mono',monospace;font-size:9px;letter-spacing:.1em;
  text-transform:uppercase;padding:3px 10px;border-radius:20px;
  color:var(--em);background:var(--em-d);border:1px solid var(--em-l)}

/* ── KPI GRID ── */
.kpi-grid{display:grid;grid-template-columns:1fr 1fr;gap:6px;margin-bottom:28px}
.kpi{background:var(--bg1);border:1px solid var(--line);border-radius:var(--r2);
  padding:14px 16px}
.kpi-lbl{font-family:'DM Mono',monospace;font-size:8px;letter-spacing:.12em;
  text-transform:uppercase;color:var(--ink4);margin-bottom:5px}
.kpi-val{font-family:'Syne',sans-serif;font-weight:800;font-size:20px;
  color:var(--ink);letter-spacing:-.02em;line-height:1}
.kpi-sub{font-size:10px;color:var(--ink3);margin-top:3px}

/* ── APRESENTAÇÃO 1NEGOCIO ── */
.box-1n{background:var(--bg1);border:1px solid var(--line2);border-radius:var(--r2);
  padding:20px;margin-bottom:28px}
.box-1n-head{display:flex;align-items:center;gap:10px;margin-bottom:12px}
.box-1n-logo{font-family:'Syne',sans-serif;font-weight:800;font-size:13px}
.box-1n-logo em{color:var(--em);font-style:normal}
.box-1n-txt{font-size:12px;color:var(--ink2);line-height:1.75}

/* ── BTN AVANCAR ── */
.btn-avancar{display:block;width:100%;background:var(--em);color:#080d0a;border:none;
  border-radius:var(--r2);padding:16px;font-family:'DM Mono',monospace;
  font-weight:500;font-size:11px;letter-spacing:.12em;cursor:pointer;
  transition:all .15s;box-shadow:0 4px 20px rgba(61,255,122,.2);text-align:center}
.btn-avancar:hover{background:#5fffa0;transform:translateY(-1px)}
.btn-avancar.roxo-btn{background:var(--roxo);box-shadow:0 4px 20px rgba(167,139,250,.2);color:#fff}
.btn-avancar.roxo-btn:hover{background:#c4b0fd}
.btn-avancar.am-btn{background:var(--am);box-shadow:0 4px 20px rgba(255,176,58,.2);color:#080d0a}
.btn-avancar.am-btn:hover{background:#ffc96a}
.btn-wrap{max-width:600px;margin:0 auto;padding:0 20px}

/* ════════════════════════════════════════
   TELA 1 — O NEGÓCIO
════════════════════════════════════════ */

/* MERCADO */
.mercado-box{background:var(--bg1);border:1px solid var(--line);border-radius:var(--r2);
  padding:18px;margin-bottom:28px}
.mercado-row{display:flex;gap:16px;align-items:flex-start}
.mercado-col{flex:1}
.mercado-item-lbl{font-family:'DM Mono',monospace;font-size:8px;letter-spacing:.14em;
  text-transform:uppercase;color:var(--ink4);margin-bottom:4px}
.mercado-item-val{font-size:13px;color:var(--ink2);line-height:1.6}

/* CLIENTES STRIPE */
.clientes-stripe{display:flex;align-items:center;gap:10px;
  background:var(--em-d);border:1px solid var(--em-l);border-radius:var(--r);
  padding:10px 14px;margin-bottom:8px}
.clientes-stripe-n{font-family:'Syne',sans-serif;font-weight:800;font-size:18px;color:var(--em)}
.clientes-stripe-txt{font-size:11px;color:var(--ink2)}

/* ════════════════════════════════════════
   TELA 2 — A BASE
════════════════════════════════════════ */

/* ISE SCORE */
.ise-wrap{margin-bottom:28px}
.ise-circle-wrap{display:flex;align-items:center;gap:24px;margin-bottom:20px}
.ise-circle{position:relative;width:100px;height:100px;flex-shrink:0}
.ise-svg{width:100px;height:100px;transform:rotate(-90deg)}
.ise-track{fill:none;stroke:var(--line2);stroke-width:8}
.ise-fill{fill:none;stroke-width:8;stroke-linecap:round;
  stroke-dasharray:251.2;stroke-dashoffset:251.2;transition:stroke-dashoffset 1.2s cubic-bezier(.16,1,.3,1)}
.ise-num{position:absolute;top:50%;left:50%;transform:translate(-50%,-50%);
  font-family:'Syne',sans-serif;font-weight:800;font-size:22px;text-align:center;line-height:1}
.ise-num-sub{font-size:9px;font-family:'DM Mono',monospace;letter-spacing:.1em;
  text-transform:uppercase;color:var(--ink3);margin-top:1px;font-weight:400}
.ise-info{flex:1}
.ise-badge{display:inline-block;font-family:'DM Mono',monospace;font-size:9px;
  letter-spacing:.14em;text-transform:uppercase;padding:3px 10px;border-radius:20px;
  margin-bottom:8px}
.ise-title{font-family:'Syne',sans-serif;font-weight:700;font-size:16px;margin-bottom:6px}
.ise-desc{font-size:12px;color:var(--ink2);line-height:1.65}

/* PILARES */
.pilares{display:flex;flex-direction:column;gap:8px;margin-bottom:20px}
.pilar{display:grid;grid-template-columns:110px 1fr 30px;align-items:center;gap:10px}
.pilar-nome{font-size:11px;color:var(--ink2)}
.pilar-bar{height:3px;background:var(--line2);border-radius:2px;overflow:hidden}
.pilar-fill{height:100%;border-radius:2px;transition:width 1s cubic-bezier(.16,1,.3,1)}
.pilar-score{font-family:'DM Mono',monospace;font-size:10px;text-align:right}

/* COMENTÁRIOS ISE */
.ise-comments{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:28px}
.ise-com{padding:14px;border-radius:var(--r2);border:1px solid var(--line)}
.ise-com.pos{background:var(--em-d);border-color:var(--em-l)}
.ise-com.neg{background:var(--am-d);border-color:var(--am-l)}
.ise-com-ico{font-size:14px;margin-bottom:6px}
.ise-com-txt{font-size:11px;line-height:1.6}
.ise-com.pos .ise-com-txt{color:rgba(61,255,122,.8)}
.ise-com.neg .ise-com-txt{color:rgba(255,176,58,.8)}

/* BENCHMARKS */
.benchmarks{display:flex;flex-direction:column;gap:10px;margin-bottom:28px}
.bench{background:var(--bg1);border:1px solid var(--line);border-radius:var(--r2);padding:16px}
.bench-head{display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:12px}
.bench-nome{font-size:12px;font-weight:700;color:var(--ink)}
.bench-fonte{font-family:'DM Mono',monospace;font-size:8px;color:var(--ink4);letter-spacing:.08em}
.bench-barra{position:relative;height:8px;background:var(--line2);border-radius:4px;overflow:visible;margin-bottom:8px}
.bench-setor{position:absolute;top:0;bottom:0;background:rgba(255,255,255,.12);border-radius:4px}
.bench-cliente{position:absolute;top:-2px;width:12px;height:12px;border-radius:50%;
  border:2px solid var(--bg);transition:left 1s cubic-bezier(.16,1,.3,1)}
.bench-labels{display:flex;justify-content:space-between;font-family:'DM Mono',monospace;
  font-size:9px;color:var(--ink4)}

/* TAG TRIBUTÁRIO */
.tag-trib{display:flex;align-items:flex-start;gap:10px;
  background:var(--am-d);border:1px solid var(--am-l);border-radius:var(--r2);
  padding:14px 16px;margin-bottom:28px}
.tag-trib-ico{font-size:14px;flex-shrink:0;margin-top:1px}
.tag-trib-titulo{font-size:11px;font-weight:700;color:var(--am);margin-bottom:3px}
.tag-trib-txt{font-size:11px;color:var(--ink2);line-height:1.6}

/* CTA 1SÓCIO */
.box-1socio{background:var(--bg1);border:1px solid var(--roxo-l);border-radius:var(--r2);
  padding:20px;margin-bottom:28px}
.box-1socio-head{display:flex;align-items:center;justify-content:space-between;margin-bottom:12px}
.box-1socio-logo{font-family:'Syne',sans-serif;font-weight:800;font-size:13px;color:var(--roxo)}
.box-1socio-badge{font-family:'DM Mono',monospace;font-size:8px;letter-spacing:.14em;
  text-transform:uppercase;color:var(--roxo);background:var(--roxo-d);
  border:1px solid var(--roxo-l);border-radius:20px;padding:3px 8px}
.box-1socio-txt{font-size:12px;color:var(--ink2);line-height:1.75;margin-bottom:14px}
.box-1socio-nums{display:grid;grid-template-columns:1fr 1fr 1fr;gap:6px}
.box-1socio-num{background:var(--roxo-d);border:1px solid var(--roxo-l);
  border-radius:var(--r);padding:10px;text-align:center}
.box-1socio-n{font-family:'Syne',sans-serif;font-weight:800;font-size:16px;color:var(--roxo)}
.box-1socio-l{font-size:9px;color:var(--ink3);font-family:'DM Mono',monospace;
  letter-spacing:.06em;margin-top:2px}

/* ════════════════════════════════════════
   TELA 3 — O VALOR
════════════════════════════════════════ */

/* TERMÔMETRO */
.termo-wrap{margin-bottom:28px}
.termo-vals{display:grid;grid-template-columns:1fr 1fr 1fr;gap:8px;margin-bottom:16px}
.termo-val{background:var(--bg1);border:1px solid var(--line);border-radius:var(--r2);
  padding:14px 16px;text-align:center}
.termo-val.destaque{border-color:var(--em-l);background:var(--em-d)}
.termo-val.am-val{border-color:var(--am-l);background:var(--am-d)}
.termo-val.roxo-val{border-color:var(--roxo-l);background:var(--roxo-d)}
.termo-lbl{font-family:'DM Mono',monospace;font-size:8px;letter-spacing:.12em;
  text-transform:uppercase;margin-bottom:6px}
.termo-val.destaque .termo-lbl{color:var(--em)}
.termo-val.am-val .termo-lbl{color:var(--am)}
.termo-val.roxo-val .termo-lbl{color:var(--roxo)}
.termo-lbl:not(.termo-val *){color:var(--ink4)}
.termo-num{font-family:'Syne',sans-serif;font-weight:800;font-size:clamp(16px,4vw,22px);
  letter-spacing:-.02em;line-height:1}
.termo-val.destaque .termo-num{color:var(--em)}
.termo-val.am-val .termo-num{color:var(--am)}
.termo-val.roxo-val .termo-num{color:var(--roxo)}
.termo-barra-wrap{position:relative;height:10px;background:var(--line2);border-radius:5px;overflow:hidden}
.termo-fill{height:100%;border-radius:5px;
  background:linear-gradient(90deg,var(--am),var(--em),var(--roxo));
  transition:width 1.2s cubic-bezier(.16,1,.3,1)}
.termo-marker{position:absolute;top:-3px;width:16px;height:16px;border-radius:50%;
  border:2px solid var(--bg);transform:translateX(-50%);
  transition:left 1.2s cubic-bezier(.16,1,.3,1)}

/* TAG ANALÍTICA */
.tag-analitica{background:var(--bg1);border:1px solid var(--line2);
  border-radius:var(--r2);padding:18px;margin-bottom:28px;
  position:relative;overflow:hidden}
.tag-analitica::before{content:'';position:absolute;top:0;left:0;right:0;height:2px;
  background:linear-gradient(90deg,var(--em),var(--roxo),var(--am))}
.tag-analitica-lbl{font-family:'DM Mono',monospace;font-size:8px;letter-spacing:.16em;
  text-transform:uppercase;color:var(--ink4);margin-bottom:8px}
.tag-analitica-txt{font-size:13px;color:var(--ink);line-height:1.75;font-weight:500}

/* DRE ACCORDION */
.accordion{margin-bottom:12px;border:1px solid var(--line);border-radius:var(--r2);overflow:hidden}
.accordion-head{display:flex;align-items:center;justify-content:space-between;
  padding:14px 16px;cursor:pointer;background:var(--bg1);transition:background .15s}
.accordion-head:hover{background:var(--bg2)}
.accordion-head-txt{font-size:12px;font-weight:700;color:var(--ink)}
.accordion-head-ico{font-size:12px;color:var(--ink3);transition:transform .25s}
.accordion.open .accordion-head-ico{transform:rotate(180deg)}
.accordion-body{max-height:0;overflow:hidden;transition:max-height .4s cubic-bezier(.4,0,.2,1)}
.accordion.open .accordion-body{max-height:800px}
.accordion-inner{padding:16px}

/* DRE TABLE */
.dre-linha{display:flex;justify-content:space-between;align-items:center;
  padding:7px 0;border-bottom:1px solid var(--line)}
.dre-linha:last-child{border-bottom:none}
.dre-linha.total{border-top:1px solid var(--line2);padding-top:10px;margin-top:4px}
.dre-k{font-size:11px;color:var(--ink2)}
.dre-v{font-family:'DM Mono',monospace;font-size:11px;color:var(--ink)}
.dre-v.pos{color:var(--em)}
.dre-v.neg{color:var(--rd)}
.dre-v.destaque{font-weight:700;font-size:13px}

/* MULTIPLO */
.multiplo-box{background:var(--bg1);border:1px solid var(--line);
  border-radius:var(--r2);padding:14px 16px;margin-bottom:8px;
  display:flex;justify-content:space-between;align-items:center}
.multiplo-lbl{font-size:11px;color:var(--ink2)}
.multiplo-val{font-family:'Syne',sans-serif;font-weight:800;font-size:18px;color:var(--em)}

/* PORTEIRA */
.porteira{background:var(--bg2);border:1px solid var(--line);border-radius:var(--r2);
  padding:14px 16px;margin-bottom:28px;display:flex;align-items:center;gap:12px}
.porteira-ico{width:36px;height:36px;border-radius:50%;background:var(--line);
  display:flex;align-items:center;justify-content:center;font-size:16px;flex-shrink:0}
.porteira-txt{font-size:11px;color:var(--ink3);line-height:1.6}

/* CTAs FINAIS */
.ctas{display:flex;flex-direction:column;gap:10px;margin-bottom:28px}
.cta-card{background:var(--bg1);border:1px solid var(--line2);
  border-radius:var(--r2);padding:20px;position:relative;overflow:hidden}
.cta-card.destaque{border-color:var(--em-l)}
.cta-card.roxo-cta{border-color:var(--roxo-l)}
.cta-card::before{content:'';position:absolute;top:0;left:0;right:0;height:2px}
.cta-card.destaque::before{background:var(--em)}
.cta-card.roxo-cta::before{background:var(--roxo)}
.cta-titulo{font-family:'Syne',sans-serif;font-weight:700;font-size:15px;margin-bottom:6px}
.cta-desc{font-size:12px;color:var(--ink2);line-height:1.65;margin-bottom:14px}
.cta-preco{display:flex;align-items:baseline;gap:6px;margin-bottom:14px}
.cta-preco-gratis{font-family:'DM Mono',monospace;font-size:9px;letter-spacing:.14em;
  text-transform:uppercase;color:var(--em);background:var(--em-d);
  border:1px solid var(--em-l);border-radius:20px;padding:3px 10px}
.cta-preco-val{font-family:'Syne',sans-serif;font-weight:800;font-size:20px;color:var(--ink)}
.cta-preco-sub{font-size:11px;color:var(--ink3)}
.cta-btn{width:100%;border:none;border-radius:var(--r2);padding:13px;
  font-family:'DM Mono',monospace;font-weight:500;font-size:10px;letter-spacing:.12em;
  cursor:pointer;transition:all .15s;text-align:center}
.cta-btn.verde{background:var(--em);color:#080d0a;box-shadow:0 4px 16px rgba(61,255,122,.2)}
.cta-btn.verde:hover{background:#5fffa0}
.cta-btn.roxo{background:var(--roxo);color:#fff;box-shadow:0 4px 16px rgba(167,139,250,.2)}
.cta-btn.roxo:hover{background:#c4b0fd}

/* RODAPÉ PDF — só tela 3 */
.rodape-pdf{position:sticky;bottom:0;z-index:90;
  background:rgba(8,13,10,.92);backdrop-filter:blur(16px);
  border-top:1px solid var(--line2);padding:12px 20px;
  display:flex;align-items:center;justify-content:space-between;gap:12px}
.rodape-itens{display:flex;gap:12px;flex-wrap:wrap}
.rodape-item{font-family:'DM Mono',monospace;font-size:8px;letter-spacing:.12em;
  text-transform:uppercase;color:var(--ink4)}
.rodape-btn{background:none;border:1px solid var(--am-l);color:var(--am);
  border-radius:var(--r);padding:8px 16px;font-family:'DM Mono',monospace;
  font-size:10px;font-weight:500;letter-spacing:.1em;cursor:pointer;
  white-space:nowrap;transition:all .15s}
.rodape-btn:hover{background:var(--am-d)}

@media(max-width:500px){
  .kpi-grid{grid-template-columns:1fr 1fr}
  .ise-comments{grid-template-columns:1fr}
  .termo-vals{grid-template-columns:1fr}
  .box-1socio-nums{grid-template-columns:1fr 1fr}
  .ise-circle-wrap{flex-direction:column;align-items:flex-start}
  .bench-barra{margin:8px 0}
}
</style>
</head>
<body>

<!-- ═════════ TOPBAR ═════════ -->
<div class="topbar">
  <div class="logo"><em>1</em>Negócio</div>
  <div class="step-dots">
    <div class="dot ativa" id="dot1" title="O Negócio"></div>
    <div class="dot" id="dot2" title="A Base"></div>
    <div class="dot" id="dot3" title="O Valor"></div>
  </div>
</div>

<!-- ═══════════════════════════════════════════════════════════
     TELA 1 — O NEGÓCIO
════════════════════════════════════════════════════════════ -->
<div class="tela ativa" id="tela1">

  <div class="hero">
    <div class="step-tag verde"><span class="dot-pulse"></span>1 · O NEGÓCIO</div>
    <div class="hero-cod" id="t1-cod">AVL-00000</div>
    <h1 class="hero-nome" id="t1-nome">Carregando...</h1>
    <div class="hero-local" id="t1-local">—</div>

    <!-- Tags positivas -->
    <div class="desc-tags" id="t1-tags"></div>

    <!-- Descrição IA -->
    <div class="desc-ia" id="t1-desc">Analisando o negócio...</div>
  </div>

  <!-- KPIs -->
  <div class="sec">
    <div class="sec-lbl">O negócio em números</div>
    <div class="kpi-grid">
      <div class="kpi">
        <div class="kpi-lbl">Faturamento</div>
        <div class="kpi-val" id="kpi-fat">—</div>
        <div class="kpi-sub">por mês</div>
      </div>
      <div class="kpi">
        <div class="kpi-lbl">Margem bruta</div>
        <div class="kpi-val" id="kpi-margem">—</div>
        <div class="kpi-sub">sobre faturamento</div>
      </div>
      <div class="kpi">
        <div class="kpi-lbl">Resultado op.</div>
        <div class="kpi-val" id="kpi-resultado">—</div>
        <div class="kpi-sub">mensal estimado</div>
      </div>
      <div class="kpi">
        <div class="kpi-lbl">Ponto de equilíbrio</div>
        <div class="kpi-val" id="kpi-pe">—</div>
        <div class="kpi-sub">mínimo para cobrir custos</div>
      </div>
      <div class="kpi">
        <div class="kpi-lbl">Equipe</div>
        <div class="kpi-val" id="kpi-equipe">—</div>
        <div class="kpi-sub">colaboradores</div>
      </div>
      <div class="kpi">
        <div class="kpi-lbl">Custo fixo</div>
        <div class="kpi-val" id="kpi-cf">—</div>
        <div class="kpi-sub">por mês</div>
      </div>
    </div>

    <!-- Clientes condicional -->
    <div class="clientes-stripe" id="t1-clientes" style="display:none">
      <div class="clientes-stripe-n" id="t1-clientes-n">—</div>
      <div class="clientes-stripe-txt">clientes ativos na base</div>
    </div>

    <!-- Mercado -->
    <div class="sec-lbl" style="margin-top:4px">Mercado e posicionamento</div>
    <div class="mercado-box">
      <div class="mercado-row">
        <div class="mercado-col">
          <div class="mercado-item-lbl">Categoria</div>
          <div class="mercado-item-val" id="t1-cat">—</div>
        </div>
        <div class="mercado-col">
          <div class="mercado-item-lbl">Maturidade</div>
          <div class="mercado-item-val" id="t1-maturidade">—</div>
        </div>
      </div>
    </div>

    <!-- Apresentação 1Negócio -->
    <div class="box-1n">
      <div class="box-1n-head">
        <div class="box-1n-logo"><em>1</em>Negócio</div>
      </div>
      <div class="box-1n-txt">
        Somos a plataforma de M&A para pequenos e médios negócios no Brasil.
        Avaliamos com metodologia técnica, conectamos com compradores qualificados
        e acompanhamos toda a negociação — sem exclusividade, sem burocracia.
        Quando a venda acontece, só então cobramos nossa taxa de sucesso.
      </div>
    </div>

    <div class="btn-wrap" style="padding:0">
      <button class="btn-avancar" onclick="irTela(2)">VER DIAGNÓSTICO DO NEGÓCIO →</button>
    </div>
  </div>
</div>

<!-- ═══════════════════════════════════════════════════════════
     TELA 2 — A BASE
════════════════════════════════════════════════════════════ -->
<div class="tela" id="tela2">

  <div class="hero">
    <div class="step-tag roxo"><span class="dot-pulse"></span>2 · A BASE</div>
    <h1 class="hero-nome" id="t2-nome">—</h1>
    <div class="hero-local" id="t2-local">—</div>
  </div>

  <!-- ISE -->
  <div class="sec">
    <div class="sec-lbl">Indicador de solidez empresarial</div>
    <div class="ise-wrap">
      <div class="ise-circle-wrap">
        <div class="ise-circle">
          <svg class="ise-svg" viewBox="0 0 100 100">
            <circle class="ise-track" cx="50" cy="50" r="40"/>
            <circle class="ise-fill" id="ise-circle-fill" cx="50" cy="50" r="40"/>
          </svg>
          <div class="ise-num">
            <span id="ise-num">—</span>
            <div class="ise-num-sub">ISE</div>
          </div>
        </div>
        <div class="ise-info">
          <div class="ise-badge" id="ise-badge">—</div>
          <div class="ise-title" id="ise-title">—</div>
          <div class="ise-desc" id="ise-desc">—</div>
        </div>
      </div>

      <!-- Pilares -->
      <div class="pilares" id="pilares-list"></div>
    </div>

    <!-- Comentários positivo / atenção -->
    <div class="ise-comments" id="ise-comments"></div>

    <!-- Benchmarks -->
    <div class="sec-lbl">Comparativos de mercado</div>
    <div class="benchmarks" id="benchmarks-list"></div>

    <!-- Tag tributário -->
    <div class="tag-trib" id="tag-trib" style="display:none">
      <div class="tag-trib-ico">💡</div>
      <div>
        <div class="tag-trib-titulo" id="tag-trib-titulo">Oportunidade tributária identificada</div>
        <div class="tag-trib-txt" id="tag-trib-txt">—</div>
      </div>
    </div>

    <!-- CTA 1Sócio -->
    <div class="box-1socio">
      <div class="box-1socio-head">
        <div class="box-1socio-logo">1Sócio</div>
        <div class="box-1socio-badge">Gestor estratégico</div>
      </div>
      <div class="box-1socio-txt" id="socio-txt">
        Identificamos pontos de alavancagem no seu negócio. Com acompanhamento
        estratégico mensal, o potencial de valorização é significativo.
      </div>
      <div class="box-1socio-nums">
        <div class="box-1socio-num">
          <div class="box-1socio-n" id="socio-n1">—</div>
          <div class="box-1socio-l">pontos de ataque</div>
        </div>
        <div class="box-1socio-num">
          <div class="box-1socio-n" id="socio-n2">—</div>
          <div class="box-1socio-l">potencial/ano</div>
        </div>
        <div class="box-1socio-num">
          <div class="box-1socio-n" id="socio-n3">—</div>
          <div class="box-1socio-l">valuation em 12m</div>
        </div>
      </div>
    </div>

    <button class="btn-avancar roxo-btn" onclick="irTela(3)">VER O VALOR DO NEGÓCIO →</button>
  </div>
</div>

<!-- ═══════════════════════════════════════════════════════════
     TELA 3 — O VALOR
════════════════════════════════════════════════════════════ -->
<div class="tela" id="tela3">

  <div class="hero">
    <div class="step-tag am"><span class="dot-pulse"></span>3 · O VALOR</div>
    <h1 class="hero-nome" id="t3-nome">—</h1>
    <div class="hero-local" id="t3-local">—</div>
  </div>

  <div class="sec">

    <!-- Termômetro -->
    <div class="sec-lbl">Avaliação comparativa</div>
    <div class="termo-wrap">
      <div class="termo-vals">
        <div class="termo-val am-val">
          <div class="termo-lbl">Sua expectativa</div>
          <div class="termo-num" id="termo-expectativa">—</div>
        </div>
        <div class="termo-val destaque">
          <div class="termo-lbl">Nossa avaliação</div>
          <div class="termo-num" id="termo-avaliacao">—</div>
        </div>
        <div class="termo-val roxo-val">
          <div class="termo-lbl">Potencial 1Sócio</div>
          <div class="termo-num" id="termo-potencial">—</div>
        </div>
      </div>
      <div class="termo-barra-wrap">
        <div class="termo-fill" id="termo-fill" style="width:60%"></div>
        <div class="termo-marker" id="termo-marker"
          style="left:60%;background:var(--em);top:-3px"></div>
      </div>
    </div>

    <!-- Tag analítica -->
    <div class="tag-analitica">
      <div class="tag-analitica-lbl">Análise 1Negócio</div>
      <div class="tag-analitica-txt" id="tag-analitica-txt">—</div>
    </div>

    <!-- DRE Accordion -->
    <div class="sec-lbl">Como chegamos nesse número</div>

    <div class="accordion" id="acc-dre" onclick="toggleAccordion('acc-dre')">
      <div class="accordion-head">
        <div class="accordion-head-txt">DRE Anual</div>
        <div class="accordion-head-ico">↓</div>
      </div>
      <div class="accordion-body">
        <div class="accordion-inner" id="dre-content"></div>
      </div>
    </div>

    <div class="accordion" id="acc-mult" onclick="toggleAccordion('acc-mult')">
      <div class="accordion-head">
        <div class="accordion-head-txt">Múltiplo e metodologia</div>
        <div class="accordion-head-ico">↓</div>
      </div>
      <div class="accordion-body">
        <div class="accordion-inner" id="mult-content"></div>
      </div>
    </div>

    <div class="accordion" id="acc-bp" onclick="toggleAccordion('acc-bp')" style="margin-bottom:28px">
      <div class="accordion-head">
        <div class="accordion-head-txt">Balanço patrimonial (ativos e passivos)</div>
        <div class="accordion-head-ico">↓</div>
      </div>
      <div class="accordion-body">
        <div class="accordion-inner" id="bp-content"></div>
      </div>
    </div>

    <!-- Porteira fechada -->
    <div class="porteira">
      <div class="porteira-ico">🔒</div>
      <div class="porteira-txt">
        <strong style="color:var(--ink);font-size:12px">Porteira fechada</strong><br>
        O comprador paga o valor acordado e assume ativos e passivos.
        O laudo completo detalha os ajustes antes da negociação.
      </div>
    </div>

    <!-- CTAs -->
    <div class="sec-lbl">Próximos passos</div>
    <div class="ctas">
      <div class="cta-card destaque">
        <div class="cta-titulo">Anunciar na 1Negócio</div>
        <div class="cta-desc">
          Publique anonimamente na nossa carteira de negócios.
          Triagem de compradores, mesa de negociação e pós-venda incluídos.
          Você só paga quando vender.
        </div>
        <div style="display:flex;gap:8px;margin-bottom:14px;flex-wrap:wrap">
          <div class="cta-preco-gratis">Gratuito</div>
          <span style="font-size:11px;color:var(--ink3);align-self:center">10% na venda</span>
          <span style="font-size:11px;color:var(--ink4);align-self:center">·</span>
          <span style="font-size:11px;color:var(--am);align-self:center">ou R$588 + 5%</span>
        </div>
        <button class="cta-btn verde" onclick="abrirCTA('anunciar')">ANUNCIAR MEU NEGÓCIO →</button>
      </div>

      <div class="cta-card roxo-cta">
        <div class="cta-titulo" style="color:var(--roxo)">1Sócio — Gestor estratégico</div>
        <div class="cta-desc">
          Acompanhamento mensal com consultor dedicado. Identificamos
          alavancas de resultado e valuation. Você cresce, nós crescemos junto.
        </div>
        <div class="cta-preco">
          <span class="cta-preco-val">R$1.800</span>
          <span class="cta-preco-sub">/ mês + 20% do lucro incremental</span>
        </div>
        <button class="cta-btn roxo" onclick="abrirCTA('socio')">FALAR COM CONSULTOR →</button>
      </div>
    </div>

  </div><!-- /sec -->

  <!-- Rodapé PDF -->
  <div class="rodape-pdf">
    <div class="rodape-itens">
      <span class="rodape-item">Laudo completo</span>
      <span class="rodape-item">Metodologia</span>
      <span class="rodape-item">Benchmark</span>
      <span class="rodape-item">Dossiê</span>
    </div>
    <button class="rodape-btn" onclick="abrirCTA('pdf')">COMPRAR R$99 →</button>
  </div>

</div><!-- /tela3 -->

<script>
// ── STATE ─────────────────────────────────────────────────────────
let D = {};
let iseScore = 0;

// ── HELPERS ───────────────────────────────────────────────────────
function fmtM(v){
  if(!v||v===0) return '—';
  const a = Math.abs(v);
  if(a>=1e6) return 'R$'+(a/1e6).toFixed(1).replace('.',',')+'M';
  if(a>=1e3) return 'R$'+Math.round(a/1e3)+'K';
  return 'R$'+Math.round(a).toLocaleString('pt-BR');
}
function fmtPct(v){ return v ? Math.round(v)+'%' : '—'; }

function scoreColor(s){
  if(s>=80) return 'var(--am)';
  if(s>=60) return 'var(--em)';
  if(s>=40) return 'var(--roxo)';
  return 'var(--rd)';
}
function scoreBadge(s){
  if(s>=80) return {txt:'Referência',cor:'var(--am)',bg:'rgba(255,176,58,.1)',bd:'rgba(255,176,58,.25)'};
  if(s>=65) return {txt:'Consolidada',cor:'var(--em)',bg:'var(--em-d)',bd:'var(--em-l)'};
  if(s>=45) return {txt:'Em transição',cor:'var(--roxo)',bg:'var(--roxo-d)',bd:'var(--roxo-l)'};
  return {txt:'Fragilizada',cor:'var(--rd)',bg:'var(--rd-d)',bd:'rgba(255,92,92,.25)'};
}

// ── NAVEGAÇÃO ─────────────────────────────────────────────────────
function irTela(n){
  document.querySelectorAll('.tela').forEach(t=>t.classList.remove('ativa'));
  document.getElementById('tela'+n).classList.add('ativa');
  window.scrollTo({top:0,behavior:'smooth'});
  // dots
  ['dot1','dot2','dot3'].forEach((id,i)=>{
    const d = document.getElementById(id);
    d.className = 'dot';
    if(i+1 < n) d.classList.add('passada');
    else if(i+1 === n){
      if(n===1) d.classList.add('ativa');
      else if(n===2) d.classList.add('roxo');
      else d.classList.add('am');
    }
  });
  if(n===2) setTimeout(animarISE, 400);
  if(n===3) setTimeout(animarTermometro, 400);
}

// ── CALCULAR ──────────────────────────────────────────────────────
function calcular(){
  const fat = D.faturamento || 0;
  const margem = D.margem_bruta || 0;
  const cf = (D.aluguel||0) + (D.outros_cf||0);
  const folha = D.folha || 0;
  const prolabore = D.prolabore || 0;
  const func = parseInt(D.funcionarios) || 0;

  // Resultado op mensal
  const receitaLiq = fat * (1 - 0.12); // deduções ~12% simples
  const cmv = fat * (1 - margem/100);
  const margemBruta = receitaLiq - cmv;
  const resultadoOp = margemBruta - cf - folha - prolabore;

  // Ponto de equilíbrio
  const pe = margem > 0 ? cf / (margem/100) : 0;

  // ISE Score (simplificado)
  let ise = 50;
  if(margem > 30) ise += 10; else if(margem < 15) ise -= 10;
  if(resultadoOp > 0) ise += 10; else ise -= 15;
  if((D.anos||0) > 5) ise += 5;
  if(D.gestao === 'independente') ise += 10;
  else if(D.gestao === 'dependente') ise -= 10;
  if(D.sistema === 'sim') ise += 5;
  if((D.concentracao||0) > 50) ise -= 10;
  if(D.regime === 'real') ise += 5;
  ise = Math.max(10, Math.min(95, Math.round(ise)));
  iseScore = ise;

  // Múltiplo por categoria
  const multiplos = {
    comercializar:2.5, distribuir:2.5, fornecer:2.8, fabricar:3.0,
    servicos_projeto:4.0, servicos_mao_obra:2.5, locar:5.0, plataforma:5.5
  };
  const mult = multiplos[D.categoria] || 3.0;

  // Ajuste ISE no múltiplo
  const multAdj = mult * (ise >= 70 ? 1.15 : ise >= 50 ? 1.0 : 0.85);

  // Valor negócio (resultado anual × múltiplo)
  const resultadoAnual = Math.max(0, resultadoOp) * 12;
  const valorNegocio = resultadoAnual * multAdj;

  // Ativos e passivos
  const ativos = (D.imovel||0)+(D.equipamentos||0)+(D.estoque||0)+(D.outros_ativos||0);
  const passivos = (D.emprestimos||0)+(D.parcelamentos||0)+(D.outros_passivos||0);
  const valorVenda = valorNegocio + ativos - passivos;
  const potencial = valorVenda * 1.35;

  return {
    fat, margem, receitaLiq, cmv, margemBruta, cf, folha, prolabore,
    resultadoOp, pe, ise, mult: multAdj, resultadoAnual,
    valorNegocio, ativos, passivos, valorVenda, potencial, func
  };
}

// ── RENDERIZAR ────────────────────────────────────────────────────
function render(){
  const C = calcular();
  const nome = D.nome_negocio || 'Seu negócio';
  const artigo = D.artigo || 'o';
  const local = [D.cidade, D.estado].filter(Boolean).join(', ') || '—';
  const cats = {
    comercializar:'Comércio',distribuir:'Distribuição',fornecer:'Fornecedor industrial',
    fabricar:'Indústria / Fabricação',servicos_projeto:'Serviços de projeto',
    servicos_mao_obra:'Serviços de mão de obra',locar:'Locação',plataforma:'Plataforma / Marketplace'
  };

  // ─── TELA 1 ───
  document.getElementById('t1-cod').textContent = D.codigo_lead || 'AVL-' + Math.random().toString(36).slice(2,7).toUpperCase();
  document.getElementById('t1-nome').textContent = nome;
  document.getElementById('t1-local').textContent = local;
  document.getElementById('t2-nome').textContent = artigo.toUpperCase()+' '+nome;
  document.getElementById('t2-local').textContent = local;
  document.getElementById('t3-nome').textContent = artigo.toUpperCase()+' '+nome;
  document.getElementById('t3-local').textContent = local;

  // Tags
  const tagsEl = document.getElementById('t1-tags');
  const tagsData = [];
  if(D.sistema==='sim') tagsData.push('Sistema de gestão');
  if((D.anos||0)>=5) tagsData.push((D.anos||0)+'+ anos');
  if(C.margem>30) tagsData.push('Margem saudável');
  if(D.gestao==='independente') tagsData.push('Gestão independente');
  if(D.crescimento==='20mais') tagsData.push('Crescimento acelerado');
  if(C.resultadoOp>0) tagsData.push('Resultado positivo');
  tagsEl.innerHTML = tagsData.slice(0,5).map(t=>`<span class="tag">${t}</span>`).join('');

  // Descrição IA
  document.getElementById('t1-desc').textContent =
    `${artigo.charAt(0).toUpperCase()+artigo.slice(1)} ${nome} é um negócio de ${(cats[D.categoria]||'').toLowerCase()} `+
    `com ${D.anos||0} ${(D.anos||0)===1?'ano':'anos'} de operação, sediado em ${local}. `+
    `Fatura ${fmtM(C.fat)} por mês com margem bruta de ${fmtPct(C.margem)}, `+
    `gerando resultado operacional estimado de ${fmtM(C.resultadoOp)}/mês. `+
    `${C.func>0?`Conta com ${C.func} colaborador${C.func>1?'es':''}.`:''} `+
    (D.gestao==='independente'?'Operação independente da presença do sócio.':'');

  // KPIs
  document.getElementById('kpi-fat').textContent = fmtM(C.fat);
  document.getElementById('kpi-margem').textContent = fmtPct(C.margem);
  document.getElementById('kpi-resultado').textContent = fmtM(C.resultadoOp);
  document.getElementById('kpi-pe').textContent = fmtM(C.pe);
  document.getElementById('kpi-equipe').textContent = C.func || '—';
  document.getElementById('kpi-cf').textContent = fmtM(C.cf);

  // Clientes
  const cliMap = {menos100:'< 100',['100a500']:'100–500',['500a2000']:'500–2.000',mais2000:'+2.000'};
  if(D.clientes_ativos && D.clientes_ativos!=='menos100'){
    document.getElementById('t1-clientes').style.display='flex';
    document.getElementById('t1-clientes-n').textContent = cliMap[D.clientes_ativos]||'—';
  }

  // Mercado
  document.getElementById('t1-cat').textContent = cats[D.categoria]||'—';
  document.getElementById('t1-maturidade').textContent =
    (D.anos||0)>=10?'Negócio maduro':(D.anos||0)>=5?'Em consolidação':'Em crescimento';

  // ─── TELA 2 ───
  // ISE
  const iseBadge = scoreBadge(C.ise);
  document.getElementById('ise-num').textContent = C.ise;
  const badgeEl = document.getElementById('ise-badge');
  badgeEl.textContent = iseBadge.txt;
  badgeEl.style.cssText = `color:${iseBadge.cor};background:${iseBadge.bg};border:1px solid ${iseBadge.bd};`;
  document.getElementById('ise-title').textContent =
    C.ise>=70?'Negócio bem estruturado':C.ise>=50?'Base em desenvolvimento':'Atenção necessária';
  document.getElementById('ise-desc').textContent =
    C.ise>=70?`Score acima da média do segmento. Negócio com processos estabelecidos e resultado consistente.`:
    C.ise>=50?`Fundamentos presentes com pontos de melhoria identificados. Potencial de valorização relevante.`:
    `Negócio com fragilidades estruturais que impactam o valuation. Recomendamos consultoria antes da venda.`;

  // ISE fill circle
  const fillEl = document.getElementById('ise-circle-fill');
  fillEl.style.stroke = scoreColor(C.ise);
  // animado em animarISE()

  // Pilares
  const pilares = [
    {n:'Financeiro',s:Math.min(100,Math.round(C.margem*2.5)),p:20},
    {n:'Resultado',s:C.resultadoOp>0?Math.min(100,Math.round(C.resultadoOp/C.fat*300)):20,p:15},
    {n:'Comercial',s:D.clientes_ativos==='mais2000'?90:D.clientes_ativos==='500a2000'?75:D.clientes_ativos==='100a500'?60:45,p:15},
    {n:'Gestão',s:D.gestao==='independente'?90:D.gestao==='parcial'?60:30,p:15},
    {n:'Sócio-dep.',s:D.gestao==='independente'?90:D.gestao==='parcial'?55:25,p:10},
    {n:'Risco Legal',s:D.regime?75:50,p:10},
    {n:'Balanço',s:C.ativos>C.passivos?80:50,p:8},
    {n:'Marca',s:(D.anos||0)>=10?80:(D.anos||0)>=5?65:45,p:7},
  ];
  document.getElementById('pilares-list').innerHTML = pilares.map(p=>{
    const cor = scoreColor(p.s);
    return `<div class="pilar">
      <span class="pilar-nome">${p.n} <span style="font-size:9px;color:var(--ink4)">${p.p}%</span></span>
      <div class="pilar-bar"><div class="pilar-fill" data-w="${p.s}" style="background:${cor};width:0%"></div></div>
      <span class="pilar-score" style="color:${cor}">${p.s}</span>
    </div>`;
  }).join('');

  // Comentários ISE
  const comments = [];
  if(C.margem>25) comments.push({tipo:'pos',ico:'↑',txt:`Margem bruta de ${fmtPct(C.margem)} acima da média do setor`});
  if(D.gestao==='independente') comments.push({tipo:'pos',ico:'✓',txt:'Operação independente do sócio — fator valorizado por compradores'});
  if(D.sistema==='sim') comments.push({tipo:'pos',ico:'✓',txt:'Uso de sistema de gestão aumenta a percepção de governança'});
  if(D.concentracao>50) comments.push({tipo:'neg',ico:'⚠',txt:`${D.concentracao}% do faturamento em um único cliente — risco relevante`});
  if(C.resultadoOp<0) comments.push({tipo:'neg',ico:'▼',txt:'Resultado operacional negativo comprime o valuation'});
  if(D.gestao==='dependente') comments.push({tipo:'neg',ico:'⚠',txt:'Alta dependência do sócio reduz o interesse de compradores'});
  document.getElementById('ise-comments').innerHTML = comments.slice(0,4).map(c=>
    `<div class="ise-com ${c.tipo}"><div class="ise-com-ico">${c.ico}</div><div class="ise-com-txt">${c.txt}</div></div>`
  ).join('');

  // Benchmarks
  const benchRef = {
    comercializar:{margem:{med:28,min:15,max:45},cmv:{med:62,min:45,max:80},rpc:{med:8500,min:3000,max:18000}},
    fabricar:     {margem:{med:35,min:20,max:55},cmv:{med:55,min:35,max:70},rpc:{med:12000,min:5000,max:25000}},
    servicos_projeto:{margem:{med:55,min:35,max:75},cmv:{med:30,min:15,max:55},rpc:{med:18000,min:8000,max:40000}},
  };
  const ref = benchRef[D.categoria] || benchRef.comercializar;
  const benches = [
    {nome:'Margem operacional',cliente:Math.round(C.margem),setor:ref.margem,sufixo:'%',fonte:'base 1Negócio'},
    {nome:'CMV % faturamento',cliente:Math.round(100-C.margem),setor:ref.cmv,sufixo:'%',fonte:'base 1Negócio'},
    {nome:'Resultado por colaborador',cliente:C.func>0?Math.round(C.resultadoOp/C.func):0,setor:ref.rpc,sufixo:'',fonte:'IBGE + base 1N'},
  ];
  document.getElementById('benchmarks-list').innerHTML = benches.map(b=>{
    const pct = b.setor.max>0?Math.round(((b.cliente-b.setor.min)/(b.setor.max-b.setor.min))*100):50;
    const clipct = Math.max(5,Math.min(95,pct));
    const setorLeft = Math.max(5,Math.min(85,Math.round(((b.setor.med-b.setor.min)/(b.setor.max-b.setor.min))*100)));
    const cor = b.cliente >= b.setor.med ? 'var(--em)' : 'var(--am)';
    const vFmt = b.sufixo ? b.cliente+b.sufixo : fmtM(b.cliente);
    const mFmt = b.sufixo ? b.setor.med+b.sufixo : fmtM(b.setor.med);
    return `<div class="bench">
      <div class="bench-head">
        <span class="bench-nome">${b.nome}</span>
        <span class="bench-fonte">${b.fonte}</span>
      </div>
      <div class="bench-barra">
        <div class="bench-setor" style="left:${setorLeft-10}%;width:20%"></div>
        <div class="bench-cliente" data-left="${clipct}" style="left:${clipct}%;background:${cor}"></div>
      </div>
      <div class="bench-labels">
        <span>${b.sufixo?b.setor.min+b.sufixo:fmtM(b.setor.min)}</span>
        <span style="color:${cor};font-weight:700">Você: ${vFmt} · Setor: ${mFmt}</span>
        <span>${b.sufixo?b.setor.max+b.sufixo:fmtM(b.setor.max)}</span>
      </div>
    </div>`;
  }).join('');

  // Tag tributário
  if(D.regime==='simples' && C.fat >= 50000){
    document.getElementById('tag-trib').style.display='flex';
    document.getElementById('tag-trib-titulo').textContent='Possível transição de regime tributário';
    document.getElementById('tag-trib-txt').textContent=
      `Com faturamento de ${fmtM(C.fat)}/mês, pode ser interessante avaliar Lucro Presumido. Fale com nosso time para uma análise sem compromisso.`;
  }

  // 1Sócio números
  const pontosAtaque = [C.margem<25,C.resultadoOp<C.fat*0.1,D.gestao!=='independente',D.concentracao>40,D.sistema!=='sim'].filter(Boolean).length;
  const potencialEco = Math.round(C.resultadoOp * 0.3 * 12);
  document.getElementById('socio-n1').textContent = pontosAtaque || 3;
  document.getElementById('socio-n2').textContent = fmtM(Math.abs(potencialEco)) || '—';
  document.getElementById('socio-n3').textContent = fmtM(C.potencial);

  // ─── TELA 3 ───
  document.getElementById('termo-expectativa').textContent = D.expectativa_val>0?fmtM(D.expectativa_val):'Não informado';
  document.getElementById('termo-avaliacao').textContent = fmtM(C.valorVenda);
  document.getElementById('termo-potencial').textContent = fmtM(C.potencial);

  // Tag analítica
  const motivos = {
    vender:'Seu objetivo de venda',socio:'Busca de sócio',
    planejamento:'Planejamento estratégico',sucessao:'Planejamento sucessório',curiosidade:'Conhecimento do valor'
  };
  const motivo = motivos[D.motivacao] || 'Avaliação';
  const gap = D.expectativa_val>0 ? C.valorVenda - D.expectativa_val : 0;
  let analiticaTxt = `${motivo} — `;
  if(gap > 0) analiticaTxt += `nossa avaliação está ${fmtM(gap)} acima da sua expectativa. `;
  else if(gap < 0) analiticaTxt += `sua expectativa está ${fmtM(Math.abs(gap))} acima da nossa avaliação — recomendamos conversar antes de precificar. `;
  else analiticaTxt += `avaliação alinhada com o mercado. `;
  analiticaTxt += D.motivacao==='vender'
    ? `Com score ISE de ${C.ise}, o negócio tem perfil adequado para publicação. Recomendamos o plano assessorado para maximizar o resultado.`
    : `O 1Sócio pode acelerar o valuation em até 35% em 12 meses com foco nos pontos identificados.`;
  document.getElementById('tag-analitica-txt').textContent = analiticaTxt;

  // DRE
  const impostos = C.fat * 0.12;
  const dreItens = [
    {k:'Faturamento bruto', v:C.fat*12, tipo:''},
    {k:'(−) Deduções e impostos', v:-(impostos*12), tipo:'neg'},
    {k:'Receita líquida', v:C.receitaLiq*12, tipo:'destaque'},
    {k:'(−) CMV / Custo direto', v:-(C.cmv*12), tipo:'neg'},
    {k:'Margem bruta', v:C.margemBruta*12, tipo:'pos'},
    {k:'(−) Custos fixos', v:-(C.cf*12), tipo:'neg'},
    {k:'(−) Folha de pessoal', v:-(C.folha*12), tipo:'neg'},
    {k:'(−) Pró-labore sócios', v:-(C.prolabore*12), tipo:'neg'},
    {k:'Resultado operacional', v:C.resultadoOp*12, tipo:C.resultadoOp>=0?'pos destaque':'neg destaque'},
  ];
  document.getElementById('dre-content').innerHTML =
    dreItens.map(i=>`<div class="dre-linha ${i.tipo==='destaque'||i.tipo.includes('destaque')?'total':''}">
      <span class="dre-k">${i.k}</span>
      <span class="dre-v ${i.v>=0?'pos':'neg'} ${i.tipo.includes('destaque')?'destaque':''}">${fmtM(Math.abs(i.v))}</span>
    </div>`).join('');

  // Múltiplo
  document.getElementById('mult-content').innerHTML = `
    <div class="multiplo-box"><span class="multiplo-lbl">Resultado operacional anual</span><span class="multiplo-val">${fmtM(C.resultadoAnual)}</span></div>
    <div class="multiplo-box"><span class="multiplo-lbl">Múltiplo setor (${cats[D.categoria]||'—'})</span><span class="multiplo-val">${C.mult.toFixed(1)}×</span></div>
    <div class="multiplo-box"><span class="multiplo-lbl">Ajuste ISE (${C.ise} pts)</span><span class="multiplo-val">${C.ise>=70?'+15%':C.ise>=50?'neutro':'-15%'}</span></div>
    <div class="dre-linha total"><span class="dre-k">Valor do negócio</span><span class="dre-v pos destaque">${fmtM(C.valorNegocio)}</span></div>`;

  // BP
  document.getElementById('bp-content').innerHTML = `
    <div class="multiplo-box"><span class="multiplo-lbl">Valor do negócio</span><span class="multiplo-val">${fmtM(C.valorNegocio)}</span></div>
    <div class="multiplo-box"><span class="multiplo-lbl">(+) Ativos totais</span><span class="multiplo-val">${fmtM(C.ativos)}</span></div>
    <div class="multiplo-box"><span class="multiplo-lbl">(−) Passivos totais</span><span class="multiplo-val" style="color:var(--rd)">${fmtM(C.passivos)}</span></div>
    <div class="dre-linha total"><span class="dre-k">Valor de venda (porteira fechada)</span><span class="dre-v pos destaque">${fmtM(C.valorVenda)}</span></div>`;
}

// ── ANIMAÇÕES ─────────────────────────────────────────────────────
function animarISE(){
  const fill = document.getElementById('ise-circle-fill');
  const pct = iseScore/100;
  fill.style.strokeDashoffset = 251.2 * (1-pct);
  document.querySelectorAll('.pilar-fill[data-w]').forEach(el=>{
    el.style.width = el.getAttribute('data-w')+'%';
  });
}

function animarTermometro(){
  const C = calcular();
  const max = C.potencial * 1.1;
  const pct = max>0 ? Math.min(95, Math.round((C.valorVenda/max)*100)) : 60;
  document.getElementById('termo-fill').style.width = pct+'%';
  document.getElementById('termo-marker').style.left = pct+'%';
}

function toggleAccordion(id){
  document.getElementById(id).classList.toggle('open');
}

function abrirCTA(tipo){
  const msgs = {
    anunciar:'Olá, quero anunciar meu negócio na 1Negócio. Acabei de ver minha avaliação.',
    socio:'Olá, tenho interesse no 1Sócio após ver minha avaliação. Podemos conversar?',
    pdf:'Olá, quero adquirir o laudo completo em PDF do meu negócio.'
  };
  window.open('https://wa.me/5511952136406?text='+encodeURIComponent(msgs[tipo]||''),'_blank');
}

// ── INIT ──────────────────────────────────────────────────────────
(function(){
  // 1. Tentar pegar da URL (?dados=...)
  const p = new URLSearchParams(window.location.search);
  const dadosUrl = p.get('dados');
  if(dadosUrl){
    try{
      const parsed = JSON.parse(decodeURIComponent(dadosUrl));
      // Mapear campos do diagnóstico v3/v4 para o formato do laudo
      D = {
        nome_negocio: parsed.nome || parsed.nome_negocio || '',
        artigo:       parsed.artigo || 'o',
        categoria:    Array.isArray(parsed.tipo) ? parsed.tipo[0] : (parsed.tipo || parsed.categoria || ''),
        cidade:       parsed.cidade || '',
        estado:       parsed.estado || '',
        anos:         parsed.anos_num || 5,
        motivacao:    parsed.motivacao || '',
        faturamento:  parsed.fat_mensal || 0,
        margem_bruta: parsed.margem_bruta || 0,
        funcionarios: (parsed.clt_qtd||0) + (parsed.pj_qtd||0),
        folha:        parsed.folha_total || 0,
        aluguel:      parsed.aluguel || 0,
        outros_cf:    (parsed.cf_outros || 0) + (parsed.facilities||0) + (parsed.terceirizados||0) + (parsed.outros_cf||0),
        prolabore:    parsed.prolabore || 0,
        regime:       parsed.regime || '',
        gestao:       parsed.dependencia || '',
        sistema:      parsed.erp || '',
        concentracao: parsed.concentracao_pct || 0,
        crescimento:  parsed.crescimento || '',
        clientes_ativos: parsed.cli_12m ? (parsed.cli_12m > 2000 ? 'mais2000' : parsed.cli_12m > 500 ? '500a2000' : parsed.cli_12m > 100 ? '100a500' : 'menos100') : '',
        imovel:       parsed.at_imovel || 0,
        equipamentos: parsed.at_equip || 0,
        estoque:      parsed.at_estoque || parsed.estoque || 0,
        outros_ativos:parsed.at_outros || 0,
        emprestimos:  parsed.saldo_devedor || 0,
        parcelamentos:0,
        outros_passivos: (parsed.pv_outros||0) + (parsed.passivo_juridico||0),
        expectativa:  parsed.expectativa || 'nao_sei',
        expectativa_val: parsed.expectativa_val || 0,
        codigo_lead:  parsed.codigo_lead || '',
        // compatibilidade com campos diretos do v4
        ...parsed
      };
    }catch(e){ console.warn('Erro ao parsear dados da URL:', e); }
  }

  // 2. Tentar sessionStorage como fallback
  if(!D.faturamento && !D.fat_mensal){
    const stored = sessionStorage.getItem('1neg_diagnostico');
    if(stored){
      try{
        const parsed = JSON.parse(stored);
        D = { ...D, ...parsed };
        if(!D.faturamento) D.faturamento = parsed.fat_mensal || 0;
        if(!D.nome_negocio) D.nome_negocio = parsed.nome || '';
        if(!D.artigo) D.artigo = parsed.artigo || 'o';
      }catch(e){}
    }
  }

  // 3. Normalizar faturamento (v4 usa fat_mensal, laudo usa faturamento)
  if(!D.faturamento && D.fat_mensal) D.faturamento = D.fat_mensal;
  if(!D.nome_negocio && D.nome) D.nome_negocio = D.nome;

  render();
})();
</script>
</body>
</html>
