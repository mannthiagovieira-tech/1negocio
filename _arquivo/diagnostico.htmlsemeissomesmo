<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>1Negócio · Avaliação</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=DM+Mono:wght@300;400;500&family=Cabinet+Grotesk:wght@400;500;700;800;900&display=swap" rel="stylesheet">
<style>
:root{
  --bg:#080d0a;--bg1:#0e1510;--bg2:#131a14;--bg3:#1a2218;
  --em:#3dff7a;--em-d:rgba(61,255,122,.08);--em-l:rgba(61,255,122,.25);
  --am:#ffb03a;--am-d:rgba(255,176,58,.10);--am-l:rgba(255,176,58,.25);
  --rd:#ff5c5c;--rd-d:rgba(255,92,92,.08);--rd-l:rgba(255,92,92,.25);
  --pu:#a78bfa;--pu-d:rgba(167,139,250,.08);--pu-l:rgba(167,139,250,.25);
  --ink:#f0f5f1;--ink2:#a8b8ac;--ink3:#5a7060;--ink4:#3a4e3e;
  --line:rgba(255,255,255,.05);--line2:rgba(255,255,255,.10);
  --r:8px;--r2:14px;--r3:20px;
}
*{box-sizing:border-box;margin:0;padding:0}
html,body{min-height:100vh;background:var(--bg);color:var(--ink);
  font-family:'Cabinet Grotesk',sans-serif;overflow-x:hidden}
body::before{content:'';position:fixed;inset:0;pointer-events:none;z-index:0;
  background-image:url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");opacity:.022}
body::after{content:'';position:fixed;top:-15%;left:50%;transform:translateX(-50%);
  width:600px;height:400px;border-radius:50%;
  background:radial-gradient(ellipse,rgba(61,255,122,.055) 0%,transparent 65%);
  pointer-events:none;z-index:0}

/* ── TOPBAR ── */
.topbar{position:fixed;top:0;left:0;right:0;z-index:100;height:50px;
  display:flex;align-items:center;justify-content:space-between;padding:0 20px;
  background:rgba(8,13,10,.85);backdrop-filter:blur(20px);
  border-bottom:1px solid var(--line)}
.logo{font-family:'Syne',sans-serif;font-weight:800;font-size:15px;letter-spacing:-.02em}
.logo em{color:var(--em);font-style:normal}
.top-right{display:flex;align-items:center;gap:12px}
.prog-wrap{display:flex;align-items:center;gap:8px}
.prog-bar{width:100px;height:3px;background:var(--line2);border-radius:2px;overflow:hidden}
.prog-fill{height:100%;background:var(--em);border-radius:2px;transition:width .4s ease;width:0%}
.prog-txt{font-family:'DM Mono',monospace;font-size:9px;color:var(--ink3);letter-spacing:.1em}
.secao-tag{font-family:'DM Mono',monospace;font-size:8px;letter-spacing:.14em;
  text-transform:uppercase;color:var(--ink4);background:var(--bg2);
  border:1px solid var(--line);border-radius:20px;padding:3px 10px}

/* ── LAYOUT ── */
.page{position:relative;z-index:1;min-height:100vh;display:flex;
  align-items:center;justify-content:center;padding:70px 20px 60px}
.card{width:100%;max-width:480px}

/* ── TELA ── */
.tela{display:none;animation:up .3s ease both}
.tela.ativa{display:block}
@keyframes up{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:translateY(0)}}

/* ── LABELS ── */
.sec-label{font-family:'DM Mono',monospace;font-size:9px;letter-spacing:.18em;
  text-transform:uppercase;color:var(--ink4);margin-bottom:20px;
  display:flex;align-items:center;gap:8px}
.sec-label::after{content:'';flex:1;height:1px;background:var(--line)}
.t-num{font-family:'DM Mono',monospace;font-size:9px;color:var(--em);
  letter-spacing:.12em;margin-bottom:10px;opacity:.7}
.t-q{font-family:'Syne',sans-serif;font-weight:700;
  font-size:clamp(19px,5vw,25px);line-height:1.15;letter-spacing:-.02em;
  color:var(--ink);margin-bottom:6px}
.t-sub{font-size:12px;color:var(--ink3);line-height:1.65;margin-bottom:22px}

/* ── OPÇÕES ── */
.ops{display:flex;flex-direction:column;gap:7px;margin-bottom:22px}
.ops.g2{display:grid;grid-template-columns:1fr 1fr;gap:7px}
.ops.g3{display:grid;grid-template-columns:1fr 1fr 1fr;gap:7px}
.op{display:flex;align-items:flex-start;gap:11px;
  background:var(--bg1);border:1px solid var(--line2);border-radius:var(--r2);
  padding:13px 15px;cursor:pointer;transition:all .15s;text-align:left;
  -webkit-tap-highlight-color:transparent}
.op:hover{border-color:var(--em-l);background:var(--em-d)}
.op.sel{border-color:var(--em);background:var(--em-d)}
.op.chip{padding:10px 14px;align-items:center}
.op-check{width:17px;height:17px;border-radius:50%;border:1.5px solid var(--line2);
  flex-shrink:0;display:flex;align-items:center;justify-content:center;margin-top:1px;
  transition:all .15s}
.op.sel .op-check{background:var(--em);border-color:var(--em)}
.op.sel .op-check::after{content:'';width:6px;height:6px;border-radius:50%;background:#080d0a}
.op.chip .op-check{margin-top:0}
.op-txt{flex:1}
.op-lbl{font-size:13px;font-weight:700;color:var(--ink);line-height:1.2}
.op-sub{font-size:11px;color:var(--ink3);margin-top:3px;line-height:1.45}
.op.chip .op-lbl{font-size:12px}

/* ── PILLS ── */
.pills{display:flex;flex-wrap:wrap;gap:7px;margin-bottom:22px}
.pill{padding:8px 14px;border-radius:20px;border:1px solid var(--line2);
  background:var(--bg1);cursor:pointer;font-size:12px;font-weight:600;
  color:var(--ink2);transition:all .15s;-webkit-tap-highlight-color:transparent}
.pill:hover{border-color:var(--em-l);color:var(--ink)}
.pill.sel{border-color:var(--em);background:var(--em-d);color:var(--em)}

/* ── INPUT ── */
.inp-wrap{margin-bottom:16px}
.inp-lbl{font-family:'DM Mono',monospace;font-size:9px;letter-spacing:.16em;
  text-transform:uppercase;color:var(--ink3);display:block;margin-bottom:7px}
.inp{width:100%;background:var(--bg1);border:1px solid var(--line2);
  border-radius:var(--r2);padding:13px 16px;font-size:15px;color:var(--ink);
  font-family:'Cabinet Grotesk',sans-serif;font-weight:500;outline:none;
  transition:border-color .15s,box-shadow .15s;-webkit-appearance:none}
.inp:focus{border-color:var(--em-l);box-shadow:0 0 0 3px rgba(61,255,122,.06)}
.inp::placeholder{color:var(--ink4);font-weight:400}
.inp.sm{font-size:13px;padding:10px 14px}
.inp-hint{font-size:11px;color:var(--ink4);margin-top:5px;line-height:1.5}
.inp-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin-bottom:16px}
.inp-grid3{display:grid;grid-template-columns:1fr 1fr 1fr;gap:8px;margin-bottom:16px}
textarea.inp{resize:vertical;min-height:90px;font-size:13px;line-height:1.6}

/* ── ARTIGO ── */
.artigo-group{display:flex;gap:10px;margin-bottom:22px}
.artigo-btn{flex:1;padding:16px;border:1px solid var(--line2);border-radius:var(--r2);
  background:var(--bg1);cursor:pointer;text-align:center;transition:all .15s}
.artigo-btn:hover{border-color:var(--em-l)}
.artigo-btn.sel{border-color:var(--em);background:var(--em-d)}
.artigo-a{font-family:'Syne',sans-serif;font-weight:800;font-size:26px;
  color:var(--ink2);display:block;margin-bottom:4px;transition:color .15s}
.artigo-btn.sel .artigo-a{color:var(--em)}
.artigo-ex{font-size:10px;color:var(--ink4);font-family:'DM Mono',monospace}

/* ── SLIDER ── */
.slider-wrap{margin-bottom:22px}
.slider-val{font-family:'Syne',sans-serif;font-weight:800;font-size:24px;
  color:var(--em);text-align:center;margin-bottom:8px}
.slider{width:100%;-webkit-appearance:none;height:4px;border-radius:2px;
  background:var(--line2);outline:none}
.slider::-webkit-slider-thumb{-webkit-appearance:none;width:22px;height:22px;
  border-radius:50%;background:var(--em);cursor:pointer;
  box-shadow:0 0 0 4px rgba(61,255,122,.15)}
.slider-labels{display:flex;justify-content:space-between;
  font-size:10px;color:var(--ink4);margin-top:6px;font-family:'DM Mono',monospace}

/* ── MODO TOGGLE (tipo anual/mensal, compra/venda) ── */
.mode-toggle{display:flex;background:var(--bg2);border:1px solid var(--line2);
  border-radius:var(--r2);padding:3px;margin-bottom:16px;gap:3px}
.mode-btn{flex:1;padding:9px;text-align:center;border-radius:var(--r);
  font-family:'DM Mono',monospace;font-size:9px;letter-spacing:.12em;
  text-transform:uppercase;cursor:pointer;color:var(--ink3);transition:all .15s}
.mode-btn.sel{background:var(--em);color:#080d0a;font-weight:700}

/* ── CALC CMV ── */
.calc-wrap{background:var(--bg1);border:1px solid var(--line2);
  border-radius:var(--r2);padding:18px;margin-bottom:16px}
.calc-row{display:grid;grid-template-columns:1fr auto 1fr;align-items:center;gap:10px;margin-bottom:12px}
.calc-op{font-size:20px;color:var(--ink3);text-align:center;font-weight:300}
.calc-result{display:flex;justify-content:space-between;align-items:center;
  padding:11px 14px;background:var(--em-d);border:1px solid var(--em-l);border-radius:var(--r)}
.calc-lbl{font-family:'DM Mono',monospace;font-size:9px;letter-spacing:.12em;
  text-transform:uppercase;color:var(--em)}
.calc-val{font-family:'Syne',sans-serif;font-weight:800;font-size:18px;color:var(--em)}

/* ── RES BOX ── */
.res-box{display:flex;justify-content:space-between;align-items:center;
  padding:12px 15px;background:var(--em-d);border:1px solid var(--em-l);
  border-radius:var(--r);margin-bottom:14px}
.res-box.am{background:var(--am-d);border-color:var(--am-l)}
.res-box.rd{background:var(--rd-d);border-color:var(--rd-l)}
.res-lbl{font-family:'DM Mono',monospace;font-size:9px;letter-spacing:.1em;
  text-transform:uppercase;color:var(--em)}
.res-box.am .res-lbl{color:var(--am)}
.res-box.rd .res-lbl{color:var(--rd)}
.res-val{font-family:'Syne',sans-serif;font-weight:800;font-size:18px;color:var(--em)}
.res-box.am .res-val{color:var(--am)}
.res-box.rd .res-val{color:var(--rd)}

/* ── AVISO ── */
.aviso{display:flex;gap:10px;align-items:flex-start;
  background:var(--am-d);border:1px solid var(--am-l);border-radius:var(--r2);
  padding:13px 15px;margin-bottom:16px;font-size:12px;color:var(--ink2);line-height:1.6}
.aviso.rd{background:var(--rd-d);border-color:var(--rd-l)}
.aviso-ico{flex-shrink:0;margin-top:1px}

/* ── SEP ── */
.sep{height:1px;background:var(--line);margin:16px 0}

/* ── BOTÕES ── */
.btn{width:100%;background:var(--em);color:#080d0a;border:none;
  border-radius:var(--r2);padding:15px;font-family:'DM Mono',monospace;
  font-weight:500;font-size:11px;letter-spacing:.12em;cursor:pointer;
  transition:all .15s;box-shadow:0 4px 20px rgba(61,255,122,.2)}
.btn:hover{background:#5fffa0;transform:translateY(-1px)}
.btn:disabled{background:var(--ink4);box-shadow:none;cursor:not-allowed;transform:none}
.btn-sec{width:100%;background:transparent;border:1px solid var(--line2);
  color:var(--ink3);border-radius:var(--r2);padding:12px;
  font-size:12px;cursor:pointer;margin-top:8px;
  font-family:'Cabinet Grotesk',sans-serif;font-weight:600;transition:all .15s}
.btn-sec:hover{color:var(--ink);background:var(--bg1);border-color:var(--line2)}
.btn-skip{background:none;border:none;color:var(--ink4);font-size:11px;
  cursor:pointer;width:100%;margin-top:10px;font-family:'DM Mono',monospace;
  letter-spacing:.1em;padding:6px}
.btn-skip:hover{color:var(--ink3)}

/* ── FADE INFERIOR (ilusão de botões flutuantes) ── */
.page::after{
  content:'';position:fixed;bottom:0;left:0;right:0;height:80px;
  background:linear-gradient(to top,var(--bg) 40%,transparent);
  pointer-events:none;z-index:10;
}
.card{padding-bottom:24px}



/* ── SPIN ── */
.spin{width:14px;height:14px;border:2px solid rgba(8,13,10,.2);border-top-color:#080d0a;
  border-radius:50%;animation:rot .6s linear infinite;display:inline-block;margin-right:5px}
@keyframes rot{to{transform:rotate(360deg)}}

/* ── RESUMO FINAL ── */
.resumo-grid{display:grid;grid-template-columns:1fr 1fr;gap:6px;margin-bottom:20px}
.res-item{background:var(--bg1);border:1px solid var(--line);border-radius:var(--r);padding:10px 12px}
.res-k{font-size:9px;font-family:'DM Mono',monospace;letter-spacing:.1em;text-transform:uppercase;color:var(--ink4);margin-bottom:3px}
.res-v{font-size:13px;font-weight:700;color:var(--ink);line-height:1.3}

/* ── WOW BOX ── */
.wow-stat-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:16px}
.wow-stat{background:var(--bg1);border:1px solid var(--line);border-radius:var(--r2);
  padding:14px;text-align:center}
.wow-n{font-family:'Syne',sans-serif;font-weight:800;font-size:22px;
  color:var(--em);letter-spacing:-.02em}
.wow-l{font-size:9px;font-family:'DM Mono',monospace;letter-spacing:.1em;
  text-transform:uppercase;color:var(--ink4);margin-top:3px}

@media(max-width:440px){
  .t-q{font-size:19px}
  .ops.g2{grid-template-columns:1fr}
  .ops.g3{grid-template-columns:1fr 1fr}
  .inp-grid{grid-template-columns:1fr}
  .wow-stat-grid{grid-template-columns:1fr 1fr}
}
</style>
</head>
<body>

<!-- TOPBAR -->
<div class="topbar">
  <div class="logo"><em>1</em>Negócio</div>
  <div class="top-right">
    <div class="secao-tag" id="tag-secao">Início</div>
    <div class="prog-wrap">
      <div class="prog-bar"><div class="prog-fill" id="prog-fill"></div></div>
      <div class="prog-txt" id="prog-txt">0%</div>
    </div>
  </div>
</div>

<div class="page"><div class="card" id="card">

<!-- T1 MOTIVAÇÃO -->
<div class="tela ativa" id="t1">
  <div class="sec-label">Início</div>
  <div class="t-num">01</div>
  <div class="t-q" id="t1-q">Conta pra gente — o que te motiva a avaliar o negócio hoje?</div>
  <div class="t-sub">Só pra seguirmos alinhados.</div>
  <div class="ops">
    <button class="op" onclick="pick(this,'motivacao','vender')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Penso em vender</div><div class="op-sub">Quero entender o valor antes de colocar à venda</div></div></button>
    <button class="op" onclick="pick(this,'motivacao','curiosidade')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Curiosidade</div><div class="op-sub">Nunca avaliei formalmente — quero saber</div></div></button>
    <button class="op" onclick="pick(this,'motivacao','socio')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Busco sócio ou investidor</div><div class="op-sub">Quero trazer capital ou parceiro estratégico</div></div></button>
    <button class="op" onclick="pick(this,'motivacao','planejamento')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Planejamento e gestão</div><div class="op-sub">Quero usar o número para tomar melhores decisões</div></div></button>
    <button class="op" onclick="pick(this,'motivacao','sucessao')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sucessão ou herança</div><div class="op-sub">Preciso do valor para planejamento familiar</div></div></button>
  </div>
  <button class="btn" id="btn1" disabled onclick="ir('t2')">CONTINUAR →</button>
</div>

<!-- T2 NOME E ARTIGO -->
<div class="tela" id="t2">
  <div class="sec-label">Identidade</div>
  <div class="t-num">02</div>
  <div class="t-q">Só pra confirmar — este é o nome do seu negócio?</div>
  <div class="t-sub">Use o nome fantasia — aquele que todos conhecem.</div>
  <div class="inp-wrap">
    <label class="inp-lbl">NOME DO NEGÓCIO</label>
    <input class="inp" id="i-nome" type="text" placeholder="Nome fantasia..."
      oninput="D.nome=this.value;atualizarArtigos();validarT2()" autocomplete="off">
  </div>
  <div id="c-artigo" style="display:none">
    <label class="inp-lbl">COMO VOCÊ PREFERE SE REFERIR AO NEGÓCIO?</label>
    <div class="artigo-group">
      <button class="artigo-btn" id="art-o" onclick="D.artigo='o';selArtigo('o')">
        <span class="artigo-a">o</span>
        <span class="artigo-ex" id="ex-o">o negócio</span>
      </button>
      <button class="artigo-btn" id="art-a" onclick="D.artigo='a';selArtigo('a')">
        <span class="artigo-a">a</span>
        <span class="artigo-ex" id="ex-a">a empresa</span>
      </button>
    </div>
  </div>
  <button class="btn" id="btn2" disabled onclick="ir('t3')">CONTINUAR →</button>
  <button class="btn-sec" onclick="ir('t1')">← Voltar</button>
</div>

<!-- T3 TIPO -->
<div class="tela" id="t3">
  <div class="sec-label">Identidade</div>
  <div class="t-num">03</div>
  <div class="t-q" id="t3-q">O que melhor representa o negócio?</div>
  <div class="t-sub">Pode selecionar mais de uma opção.</div>
  <div class="ops">
    <button class="op" onclick="toggleMulti(this,'tipo','comercio')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Comércio / Varejo</div><div class="op-sub">Venda para cliente final — lojas, e-commerce</div></div></button>
    <button class="op" onclick="toggleMulti(this,'tipo','distribuicao')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Distribuição</div><div class="op-sub">Venda para revendedores / varejistas</div></div></button>
    <button class="op" onclick="toggleMulti(this,'tipo','fornecimento')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Fornecimento para indústria</div><div class="op-sub">Matéria prima, insumos, componentes</div></div></button>
    <button class="op" onclick="toggleMulti(this,'tipo','fabricacao')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Fabricação / Industrialização</div><div class="op-sub">Transforma insumos em produto acabado</div></div></button>
    <button class="op" onclick="toggleMulti(this,'tipo','servico_projeto')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Serviços de projeto e consultoria</div><div class="op-sub">Tecnologia, projetos, consultas, corretagem, assessoria — médicos, dentistas, terapeutas, contadores</div></div></button>
    <button class="op" onclick="toggleMulti(this,'tipo','servico_mao_obra')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Serviços de mão de obra</div><div class="op-sub">Postos de trabalho, hora trabalhada, alocação</div></div></button>
    <button class="op" onclick="toggleMulti(this,'tipo','hospedagem')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Hospedagem e hotelaria</div><div class="op-sub">Hotéis, pousadas, aluguel por temporada, resorts</div></div></button>
    <button class="op" onclick="toggleMulti(this,'tipo','locacao')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Locações</div><div class="op-sub">Equipamentos, espaços, veículos, imóveis comerciais</div></div></button>
    <button class="op" onclick="toggleMulti(this,'tipo','plataforma')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Plataformas ou marketplaces</div><div class="op-sub">Conecta oferta e demanda</div></div></button>
  </div>
  <button class="btn" id="btn3" disabled onclick="ir('t4')">CONTINUAR →</button>
  <button class="btn-sec" onclick="ir('t2')">← Voltar</button>
</div>

<!-- T4 SETOR -->
<div class="tela" id="t4">
  <div class="sec-label">Identidade</div>
  <div class="t-num">04</div>
  <div class="t-q">Em qual setor o negócio melhor se encaixa?</div>
  <div class="t-sub">Com esta informação lhe mostraremos comparativo com empresas similares.</div>
  <div class="ops">
    <button class="op chip" onclick="pick(this,'setor','alimentacao');ir('t5')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Alimentação</div><div class="op-sub">Restaurantes, bares, padarias, açougues, delivery</div></div></button>
    <button class="op chip" onclick="pick(this,'setor','varejo');;ir('t5')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Varejo e consumo</div><div class="op-sub">Lojas, farmácias, mercados, moda, eletro, móveis</div></div></button>
    <button class="op chip" onclick="pick(this,'setor','saude');ir('t5')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Saúde e estética</div><div class="op-sub">Clínicas, consultórios, odonto, salões, spas</div></div></button>
    <button class="op chip" onclick="pick(this,'setor','educacao');ir('t5')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Educação</div><div class="op-sub">Escolas, cursos, idiomas, treinamentos, EAD</div></div></button>
    <button class="op chip" onclick="pick(this,'setor','tech');ir('t5')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Tecnologia</div><div class="op-sub">Software, apps, SaaS, TI, desenvolvimento</div></div></button>
    <button class="op chip" onclick="pick(this,'setor','construcao');ir('t5')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Construção e reformas</div><div class="op-sub">Construtoras, incorporadoras, reformas, acabamento</div></div></button>
    <button class="op chip" onclick="pick(this,'setor','industrial');ir('t5')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Serviços industriais</div><div class="op-sub">Manutenção, instalação, elétrica, mecânica, HVAC</div></div></button>
    <button class="op chip" onclick="pick(this,'setor','logistica');ir('t5')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Logística e transporte</div><div class="op-sub">Fretes, transportadoras, mudanças, delivery B2B</div></div></button>
    <button class="op chip" onclick="pick(this,'setor','marketing');ir('t5')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Marketing e comunicação</div><div class="op-sub">Agências, mídia, design, publicidade, PR</div></div></button>
    <button class="op chip" onclick="pick(this,'setor','wellness');ir('t5')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Bem-estar e fitness</div><div class="op-sub">Academias, studios, yoga, nutrição, saúde mental</div></div></button>
    <button class="op chip" onclick="pick(this,'setor','pet');ir('t5')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Pet</div><div class="op-sub">Clínicas vet, pet shops, banho e tosa</div></div></button>
    <button class="op chip" onclick="pick(this,'setor','agro');ir('t5')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Agronegócio</div><div class="op-sub">Insumos, produção rural, irrigação, máquinas</div></div></button>
    <button class="op chip" onclick="pick(this,'setor','entretenimento');ir('t5')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Entretenimento e lazer</div><div class="op-sub">Eventos, turismo, bares noturnos, esportes, jogos</div></div></button>
    <button class="op chip" onclick="pick(this,'setor','financeiro');ir('t5')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Financeiro e jurídico</div><div class="op-sub">Contabilidade, advocacia, seguros, crédito, câmbio</div></div></button>
    <button class="op chip" onclick="pick(this,'setor','consultoria');ir('t5')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Consultoria e serviços profissionais</div><div class="op-sub">Médicos, dentistas, terapeutas, coaches, assessorias</div></div></button>
    <button class="op" onclick="pick(this,'setor','outro');mostrar('c-setor-outro')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Outro setor</div></div></button>
  </div>
  <div id="c-setor-outro" style="display:none;margin-bottom:16px">
    <input class="inp" id="i-setor-outro" type="text" placeholder="Qual setor?" oninput="D.setor_outro=this.value">
    <button class="btn" onclick="ir('t5')" style="margin-top:10px">CONTINUAR →</button>
  </div>
  <button class="btn-sec" onclick="ir('t3')">← Voltar</button>
</div>

<!-- T5 DESCRIÇÃO -->
<div class="tela" id="t5">
  <div class="sec-label">Identidade</div>
  <div class="t-num">05</div>
  <div class="t-q" id="t5-q">Descreva o negócio em poucas palavras</div>
  <div class="t-sub">Do jeito que você explicaria para um amigo, sem rodeios — 3 a 5 frases.</div>
  <div class="inp-wrap" style="margin-bottom:22px">
    <textarea class="inp" id="i-desc" rows="4"
      placeholder="Ex: Academia de musculação em São Paulo com 6 anos, foco em treino funcional, clientela adulta 25–45 anos..."
      oninput="D.descricao=this.value"></textarea>
    <div class="inp-hint">Opcional — mas quanto mais detalhe, mais precisa a avaliação.</div>
  </div>
  <button class="btn" onclick="ir('t6')">CONTINUAR →</button>
  <button class="btn-skip" onclick="ir('t6')">Pular →</button>
  <button class="btn-sec" onclick="ir('t4')">← Voltar</button>
</div>

<!-- T6 FRANQUIA -->
<div class="tela" id="t6">
  <div class="sec-label">Identidade</div>
  <div class="t-num">06</div>
  <div class="t-q">É uma franquia?</div>
  <div class="ops g2" style="margin-bottom:16px">
    <button class="op" onclick="pick(this,'franquia','sim');mostrar('c-franquia')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sim, é franquia</div></div></button>
    <button class="op" onclick="pick(this,'franquia','nao');esconder('c-franquia');ir('t7')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não, independente</div></div></button>
  </div>
  <div id="c-franquia" style="display:none">
    <div class="sep"></div>
    <div class="t-sub" style="margin-bottom:16px">Quais são os encargos da franquia?</div>
    <!-- ROYALTIES -->
    <div class="inp-lbl" style="margin-bottom:8px">ROYALTIES</div>
    <div class="mode-toggle" id="tog-royalty">
      <div class="mode-btn sel" onclick="selMode(this,'royalty_tipo','pct','tog-royalty');atualizarInputRoyalty('pct')">% FATURAMENTO</div>
      <div class="mode-btn" onclick="selMode(this,'royalty_tipo','fixo','tog-royalty');atualizarInputRoyalty('fixo')">R$ FIXO</div>
    </div>
    <div class="inp-wrap">
      <input class="inp" id="i-royalty" type="number" min="0" max="100" placeholder="Ex: 5"
        oninput="inputRoyalty(this)">
      <div style="font-size:11px;color:var(--ink3);margin-top:4px" id="royalty-suffix-hint">Digite apenas o número — o % é aplicado sobre o faturamento</div>
      <div class="inp-hint" id="hint-royalty">Percentual sobre o faturamento bruto</div>
    </div>
    <!-- FUNDO MKT -->
    <div class="inp-lbl" style="margin-bottom:8px">FUNDO DE MARKETING</div>
    <div class="mode-toggle" id="tog-mkt-franquia">
      <div class="mode-btn" onclick="selMode(this,'mkt_franquia_tipo','pct','tog-mkt-franquia');atualizarInputMkt('pct')">% FATURAMENTO</div>
      <div class="mode-btn sel" onclick="selMode(this,'mkt_franquia_tipo','fixo','tog-mkt-franquia');atualizarInputMkt('fixo')">R$ FIXO</div>
    </div>
    <div class="inp-wrap">
      <input class="inp inp-money" id="i-mkt-franquia" type="text" placeholder="R$ 0"
        oninput="maskMoney(this);D.mkt_franquia=parseMasked('i-mkt-franquia')">
    </div>
    <!-- TAXA DE FRANQUIA PAGA -->
    <div class="inp-wrap" style="margin-bottom:22px">
      <label class="inp-lbl">TAXA DE FRANQUIA PAGA (ativo do negócio)</label>
      <input class="inp inp-money" id="i-taxa-franquia" type="text" placeholder="R$ 0"
        oninput="maskMoney(this);D.taxa_franquia=parseMasked('i-taxa-franquia')">
      <div class="inp-hint">Valor pago para adquirir a franquia — entra no inventário de ativos</div>
    </div>
    <button class="btn" onclick="ir('t7')">CONTINUAR →</button>
  </div>
  <button class="btn-sec" onclick="ir('t5')">← Voltar</button>
</div>

<!-- T7 LOCAL -->
<div class="tela" id="t7">
  <div class="sec-label">Identidade</div>
  <div class="t-num">07</div>
  <div class="t-q" id="t7-q">O negócio está em um local fixo?</div>
  <div class="t-sub">Ponto comercial, escritório ou galpão.</div>
  <div class="ops g2" style="margin-bottom:16px">
    <button class="op" onclick="pick(this,'local_tipo','sim');mostrar('c-local')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sim, temos um endereço</div></div></button>
    <button class="op" onclick="pick(this,'local_tipo','remoto');esconder('c-local');ir('t9')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não, estamos 100% remoto</div></div></button>
  </div>
  <div id="c-local" style="display:none">
    <div class="ops g2" style="margin-bottom:12px">
      <button class="op chip" onclick="pick(this,'imovel_tipo','alugado')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Alugado</div></div></button>
      <button class="op chip" onclick="pick(this,'imovel_tipo','proprio')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Próprio</div></div></button>
    </div>
    <div class="inp-wrap">
      <label class="inp-lbl">ALUGUEL + CONDOMÍNIO + IPTU / MÊS</label>
      <input class="inp inp-money" id="i-aluguel" type="text" placeholder="R$ 0"
        oninput="maskMoney(this);D.aluguel=parseMasked('i-aluguel')">
      <div class="inp-hint">Se imóvel próprio, informe quanto cobraria de locação de um terceiro</div>
    </div>
    <button class="btn" onclick="ir('t8')">CONTINUAR →</button>
  </div>
  <button class="btn-sec" onclick="ir('t6')">← Voltar</button>
</div>

<!-- T8 LOCALIZAÇÃO (só aparece se tem local fixo) -->
<div class="tela" id="t8">
  <div class="sec-label">Identidade</div>
  <div class="t-num">08</div>
  <div class="t-q">Onde fica?</div>
  <div class="pills" id="estados-pills">
    <button class="pill" onclick="selPillEstado(this,'SP')">SP</button>
    <button class="pill" onclick="selPillEstado(this,'RJ')">RJ</button>
    <button class="pill" onclick="selPillEstado(this,'MG')">MG</button>
    <button class="pill" onclick="selPillEstado(this,'RS')">RS</button>
    <button class="pill" onclick="selPillEstado(this,'SC')">SC</button>
    <button class="pill" onclick="selPillEstado(this,'PR')">PR</button>
    <button class="pill" onclick="selPillEstado(this,'BA')">BA</button>
    <button class="pill" onclick="selPillEstado(this,'GO')">GO</button>
    <button class="pill" onclick="selPillEstado(this,'PE')">PE</button>
    <button class="pill" onclick="selPillEstado(this,'CE')">CE</button>
    <button class="pill" onclick="selPillEstado(this,'DF')">DF</button>
    <button class="pill" onclick="selPillEstado(this,'MT')">MT</button>
    <button class="pill" onclick="selPillEstado(this,'MS')">MS</button>
    <button class="pill" onclick="selPillEstado(this,'outro')">Outro</button>
  </div>
  <!-- Top cidades por estado (preenchido dinamicamente) -->
  <div id="cidades-sugeridas" style="display:none;margin-bottom:12px">
    <div class="inp-lbl" style="margin-bottom:8px">CIDADES MAIS COMUNS</div>
    <div class="pills" id="cidades-pills"></div>
  </div>
  <div class="inp-wrap" style="margin-bottom:22px">
    <label class="inp-lbl">OU DIGITE SUA CIDADE</label>
    <input class="inp" id="i-cidade" type="text" placeholder="Ou digite sua cidade..."
      oninput="D.cidade=this.value;validarT8()" autocomplete="off">
  </div>
  <button class="btn" id="btn8" disabled onclick="ir('t9')">CONTINUAR →</button>
  <button class="btn-sec" onclick="ir('t7')">← Voltar</button>
</div>

<!-- T9 TEMPO + ORIGEM -->
<div class="tela" id="t9">
  <div class="sec-label">Identidade</div>
  <div class="t-num">09</div>
  <div class="t-q">Há quanto tempo o negócio existe?</div>
  <div class="slider-wrap">
    <div class="slider-val" id="anos-val">5 anos</div>
    <input class="slider" type="range" min="1" max="30" value="5" id="sl-anos"
      oninput="atualizarAnos(this.value)">
    <div class="slider-labels"><span>1 ano</span><span>30+ anos</span></div>
  </div>
  <div class="sep"></div>
  <div class="inp-lbl" style="margin-bottom:10px">E VOCÊ...</div>
  <div class="ops g3" style="margin-bottom:16px">
    <button class="op chip" onclick="pick(this,'origem_socio','fundou');esconder('c-origem-tempo')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Fundou</div></div></button>
    <button class="op chip" onclick="pick(this,'origem_socio','herdou');mostrar('c-origem-tempo')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Herdou</div></div></button>
    <button class="op chip" onclick="pick(this,'origem_socio','comprou');mostrar('c-origem-tempo')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Comprou</div></div></button>
  </div>
  <div id="c-origem-tempo" style="display:none;margin-bottom:16px">
    <div class="inp-lbl" style="margin-bottom:8px">HÁ QUANTO TEMPO?</div>
    <div class="slider-wrap">
      <div class="slider-val" id="origem-anos-val">3 anos</div>
      <input class="slider" type="range" min="1" max="30" value="3" id="sl-origem-anos"
        oninput="atualizarOrigemAnos(this.value)">
      <div class="slider-labels"><span>1 ano</span><span>30+ anos</span></div>
    </div>
  </div>
  <button class="btn" id="btn9" onclick="ir('t10')">CONTINUAR →</button>
  <button class="btn-sec" onclick="voltar_t9()">← Voltar</button>
</div>

<!-- T10 SÓCIOS -->
<div class="tela" id="t10">
  <div class="sec-label">Identidade</div>
  <div class="t-num">10</div>
  <div class="t-q" id="t10-q">Você tem sócios?</div>
  <div class="ops g2" style="margin-bottom:16px">
    <button class="op" onclick="pick(this,'socios','nao');esconder('c-socios');ir('t11')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sou o único sócio</div></div></button>
    <button class="op" onclick="pick(this,'socios','sim');mostrar('c-socios')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sim, tenho sócios</div></div></button>
  </div>
  <div id="c-socios" style="display:none">
    <div class="inp-wrap">
      <label class="inp-lbl">TOTAL DE SÓCIOS (incluindo você)</label>
      <input class="inp sm" id="i-n-socios" type="number" min="2" max="20" placeholder="2"
        oninput="D.n_socios=parseInt(this.value)||2">
    </div>
    <label class="inp-lbl" style="margin-bottom:8px">ALGUM DE VOCÊS TRABALHA ATIVAMENTE NO NEGÓCIO?</label>
    <div class="ops g2" style="margin-bottom:16px">
      <button class="op chip" onclick="pick(this,'socio_ativo','sim')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sim</div></div></button>
      <button class="op chip" onclick="pick(this,'socio_ativo','nao')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não</div></div></button>
    </div>
    <button class="btn" onclick="ir('t11')">CONTINUAR →</button>
  </div>
  <button class="btn-sec" onclick="ir('t9')">← Voltar</button>
</div>

<!-- T11 DEPENDÊNCIA -->
<div class="tela" id="t11">
  <div class="sec-label">Identidade</div>
  <div class="t-num">11</div>
  <div class="t-q">Sem vocês por 30 dias, o que aconteceria com o negócio?</div>
  <div class="t-sub" style="margin-bottom:22px"> </div>
  <div class="ops" style="margin-bottom:16px">
    <button class="op" onclick="pick(this,'dependencia','independente');esconder('c-dep-desc');ir('t11b')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Funcionaria normalmente</div><div class="op-sub">A equipe toca tudo sem os sócios</div></div></button>
    <button class="op" onclick="pick(this,'dependencia','parcial');mostrar('c-dep-desc')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Funcionaria com dificuldades</div><div class="op-sub">Algumas coisas travariam</div></div></button>
    <button class="op" onclick="pick(this,'dependencia','dependente');mostrar('c-dep-desc')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Pararia</div><div class="op-sub">O negócio depende diretamente dos sócios</div></div></button>
  </div>
  <div id="c-dep-desc" style="display:none;margin-bottom:16px">
    <input class="inp" id="i-dep-desc" type="text"
      placeholder="Descreva quais dificuldades..." oninput="D.dependencia_desc=this.value">
  </div>
  <button class="btn" id="btn11" onclick="ir('t11b')">CONTINUAR →</button>
  <button class="btn-sec" onclick="ir('t10')">← Voltar</button>
</div>

<!-- T11B — ISSO JÁ ACONTECEU? -->
<div class="tela" id="t11b">
  <div class="sec-label">Identidade</div>
  <div class="t-num">11b</div>
  <div class="t-q">E isso já aconteceu alguma vez?</div>
  <div class="t-sub">Ficaram ausentes por um período prolongado.</div>
  <div class="ops g2" style="margin-bottom:22px">
    <button class="op" onclick="pick(this,'ausencia_ocorreu','sim');ir('t12')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sim</div></div></button>
    <button class="op" onclick="pick(this,'ausencia_ocorreu','nao');ir('t12')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não</div></div></button>
  </div>
  <button class="btn-sec" onclick="ir('t11')">← Voltar</button>
</div>

<!-- T12 REGIME -->
<div class="tela" id="t12">
  <div class="sec-label">Identidade</div>
  <div class="t-num">12</div>
  <div class="t-q" id="t12-q">Qual o regime tributário atual?</div>
  <div class="t-sub">Se houver economia em outro, te informaremos ao final.</div>
  <div class="ops">
    <button class="op chip" onclick="pick(this,'regime','mei');ir('t13')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">MEI</div></div></button>
    <button class="op chip" onclick="pick(this,'regime','simples');ir('t13')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Simples Nacional</div></div></button>
    <button class="op chip" onclick="pick(this,'regime','presumido');ir('t13')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Lucro Presumido</div></div></button>
    <button class="op chip" onclick="pick(this,'regime','real');ir('t13')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Lucro Real</div></div></button>
    <button class="op chip" onclick="pick(this,'regime','nao_sei');ir('t13')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não sei informar</div></div></button>
  </div>
  <button class="btn-sec" onclick="ir('t11b')">← Voltar</button>
</div>

<!-- T13 ALVARÁ -->
<div class="tela" id="t13">
  <div class="sec-label">Identidade</div>
  <div class="t-num">13</div>
  <div class="t-q" id="t13-q">O negócio precisa de alguma licença especial para operar?</div>
  <div class="ops g2" style="margin-bottom:16px">
    <button class="op" onclick="pick(this,'alvara','sim');mostrar('c-alvara')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sim</div></div></button>
    <button class="op" onclick="pick(this,'alvara','nao');esconder('c-alvara');ir('t14')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não</div></div></button>
  </div>
  <div id="c-alvara" style="display:none">
    <input class="inp sm" id="i-alvara-tipo" type="text"
      placeholder="Ex: Vigilância sanitária, CRM, OAB..."
      style="margin-bottom:12px" oninput="D.alvara_tipo=this.value">
    <label class="inp-lbl">TODOS OS ALVARÁS ESTÃO EM DIA?</label>
    <div class="ops g2" style="margin-bottom:12px">
      <button class="op chip" onclick="pick(this,'alvara_ok','sim')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sim, todos</div></div></button>
      <button class="op chip" onclick="pick(this,'alvara_ok','pendencia');mostrar('c-alvara-sit')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Tem pendência</div></div></button>
    </div>
    <div id="c-alvara-sit" style="display:none;margin-bottom:12px">
      <input class="inp sm" id="i-alvara-sit" type="text" placeholder="Qual a situação?" oninput="D.alvara_situacao=this.value">
    </div>
    <button class="btn" onclick="ir('t14')">CONTINUAR →</button>
  </div>
  <button class="btn-sec" onclick="ir('t12')">← Voltar</button>
</div>

<!-- T14 BENEFÍCIO FISCAL -->
<div class="tela" id="t14">
  <div class="sec-label">Identidade</div>
  <div class="t-num">14</div>
  <div class="t-q">E tem algum benefício fiscal especial? Descontos, incentivos...</div>
  <div class="t-sub">Zona Franca, supersimples específico, incentivo municipal, etc.</div>
  <div class="ops g2" style="margin-bottom:16px">
    <button class="op" onclick="pick(this,'beneficio_fiscal','sim');mostrar('c-beneficio')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sim</div></div></button>
    <button class="op" onclick="pick(this,'beneficio_fiscal','nao');esconder('c-beneficio');ir('t15')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não</div></div></button>
  </div>
  <div id="c-beneficio" style="display:none">
    <input class="inp" id="i-beneficio-desc" type="text"
      placeholder="Ex: Zona Franca, Supersimples específico, incentivo municipal..."
      style="margin-bottom:16px" oninput="D.beneficio_desc=this.value">
    <button class="btn" onclick="ir('t15')">CONTINUAR →</button>
  </div>
  <button class="btn-sec" onclick="ir('t13')">← Voltar</button>
</div>

<!-- T15 MARCA E REPUTAÇÃO -->
<div class="tela" id="t15">
  <div class="sec-label">Identidade</div>
  <div class="t-num">15</div>
  <div class="t-q">Sobre marca e reputação</div>
  <div class="t-sub">Dois assuntos que importam nos dias de hoje.</div>
  <label class="inp-lbl">PRA COMEÇAR, A MARCA ESTÁ REGISTRADA NO INPI?</label>
  <div class="ops g3" style="margin-bottom:16px">
    <button class="op chip" onclick="pick(this,'marca_inpi','registrada')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Registrada</div></div></button>
    <button class="op chip" onclick="pick(this,'marca_inpi','processo')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Em processo</div></div></button>
    <button class="op chip" onclick="pick(this,'marca_inpi','sem_registro')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sem registro</div></div></button>
  </div>
  <label class="inp-lbl">COMO O PÚBLICO AVALIA A ATIVIDADE DO NEGÓCIO?</label>
  <div class="ops" style="margin-bottom:22px">
    <button class="op chip" onclick="pick(this,'reputacao','excelente')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Excelente — indicam muito</div></div></button>
    <button class="op chip" onclick="pick(this,'reputacao','boa')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Boa — sem problemas relevantes</div></div></button>
    <button class="op chip" onclick="pick(this,'reputacao','neutra')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Neutra — pouco conhecido ainda</div></div></button>
    <button class="op chip" onclick="pick(this,'reputacao','problemas')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Já teve problemas de reputação</div></div></button>
  </div>
  <button class="btn" id="btn15" disabled onclick="ir('t16')">CONTINUAR →</button>
  <button class="btn-sec" onclick="ir('t14')">← Voltar</button>
</div>

<!-- T16 PRESENÇA ONLINE -->
<div class="tela" id="t16">
  <div class="sec-label">Identidade</div>
  <div class="t-num">16</div>
  <div class="t-q" id="t16-q">O negócio tem presença online?</div>
  <div class="t-sub">Selecione tudo que se aplica.</div>
  <div class="ops">
    <button class="op" onclick="toggleMulti(this,'online','nenhum')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sem presença digital relevante</div></div></button>
    <button class="op" onclick="toggleMulti(this,'online','site')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Site próprio</div></div></button>
    <button class="op" onclick="toggleMulti(this,'online','ecommerce')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">E-commerce</div></div></button>
    <button class="op" onclick="toggleMulti(this,'online','instagram')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Instagram / redes sociais ativas</div></div></button>
    <button class="op" onclick="toggleMulti(this,'online','gmaps')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Google Meu Negócio / Google Maps</div></div></button>
    <button class="op" onclick="toggleMulti(this,'online','marketplace')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Marketplace (Mercado Livre, iFood, etc.)</div></div></button>
  </div>
  <button class="btn" id="btn16" disabled onclick="ir('t17')">CONTINUAR →</button>
  <button class="btn-sec" onclick="ir('t15')">← Voltar</button>
</div>

<!-- T17 FATURAMENTO -->
<div class="tela" id="t17">
  <div class="sec-label">Faturamento</div>
  <div class="t-num">17</div>
  <div class="t-q">Quanto faturou nos últimos 12 meses?</div>
  <div class="t-sub">Faturamento bruto total — sem descontar impostos ou custos.</div>
  <div class="mode-toggle" id="tog-fat">
    <div class="mode-btn" onclick="selMode(this,'fat_modo','anual','tog-fat');mostrar('c-fat-anual');esconder('c-fat-mensal')">INFORMAR ANUAL</div>
    <div class="mode-btn sel" onclick="selMode(this,'fat_modo','mensal','tog-fat');mostrar('c-fat-mensal');esconder('c-fat-anual')">INFORMAR MÉDIA / MÊS</div>
  </div>
  <div id="c-fat-anual" style="display:none" data-default-hidden="1">
    <label class="inp-lbl">TOTAL DOS ÚLTIMOS 12 MESES</label>
    <input class="inp inp-money" id="i-fat-anual" type="text" placeholder="R$ 0"
      oninput="maskMoney(this);D.fat_anual=parseMasked('i-fat-anual');D.fat_mensal=D.fat_anual/12;atualizarFatDisplay()" style="margin-bottom:6px">
  </div>
  <div id="c-fat-mensal">
    <label class="inp-lbl">MÉDIA MENSAL</label>
    <input class="inp inp-money" id="i-fat-mensal" type="text" placeholder="R$ 0"
      oninput="maskMoney(this);D.fat_mensal=parseMasked('i-fat-mensal');D.fat_anual=D.fat_mensal*12;atualizarFatDisplay()" style="margin-bottom:6px">
  </div>
  <div class="res-box" id="fat-display" style="display:none;margin-bottom:16px">
    <span class="res-lbl">Equivale a</span>
    <span class="res-val" id="fat-display-val">—</span>
  </div>
  <button class="btn" id="btn17" disabled onclick="ir('t18')">CONTINUAR →</button>
  <button class="btn-sec" onclick="ir('t16')">← Voltar</button>
</div>

<!-- T18 DECLARAÇÃO -->
<div class="tela" id="t18">
  <div class="sec-label">Faturamento</div>
  <div class="t-num">18</div>
  <div class="t-q">Esse faturamento é 100% declarado?</div>
  <div class="ops" style="margin-bottom:22px">
    <button class="op" onclick="pick(this,'declarado','sim');ir('t19')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sim — 100% com nota fiscal</div></div></button>
    <button class="op" onclick="pick(this,'declarado','parcial');ir('t19')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não — parte sem nota fiscal</div></div></button>
  </div>
  <button class="btn-sec" onclick="ir('t17')">← Voltar</button>
</div>

<!-- T19 CRESCIMENTO -->
<div class="tela" id="t19">
  <div class="sec-label">Faturamento</div>
  <div class="t-num">19</div>
  <div class="t-q">Olhando pra trás — como foi o crescimento neste último ano?</div>
  <div class="ops">
    <button class="op chip" onclick="pick(this,'crescimento','mais20');ir('t20')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Cresceu mais de 20%</div></div></button>
    <button class="op chip" onclick="pick(this,'crescimento','10a20');ir('t20')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Cresceu de 10% a 20%</div></div></button>
    <button class="op chip" onclick="pick(this,'crescimento','ate10');ir('t20')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Cresceu até 10%</div></div></button>
    <button class="op chip" onclick="pick(this,'crescimento','estavel');ir('t20')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Ficou estável</div></div></button>
    <button class="op chip" onclick="pick(this,'crescimento','caiu10');ir('t20')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Caiu até 10%</div></div></button>
    <button class="op chip" onclick="pick(this,'crescimento','caiu_mais');ir('t20')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Caiu mais que 10%</div></div></button>
  </div>
  <button class="btn-sec" onclick="ir('t18')">← Voltar</button>
</div>

<!-- T20 RECORRÊNCIA -->
<div class="tela" id="t20">
  <div class="sec-label">Faturamento</div>
  <div class="t-num">20</div>
  <div class="t-q">Tem receita recorrente?</div>
  <div class="t-sub">Mensalidades, assinaturas, contratos fixos — receita que se repete todo mês.</div>
  <div class="ops g2" style="margin-bottom:16px">
    <button class="op" onclick="pick(this,'recorrencia','sim');mostrar('c-recorrencia')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sim</div></div></button>
    <button class="op" onclick="pick(this,'recorrencia','nao');esconder('c-recorrencia');ir('t21')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não</div></div></button>
  </div>
  <div id="c-recorrencia" style="display:none">
    <label class="inp-lbl">% DO FATURAMENTO RECORRENTE</label>
    <div class="pills" style="margin-bottom:10px">
      <button class="pill" onclick="selPill(this,'pct_recorrencia','10')">~10%</button>
      <button class="pill" onclick="selPill(this,'pct_recorrencia','25')">~25%</button>
      <button class="pill" onclick="selPill(this,'pct_recorrencia','50')">~50%</button>
      <button class="pill" onclick="selPill(this,'pct_recorrencia','75')">~75%</button>
      <button class="pill" onclick="selPill(this,'pct_recorrencia','90')">+90%</button>
      <button class="pill" onclick="selPill(this,'pct_recorrencia','100')">100%</button>
    </div>
    <!-- Digitar % -->
    <div class="inp-wrap" style="margin-bottom:8px">
      <input class="inp sm" id="i-pct-recorrencia" type="number" min="1" max="100"
        placeholder="Ou digite o %"
        oninput="D.pct_recorrencia=this.value;atualizarRecorrenciaVal()">
    </div>
    <!-- Valor calculado -->
    <div class="res-box" id="recorrencia-val-box" style="display:none;margin-bottom:12px">
      <span class="res-lbl">Equivale a</span>
      <span class="res-val" id="recorrencia-val">—</span>
    </div>
    <button class="btn" onclick="ir('t21')">CONTINUAR →</button>
  </div>
  <button class="btn-sec" onclick="ir('t19')">← Voltar</button>
</div>

<!-- T21 BREAKDOWN -->
<div class="tela" id="t21">
  <div class="sec-label">Faturamento</div>
  <div class="t-num">21</div>
  <div class="t-q">Você separa o faturamento por produto ou categoria?</div>
  <div class="t-sub">Resposta opcional — útil quando há linhas de receita com margens muito diferentes.</div>
  <div class="ops g2" style="margin-bottom:16px">
    <button class="op" onclick="pick(this,'breakdown','sim');mostrar('c-breakdown')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sim, tenho</div></div></button>
    <button class="op" onclick="pick(this,'breakdown','nao');esconder('c-breakdown');ir('t22')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não / Prefiro pular</div></div></button>
  </div>
  <div id="c-breakdown" style="display:none">
    <div class="inp-grid">
      <div><label class="inp-lbl">LINHA 1</label><input class="inp sm" id="bd1-nome" placeholder="Ex: Almoço executivo" oninput="D.bd1_nome=this.value"></div>
      <div><label class="inp-lbl">% DO FAT.</label><input class="inp sm" id="bd1-pct" type="number" min="0" max="100" placeholder="40" oninput="D.bd1_pct=this.value"></div>
    </div>
    <div class="inp-grid">
      <div><label class="inp-lbl">LINHA 2</label><input class="inp sm" id="bd2-nome" placeholder="Ex: Jantar à la carte" oninput="D.bd2_nome=this.value"></div>
      <div><label class="inp-lbl">% DO FAT.</label><input class="inp sm" id="bd2-pct" type="number" min="0" max="100" placeholder="40" oninput="D.bd2_pct=this.value"></div>
    </div>
    <div class="inp-grid" style="margin-bottom:16px">
      <div><label class="inp-lbl">LINHA 3</label><input class="inp sm" id="bd3-nome" placeholder="Ex: Eventos privados" oninput="D.bd3_nome=this.value"></div>
      <div><label class="inp-lbl">% DO FAT.</label><input class="inp sm" id="bd3-pct" type="number" min="0" max="100" placeholder="20" oninput="D.bd3_pct=this.value"></div>
    </div>
    <button class="btn" onclick="ir('t22')">CONTINUAR →</button>
  </div>
  <button class="btn-sec" onclick="ir('t20')">← Voltar</button>
</div>

<!-- T22 CANAIS -->
<div class="tela" id="t22">
  <div class="sec-label">Comercial</div>
  <div class="t-num">22</div>
  <div class="t-q" id="t22-q">Por onde o negócio vende?</div>
  <div class="t-sub">Selecione todos os canais ativos.</div>
  <div class="ops">
    <button class="op" onclick="toggleMulti(this,'canais','loja_fisica')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Loja física</div></div></button>
    <button class="op" onclick="toggleMulti(this,'canais','ecommerce_proprio')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">E-commerce próprio</div></div></button>
    <button class="op" onclick="toggleMulti(this,'canais','marketplace')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Marketplace (Mercado Livre, Shopee, etc.)</div></div></button>
    <button class="op" onclick="toggleMulti(this,'canais','delivery')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Delivery (iFood, Rappi, etc.)</div></div></button>
    <button class="op" onclick="toggleMulti(this,'canais','representantes')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Representantes comerciais</div></div></button>
    <button class="op" onclick="toggleMulti(this,'canais','whatsapp')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">WhatsApp / venda direta</div></div></button>
    <button class="op" onclick="toggleMulti(this,'canais','outros');mostrar('c-canais-outros')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Outros</div></div></button>
  </div>
  <div id="c-canais-outros" style="display:none;margin-bottom:12px">
    <input class="inp sm" id="i-canais-outros" type="text" placeholder="Quais outros canais?" oninput="D.canais_outros=this.value">
  </div>
  <button class="btn" id="btn22" disabled onclick="ir('t23')">CONTINUAR →</button>
  <button class="btn-sec" onclick="ir('t21')">← Voltar</button>
</div>

<!-- T23 MARKETING PAGO -->
<div class="tela" id="t23">
  <div class="sec-label">Comercial</div>
  <div class="t-num">23</div>
  <div class="t-q">O negócio investe em marketing pago?</div>
  <div class="ops" style="margin-bottom:16px">
    <button class="op" onclick="pick(this,'mkt_pago','sim');mostrar('c-mkt')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sim</div></div></button>
    <button class="op" onclick="pick(this,'mkt_pago','nao');esconder('c-mkt');ir('t24')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não / só orgânico</div></div></button>
  </div>
  <div id="c-mkt" style="display:none">
    <div class="inp-wrap">
      <label class="inp-lbl">INVESTIMENTO MENSAL EM MARKETING PAGO</label>
      <input class="inp inp-money" id="i-mkt" type="text" placeholder="R$ 0"
        oninput="maskMoney(this);D.mkt_valor=parseMasked('i-mkt')">
      <!-- Se franquia, mostrar que fundo já foi contabilizado -->
      <div class="inp-hint" id="mkt-hint-franquia" style="display:none">O fundo de marketing da franquia já foi incluído. Informe apenas investimento adicional.</div>
    </div>
    <button class="btn" onclick="ir('t24')">CONTINUAR →</button>
  </div>
  <button class="btn-sec" onclick="ir('t22')">← Voltar</button>
</div>

<!-- T24 PRAZO + CONTAS A RECEBER -->
<div class="tela" id="t24">
  <div class="sec-label">Comercial</div>
  <div class="t-num">24</div>
  <div class="t-q">Vende a prazo ou parcelado?</div>
  <div class="ops g2" style="margin-bottom:16px">
    <button class="op" onclick="pick(this,'prazo_venda','avista');mostrar('c-cr-sempre')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Somente à vista dinheiro / débito / pix</div></div></button>
    <button class="op" onclick="pick(this,'prazo_venda','prazo');mostrar('c-prazo');mostrar('c-cr-sempre')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Vende a prazo, parcelado</div></div></button>
  </div>
  <div id="c-prazo" style="display:none">
    <label class="inp-lbl">PRAZO MÉDIO DE RECEBIMENTO</label>
    <div class="pills" style="margin-bottom:16px">
      <button class="pill" onclick="selPill(this,'prazo_dias','30')">1x (30 dias)</button>
      <button class="pill" onclick="selPill(this,'prazo_dias','60')">2x (60 dias)</button>
      <button class="pill" onclick="selPill(this,'prazo_dias','90')">3x (90 dias)</button>
      <button class="pill" onclick="selPill(this,'prazo_dias','120')">4x+ (120+ dias)</button>
    </div>
  </div>
  <!-- Contas a receber SEMPRE aparece -->
  <div id="c-cr-sempre" style="display:none">
    <div class="inp-wrap" style="margin-bottom:22px">
      <label class="inp-lbl">QUANTO TEM A RECEBER HOJE (CONTAS A RECEBER)</label>
      <input class="inp inp-money" id="i-cr" type="text" placeholder="R$ 0"
        oninput="maskMoney(this);D.contas_receber=parseMasked('i-cr')">
      <div class="inp-hint">Total de vendas realizadas ainda não recebidas</div>
    </div>
    <button class="btn" onclick="ir('t25')">CONTINUAR →</button>
  </div>
  <button class="btn-sec" onclick="ir('t23')">← Voltar</button>
</div>

<!-- T25 ANTECIPAÇÃO -->
<div class="tela" id="t25">
  <div class="sec-label">Comercial</div>
  <div class="t-num">25</div>
  <div class="t-q">Antecipa seus recebíveis ou desconta duplicatas?</div>
  <div class="ops" style="margin-bottom:16px">
    <button class="op chip" onclick="pick(this,'antecipacao','frequente');mostrar('c-antecip')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Frequentemente</div></div></button>
    <button class="op chip" onclick="pick(this,'antecipacao','as_vezes');mostrar('c-antecip')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Às vezes</div></div></button>
    <button class="op chip" onclick="pick(this,'antecipacao','nunca');esconder('c-antecip');ir('t26')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Nunca</div></div></button>
  </div>
  <div id="c-antecip" style="display:none">
    <label class="inp-lbl">TAXA MÉDIA DE ANTECIPAÇÃO (% AO MÊS)</label>
    <div class="pills" style="margin-bottom:16px">
      <button class="pill" onclick="selPill(this,'taxa_antecip','1')">~1%</button>
      <button class="pill" onclick="selPill(this,'taxa_antecip','2')">~2%</button>
      <button class="pill" onclick="selPill(this,'taxa_antecip','3')">~3%</button>
      <button class="pill" onclick="selPill(this,'taxa_antecip','4')">~4%</button>
      <button class="pill" onclick="selPill(this,'taxa_antecip','5mais')">5%+</button>
    </div>
    <button class="btn" onclick="ir('t26')">CONTINUAR →</button>
  </div>
  <button class="btn-sec" onclick="ir('t24')">← Voltar</button>
</div>

<!-- T26 COMISSÕES -->
<div class="tela" id="t26">
  <div class="sec-label">Comercial</div>
  <div class="t-num">26</div>
  <div class="t-q">Paga comissão sobre vendas?</div>
  <div class="ops g2" style="margin-bottom:16px">
    <button class="op" onclick="pick(this,'comissao','sim');mostrar('c-comissao')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sim</div></div></button>
    <button class="op" onclick="pick(this,'comissao','nao');esconder('c-comissao');ir('t27')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não</div></div></button>
  </div>
  <div id="c-comissao" style="display:none">
    <label class="inp-lbl">% DO FATURAMENTO EM COMISSÕES</label>
    <div class="pills" style="margin-bottom:10px">
      <button class="pill" onclick="selPill(this,'pct_comissao','5');calcComissaoValor()">~5%</button>
      <button class="pill" onclick="selPill(this,'pct_comissao','10');calcComissaoValor()">~10%</button>
      <button class="pill" onclick="selPill(this,'pct_comissao','15');calcComissaoValor()">~15%</button>
      <button class="pill" onclick="selPill(this,'pct_comissao','20');calcComissaoValor()">~20%</button>
    </div>
    <div class="inp-wrap" style="margin-bottom:8px">
      <input class="inp sm" id="i-pct-comissao" type="number" min="0" max="100"
        placeholder="Ou digite o %"
        oninput="D.pct_comissao=this.value;calcComissaoValor()">
    </div>
    <div class="res-box am" id="comissao-val-box" style="display:none;margin-bottom:12px">
      <span class="res-lbl">Equivale a</span>
      <span class="res-val" id="comissao-val">—</span>
    </div>
    <div class="inp-wrap" style="margin-bottom:16px">
      <label class="inp-lbl">OU INFORME O VALOR MENSAL (R$)</label>
      <input class="inp inp-money sm" id="i-comissao-val" type="text" placeholder="R$ 0"
        oninput="maskMoney(this);D.comissao_val_direto=parseMasked('i-comissao-val')">
    </div>
    <button class="btn" onclick="ir('t27')">CONTINUAR →</button>
  </div>
  <button class="btn-sec" onclick="ir('t25')">← Voltar</button>
</div>

<!-- T27 PROCESSADORES (T27 anterior "forma de pagamento" foi deletado; renumerado) -->
<div class="tela" id="t27">
  <div class="sec-label">Comercial</div>
  <div class="t-num">27</div>
  <div class="t-q">Quanto do faturamento passa por processadores de pagamento?</div>
  <div class="t-sub">Maquininha de cartão, pagamentos por sites ou portais, links de pagamento, gateways...</div>
  <div class="pills" style="margin-bottom:16px">
    <button class="pill" onclick="selPill(this,'pct_proc','20')">~20%</button>
    <button class="pill" onclick="selPill(this,'pct_proc','40')">~40%</button>
    <button class="pill" onclick="selPill(this,'pct_proc','60')">~60%</button>
    <button class="pill" onclick="selPill(this,'pct_proc','80')">~80%</button>
    <button class="pill" onclick="selPill(this,'pct_proc','100')">~100%</button>
  </div>
  <label class="inp-lbl">TAXA MÉDIA COBRADA PELOS PROCESSADORES</label>
  <div class="pills" style="margin-bottom:22px">
    <button class="pill" onclick="selPill(this,'taxa_proc','1')">~1%</button>
    <button class="pill" onclick="selPill(this,'taxa_proc','2')">~2%</button>
    <button class="pill" onclick="selPill(this,'taxa_proc','3')">~3%</button>
    <button class="pill" onclick="selPill(this,'taxa_proc','4')">~4%</button>
    <button class="pill" onclick="selPill(this,'taxa_proc','5mais')">5%+</button>
  </div>
  <button class="btn" id="btn27" disabled onclick="ir('t28')">CONTINUAR →</button>
  <button class="btn-sec" onclick="ir('t26')">← Voltar</button>
</div>

<!-- T28 CONCENTRAÇÃO (slider) -->
<div class="tela" id="t28">
  <div class="sec-label">Comercial</div>
  <div class="t-num">28</div>
  <div class="t-q">Seus 3 maiores clientes representam quanto % do faturamento?</div>
  <div class="t-sub">Alta concentração reduz o valuation — compradores veem isso como risco.</div>
  <div class="slider-wrap">
    <div class="slider-val" id="conc-val">20%</div>
    <input class="slider" type="range" min="0" max="100" value="20" id="sl-conc"
      oninput="D.concentracao_pct=parseInt(this.value);document.getElementById('conc-val').textContent=this.value+'%';atualizarConcHint()">
    <div class="slider-labels"><span>0%</span><span>100%</span></div>
  </div>
  <div class="inp-hint" id="conc-hint" style="text-align:center;margin-bottom:22px"></div>
  <button class="btn" onclick="ir('t29')">CONTINUAR →</button>
  <button class="btn-sec" onclick="ir('t27')">← Voltar</button>
</div>

<!-- T29 CRM -->
<div class="tela" id="t29">
  <div class="sec-label">Comercial</div>
  <div class="t-num">29</div>
  <div class="t-q">Você mantém registro dos seus clientes?</div>
  <div class="ops" style="margin-bottom:16px">
    <button class="op" onclick="pick(this,'crm','sim_crm');mostrar('c-crm-nome');esconder('c-crm-planilha')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sim, uso um CRM</div><div class="op-sub">Software dedicado de gestão de clientes</div></div></button>
    <button class="op" onclick="pick(this,'crm','planilha');esconder('c-crm-nome');mostrar('c-crm-planilha')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sim, uso uma planilha ou forma própria</div></div></button>
    <button class="op" onclick="pick(this,'crm','nao');esconder('c-crm-nome');esconder('c-crm-planilha')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não mantenho registro</div></div></button>
  </div>
  <div id="c-crm-nome" style="display:none;margin-bottom:12px">
    <input class="inp sm" id="i-crm-nome" type="text" placeholder="Ex: RD Station, Hubspot, Pipedrive..." oninput="D.crm_nome=this.value">
  </div>
  <div id="c-crm-planilha" style="display:none;margin-bottom:12px">
    <div class="inp-hint">OK — o importante é ter os dados organizados.</div>
  </div>
  <div class="sep"></div>
  <div class="t-sub" style="margin:12px 0 10px">Tamanho da base de clientes</div>
  <div class="inp-grid3">
    <div><label class="inp-lbl">ÚLTIMO MÊS</label><input class="inp sm" id="i-cli-1m" type="number" placeholder="0" oninput="D.cli_1m=parseInt(this.value)||0"></div>
    <div><label class="inp-lbl">ÚLTIMOS 12M</label><input class="inp sm" id="i-cli-12m" type="number" placeholder="0" oninput="D.cli_12m=parseInt(this.value)||0"></div>
    <div><label class="inp-lbl">TOTAL BASE</label><input class="inp sm" id="i-cli-total" type="number" placeholder="0" oninput="D.cli_total=parseInt(this.value)||0"></div>
  </div>
  <button class="btn" onclick="ir('t30')">CONTINUAR →</button>
  <button class="btn-sec" onclick="ir('t28')">← Voltar</button>
</div>

<!-- T30 CMV (abre com calculadora já visível) -->
<div class="tela" id="t30">
  <div class="sec-label">Custos</div>
  <div class="t-num">30</div>
  <div class="t-q">Qual o custo direto de mercadoria ou produção?</div>
  <div class="t-sub">Se sua atividade é predominantemente serviço (não vende produto físico), pode pular.</div>
  <div class="mode-toggle" id="tog-cmv">
    <div class="mode-btn sel" onclick="selMode(this,'cmv_modo','calc','tog-cmv');mostrar('c-cmv-calc');esconder('c-cmv-direto')">ME CUSTA × VENDO POR</div>
    <div class="mode-btn" onclick="selMode(this,'cmv_modo','direto','tog-cmv');esconder('c-cmv-calc');mostrar('c-cmv-direto')">INFORMAR CMV / CPV</div>
  </div>
  <!-- Calculadora aberta por padrão -->
  <div id="c-cmv-calc">
    <div class="calc-wrap">
      <div class="calc-row">
        <div><label class="inp-lbl">ME CUSTA</label>
          <input class="inp inp-money sm" id="i-compra" type="text" placeholder="R$ 0" oninput="maskMoney(this);calcCMV()"></div>
        <div class="calc-op">→</div>
        <div><label class="inp-lbl">VENDO POR</label>
          <input class="inp inp-money sm" id="i-venda" type="text" placeholder="R$ 0" oninput="maskMoney(this);calcCMV()"></div>
      </div>
      <div class="calc-result" id="calc-cmv-res" style="display:none">
        <span class="calc-lbl">Margem bruta</span>
        <span class="calc-val" id="calc-cmv-val">—</span>
      </div>
    </div>
  </div>
  <div id="c-cmv-direto" style="display:none">
    <label class="inp-lbl">CMV / CPV % DO FATURAMENTO</label>
    <input class="inp sm" id="i-cmv-pct" type="number" min="0" max="100" placeholder="45"
      oninput="D.cmv_pct=parseFloat(this.value)||0;D.margem_bruta=100-D.cmv_pct" style="margin-bottom:10px">
  </div>
  <div class="inp-wrap" style="margin-top:12px">
    <label class="inp-lbl">VOCÊ TEM ROTINA DE CONTROLE DE PRECIFICAÇÃO?</label>
    <div class="ops g2">
      <button class="op chip" onclick="pick(this,'controle_preco','sim')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sim</div></div></button>
      <button class="op chip" onclick="pick(this,'controle_preco','nao')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não</div></div></button>
    </div>
  </div>
  <button class="btn" onclick="ir('t31')">CONTINUAR →</button>
  <button class="btn-skip" onclick="D.cmv_pct=0;D.cmv_modo='servico';ir('t31')">Sou serviço puro — pular →</button>
  <button class="btn-sec" onclick="ir('t29')">← Voltar</button>
</div>

<!-- T31 FORNECEDORES / ESTOQUE -->
<div class="tela" id="t31">
  <div class="sec-label">Custos</div>
  <div class="t-num">31</div>
  <div class="t-q">Como o negócio paga seus fornecedores?</div>
  <div class="pills" style="margin-bottom:16px">
    <button class="pill" onclick="selPill(this,'pgto_forn','antecipado')">Antecipado</button>
    <button class="pill" onclick="selPill(this,'pgto_forn','avista')">À vista na entrega</button>
    <button class="pill" onclick="selPill(this,'pgto_forn','30d')">30 dias</button>
    <button class="pill" onclick="selPill(this,'pgto_forn','60d')">60 dias</button>
    <button class="pill" onclick="selPill(this,'pgto_forn','90d')">90+ dias</button>
  </div>
  <div class="inp-wrap">
    <label class="inp-lbl">CONTAS A PAGAR A FORNECEDORES HOJE</label>
    <input class="inp inp-money" id="i-cp" type="text" placeholder="R$ 0"
      oninput="maskMoney(this);D.contas_pagar=parseMasked('i-cp')">
  </div>
  <div class="inp-wrap" style="margin-bottom:22px">
    <label class="inp-lbl">POSIÇÃO DE ESTOQUE ATUAL (valor de custo)</label>
    <input class="inp inp-money" id="i-estoque" type="text" placeholder="R$ 0"
      oninput="maskMoney(this);D.estoque=parseMasked('i-estoque')">
    <div class="inp-hint">Se não tem estoque físico, deixe em R$ 0</div>
  </div>
  <button class="btn" onclick="ir('t32')">CONTINUAR →</button>
  <button class="btn-sec" onclick="ir('t30')">← Voltar</button>
</div>

<!-- T32 EQUIPE -->
<div class="tela" id="t32">
  <div class="sec-label">Equipe</div>
  <div class="t-num">32</div>
  <div class="t-q" id="t32-q">Qual o tamanho do time?</div>
  <div class="t-sub">Não inclua os sócios e pró-labore / retiradas aqui.</div>
  <div class="inp-grid">
    <div><label class="inp-lbl">CLT (carteira)</label><input class="inp sm" id="i-clt-qtd" type="number" min="0" placeholder="0" oninput="D.clt_qtd=parseInt(this.value)||0;calcFolha()"></div>
    <div><label class="inp-lbl">FOLHA CLT / MÊS</label><input class="inp inp-money sm" id="i-clt-folha" type="text" placeholder="R$ 0" oninput="maskMoney(this);D.clt_folha=parseMasked('i-clt-folha');calcFolha()"></div>
  </div>
  <div class="inp-grid">
    <div><label class="inp-lbl">PJ / FREELA</label><input class="inp sm" id="i-pj-qtd" type="number" min="0" placeholder="0" oninput="D.pj_qtd=parseInt(this.value)||0;calcFolha()"></div>
    <div><label class="inp-lbl">CUSTO PJ / MÊS</label><input class="inp inp-money sm" id="i-pj-custo" type="text" placeholder="R$ 0" oninput="maskMoney(this);D.pj_custo=parseMasked('i-pj-custo');calcFolha()"></div>
  </div>
  <div class="inp-hint" style="margin-bottom:10px">Informe valor bruto — calcularemos os tributos automaticamente.</div>
  <div class="res-box" id="folha-total-box" style="display:none;margin-bottom:16px">
    <span class="res-lbl">Custo total de pessoal / mês</span>
    <span class="res-val" id="folha-total-val">—</span>
  </div>
  <button class="btn" onclick="ir('t33')">CONTINUAR →</button>
  <button class="btn-sec" onclick="ir('t31')">← Voltar</button>
</div>

<!-- T33 PRÓ-LABORE / CAIXA -->
<div class="tela" id="t33">
  <div class="sec-label">Equipe</div>
  <div class="t-num">33</div>
  <div class="t-q">Como os sócios são remunerados?</div>
  <div class="ops" style="margin-bottom:16px">
    <button class="op chip" onclick="pick(this,'remuneracao_socios','fixo');mostrar('c-prolabore-val')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Pró-labore fixo mensal</div></div></button>
    <button class="op chip" onclick="pick(this,'remuneracao_socios','sobra');esconder('c-prolabore-val')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Distribuição do que sobra</div></div></button>
    <button class="op chip" onclick="pick(this,'remuneracao_socios','nao');esconder('c-prolabore-val')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não retiram ainda</div></div></button>
  </div>
  <div id="c-prolabore-val" style="display:none">
    <div class="inp-wrap">
      <label class="inp-lbl">TOTAL RETIRADO / MÊS</label>
      <input class="inp inp-money sm" id="i-prolabore" type="text" placeholder="R$ 0"
        oninput="maskMoney(this);D.prolabore=parseMasked('i-prolabore')">
      <div class="inp-hint">Pró-labore de todos os sócios somado</div>
    </div>
  </div>
  <div class="inp-wrap" style="margin-bottom:22px">
    <label class="inp-lbl">CAIXA DISPONÍVEL HOJE</label>
    <input class="inp inp-money sm" id="i-caixa" type="text" placeholder="R$ 0"
      oninput="maskMoney(this);D.caixa=parseMasked('i-caixa')">
    <div class="inp-hint">Disponibilidade total em dinheiro e contas</div>
  </div>
  <button class="btn" onclick="ir('t34')">CONTINUAR →</button>
  <button class="btn-sec" onclick="ir('t32')">← Voltar</button>
</div>

<!-- T34 GESTÃO -->
<div class="tela" id="t34">
  <div class="sec-label">Gestão</div>
  <div class="t-num">34</div>
  <div class="t-q">Como está a gestão do negócio?</div>
  <div class="t-sub">Três perguntas rápidas que compõem o score de solidez.</div>
  <label class="inp-lbl">OS PROCESSOS E ROTINAS ESTÃO DOCUMENTADOS?</label>
  <div class="ops g3" style="margin-bottom:14px">
    <button class="op chip" onclick="pick(this,'processos','sim')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Documentados</div></div></button>
    <button class="op chip" onclick="pick(this,'processos','parcial')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Parcialmente</div></div></button>
    <button class="op chip" onclick="pick(this,'processos','nao')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Tudo na cabeça</div></div></button>
  </div>
  <label class="inp-lbl">HÁ UM ORGANOGRAMA COM PAPÉIS DEFINIDOS?</label>
  <div class="ops g3" style="margin-bottom:14px">
    <button class="op chip" onclick="pick(this,'organograma','sim')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sim</div></div></button>
    <button class="op chip" onclick="pick(this,'organograma','informal')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Informal</div></div></button>
    <button class="op chip" onclick="pick(this,'organograma','nao')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não</div></div></button>
  </div>
  <label class="inp-lbl">EXISTE UM GESTOR QUE OPERARIA SEM OS SÓCIOS?</label>
  <div class="ops g2" style="margin-bottom:22px">
    <button class="op chip" onclick="pick(this,'gestor_autonomo','sim')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sim</div></div></button>
    <button class="op chip" onclick="pick(this,'gestor_autonomo','nao')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não</div></div></button>
  </div>
  <button class="btn" id="btn34" disabled onclick="ir('t35')">CONTINUAR →</button>
  <button class="btn-sec" onclick="ir('t33')">← Voltar</button>
</div>

<!-- T35 ERP (separado da T36 original) -->
<div class="tela" id="t35">
  <div class="sec-label">Gestão</div>
  <div class="t-num">35</div>
  <div class="t-q">O negócio usa sistema de gestão?</div>
  <div class="t-sub">ERP, CRM, sistema de ponto de venda, etc.</div>
  <div class="ops" style="margin-bottom:16px">
    <button class="op" onclick="pick(this,'erp','sim');mostrar('c-erp-nome')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sim</div></div></button>
    <button class="op chip" onclick="pick(this,'erp','parcial');esconder('c-erp-nome');ir('t36')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Parcialmente</div></div></button>
    <button class="op chip" onclick="pick(this,'erp','nao');esconder('c-erp-nome');ir('t36')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não</div></div></button>
  </div>
  <div id="c-erp-nome" style="display:none">
    <input class="inp sm" id="i-erp" type="text" placeholder="Ex: Totvs, Omie, Bling, Netsuite..."
      style="margin-bottom:16px" oninput="D.erp_nome=this.value">
    <button class="btn" onclick="ir('t36')">CONTINUAR →</button>
  </div>
  <button class="btn-sec" onclick="ir('t34')">← Voltar</button>
</div>

<!-- T36 CRESCIMENTO PROJETADO (separado) -->
<div class="tela" id="t36">
  <div class="sec-label">Gestão</div>
  <div class="t-num">36</div>
  <div class="t-q">Expectativa de crescimento nos próximos 12 meses</div>
  <div class="ops" style="margin-bottom:22px">
    <button class="op chip" onclick="pick(this,'crescimento_proj','queda');ir('t37')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Queda</div></div></button>
    <button class="op chip" onclick="pick(this,'crescimento_proj','estavel');ir('t37')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Estável</div></div></button>
    <button class="op chip" onclick="pick(this,'crescimento_proj','10');ir('t37')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Crescimento até 10%</div></div></button>
    <button class="op chip" onclick="pick(this,'crescimento_proj','20');ir('t37')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Crescimento até 20%</div></div></button>
    <button class="op chip" onclick="pick(this,'crescimento_proj','mais20');ir('t37')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Crescimento +20%</div></div></button>
  </div>
  <button class="btn-sec" onclick="ir('t35')">← Voltar</button>
</div>

<!-- T37 ESCALABILIDADE (separado) -->
<div class="tela" id="t37">
  <div class="sec-label">Gestão</div>
  <div class="t-num">37</div>
  <div class="t-q">Com a estrutura atual, quanto pode crescer sem aumentar custo fixo?</div>
  <div class="ops" style="margin-bottom:22px">
    <button class="op chip" onclick="pick(this,'scalability','limite');ir('t38')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Estamos no limite — precisaria investir</div></div></button>
    <button class="op chip" onclick="pick(this,'scalability','20');ir('t38')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Até +20%</div></div></button>
    <button class="op chip" onclick="pick(this,'scalability','50');ir('t38')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Até +50%</div></div></button>
    <button class="op chip" onclick="pick(this,'scalability','dobrar');ir('t38')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Poderia dobrar</div></div></button>
    <button class="op chip" onclick="pick(this,'scalability','triplicar');ir('t38')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Poderia triplicar</div></div></button>
  </div>
  <button class="btn-sec" onclick="ir('t36')">← Voltar</button>
</div>

<!-- T38 CUSTOS FIXOS (pré-preenchido) -->
<div class="tela" id="t38">
  <div class="sec-label">Custos fixos</div>
  <div class="t-num">38</div>
  <div class="t-q">Despesas fixas mensais</div>
  <div class="t-sub">O que o negócio paga todo mês independente do faturamento.</div>

  <!-- Pré-preenchidos automaticamente -->
  <div class="inp-grid" id="cf-prec-boxes" style="margin-bottom:4px">
    <div>
      <label class="inp-lbl">ALUGUEL ★</label>
      <input class="inp inp-money sm" id="i-cf-aluguel" type="text" placeholder="R$ 0"
        oninput="maskMoney(this);D.aluguel=parseMasked('i-cf-aluguel');calcCF()">
      <div class="inp-hint">Pré-preenchido da T07</div>
    </div>
    <div>
      <label class="inp-lbl">PESSOAL (CLT + PJ) ★</label>
      <input class="inp inp-money sm" id="i-cf-pessoal" type="text" placeholder="R$ 0"
        oninput="maskMoney(this);D.folha_total=parseMasked('i-cf-pessoal');calcCF()">
      <div class="inp-hint">Pré-preenchido da T32</div>
    </div>
  </div>
  <div id="cf-prolabore-box" style="margin-bottom:4px;display:none">
    <label class="inp-lbl">PRÓ-LABORE SÓCIOS ★</label>
    <input class="inp inp-money sm" id="i-cf-prolabore" type="text" placeholder="R$ 0"
      oninput="maskMoney(this);D.prolabore=parseMasked('i-cf-prolabore');calcCF()">
    <div class="inp-hint">Pré-preenchido da T33</div>
  </div>

  <div style="height:1px;background:var(--line);margin:14px 0 14px"></div>

  <!-- Novos campos -->
  <div class="inp-grid">
    <div>
      <label class="inp-lbl">FACILITIES</label>
      <input class="inp inp-money sm" id="i-facilities" type="text" placeholder="R$ 0"
        oninput="maskMoney(this);D.facilities=parseMasked('i-facilities');calcCF()">
      <div class="inp-hint">Luz, água, internet</div>
    </div>
    <div>
      <label class="inp-lbl">TERCEIRIZADOS</label>
      <input class="inp inp-money sm" id="i-terceirizados" type="text" placeholder="R$ 0"
        oninput="maskMoney(this);D.terceirizados=parseMasked('i-terceirizados');calcCF()">
      <div class="inp-hint">Contador, advogado</div>
    </div>
  </div>
  <div class="inp-wrap">
    <label class="inp-lbl">OUTROS CUSTOS FIXOS</label>
    <input class="inp inp-money" id="i-outros-cf" type="text" placeholder="R$ 0"
      oninput="maskMoney(this);D.outros_cf=parseMasked('i-outros-cf');calcCF()">
    <div class="inp-hint">Seguros, softwares, benefícios, sistemas</div>
  </div>
  <div class="res-box" id="cf-total-box" style="display:none;margin-bottom:16px">
    <span class="res-lbl">Total despesas fixas / mês</span>
    <span class="res-val" id="cf-total-val">—</span>
  </div>
  <button class="btn" onclick="ir('t39')">CONTINUAR →</button>
  <button class="btn-sec" onclick="ir('t37')">← Voltar</button>
</div>

<!-- T39 DÍVIDAS (T38 + T39 original unificados) -->
<div class="tela" id="t39">
  <div class="sec-label">Balanço</div>
  <div class="t-num">39</div>
  <div class="t-q">O negócio paga parcelas de empréstimos ou tem dívidas?</div>
  <div class="ops g2" style="margin-bottom:16px">
    <button class="op" onclick="pick(this,'dividas','sim');mostrar('c-dividas')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sim</div></div></button>
    <button class="op" onclick="pick(this,'dividas','nao');esconder('c-dividas');ir('t40')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não</div></div></button>
  </div>
  <div id="c-dividas" style="display:none">
    <div class="inp-grid">
      <div><label class="inp-lbl">PARCELAS MENSAIS TOTAIS</label>
        <input class="inp inp-money sm" id="i-parcelas" type="text" placeholder="R$ 0"
          oninput="maskMoney(this);D.parcelas_mensais=parseMasked('i-parcelas')"></div>
      <div><label class="inp-lbl">SALDO DEVEDOR TOTAL</label>
        <input class="inp inp-money sm" id="i-saldo-dev" type="text" placeholder="R$ 0"
          oninput="maskMoney(this);D.saldo_devedor=parseMasked('i-saldo-dev')"></div>
    </div>
    <label class="inp-lbl" style="margin-bottom:8px">NATUREZA DA DÍVIDA (pode marcar mais de um)</label>
    <div class="ops" style="margin-bottom:16px">
      <button class="op" onclick="toggleMulti(this,'divida_natureza','bancaria')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Bancária</div></div></button>
      <button class="op" onclick="toggleMulti(this,'divida_natureza','impostos')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Impostos / dívida fiscal</div></div></button>
      <button class="op" onclick="toggleMulti(this,'divida_natureza','socios')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Empréstimo de sócios</div></div></button>
      <button class="op" onclick="toggleMulti(this,'divida_natureza','terceiros')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Terceiros / fornecedores</div></div></button>
      <button class="op" onclick="toggleMulti(this,'divida_natureza','outros')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Outros</div></div></button>
    </div>
    <button class="btn" onclick="ir('t40')">CONTINUAR →</button>
  </div>
  <button class="btn-sec" onclick="ir('t38')">← Voltar</button>
</div>

<!-- T40 PROCESSOS JURÍDICOS -->
<div class="tela" id="t40">
  <div class="sec-label">Balanço</div>
  <div class="t-num">40</div>
  <div class="t-q">Tem processos judiciais ou administrativos em andamento?</div>
  <div class="ops g2" style="margin-bottom:16px">
    <button class="op" onclick="pick(this,'processos_juridicos','sim');mostrar('c-juridico')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sim</div></div></button>
    <button class="op" onclick="pick(this,'processos_juridicos','nao');esconder('c-juridico');ir('t41')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não</div></div></button>
  </div>
  <div id="c-juridico" style="display:none">
    <div class="inp-grid">
      <div><label class="inp-lbl">COMO RÉU — TOTAL ESTIMADO</label>
        <input class="inp inp-money sm" id="i-reu" type="text" placeholder="R$ 0"
          oninput="maskMoney(this);D.passivo_juridico=parseMasked('i-reu')"></div>
      <div><label class="inp-lbl">COMO AUTOR — TOTAL ESTIMADO</label>
        <input class="inp inp-money sm" id="i-autor" type="text" placeholder="R$ 0"
          oninput="maskMoney(this);D.ativo_juridico=parseMasked('i-autor')"></div>
    </div>
    <label class="inp-lbl" style="margin-bottom:8px">NATUREZA (pode marcar mais de um)</label>
    <div class="ops" style="margin-bottom:16px">
      <button class="op" onclick="toggleMulti(this,'juridico_tipo','trabalhista')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Trabalhista</div></div></button>
      <button class="op" onclick="toggleMulti(this,'juridico_tipo','fiscal')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Fiscal</div></div></button>
      <button class="op" onclick="toggleMulti(this,'juridico_tipo','civil')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Civil / contratos</div></div></button>
      <button class="op" onclick="toggleMulti(this,'juridico_tipo','outro')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Outro</div></div></button>
    </div>
    <button class="btn" onclick="ir('t41')">CONTINUAR →</button>
  </div>
  <button class="btn-sec" onclick="ir('t39')">← Voltar</button>
</div>

<!-- T41 ATIVOS (pré-preenchido) -->
<div class="tela" id="t41">
  <div class="sec-label">Balanço</div>
  <div class="t-num">41</div>
  <div class="t-q">Inventário de ativos</div>
  <div class="t-sub">Tudo que o negócio tem hoje.</div>
  <div class="aviso" style="margin-bottom:16px">
    <span>ℹ</span>
    <span>Os campos marcados com ★ foram pré-preenchidos com dados que você já informou. Confira e corrija se necessário.</span>
  </div>
  <div class="inp-grid">
    <div><label class="inp-lbl">CAIXA E APLICAÇÕES ★</label>
      <input class="inp inp-money sm" id="i-at-caixa" type="text" placeholder="R$ 0"
        oninput="maskMoney(this);D.at_caixa=parseMasked('i-at-caixa');calcAtivos()"></div>
    <div><label class="inp-lbl">CONTAS A RECEBER ★</label>
      <input class="inp inp-money sm" id="i-at-cr" type="text" placeholder="R$ 0"
        oninput="maskMoney(this);D.at_cr=parseMasked('i-at-cr');calcAtivos()"></div>
  </div>
  <div class="inp-grid">
    <div><label class="inp-lbl">ESTOQUE (custo) ★</label>
      <input class="inp inp-money sm" id="i-at-estoque" type="text" placeholder="R$ 0"
        oninput="maskMoney(this);D.at_estoque=parseMasked('i-at-estoque');calcAtivos()"></div>
    <div><label class="inp-lbl">EQUIPAMENTOS E MOBILIÁRIO</label>
      <input class="inp inp-money sm" id="i-at-equip" type="text" placeholder="R$ 0"
        oninput="maskMoney(this);D.at_equip=parseMasked('i-at-equip');calcAtivos()"></div>
  </div>
  <div class="inp-wrap">
    <label class="inp-lbl">IMÓVEIS E AUTOMÓVEIS PRÓPRIOS</label>
    <input class="inp inp-money" id="i-at-imovel" type="text" placeholder="R$ 0"
      oninput="maskMoney(this);D.at_imovel=parseMasked('i-at-imovel');calcAtivos()">
  </div>
  <div class="inp-wrap">
    <label class="inp-lbl">OUTROS ATIVOS</label>
    <input class="inp inp-money" id="i-at-outros" type="text" placeholder="R$ 0"
      oninput="maskMoney(this);D.at_outros=parseMasked('i-at-outros');calcAtivos()">
    <input class="inp sm" id="i-at-outros-desc" type="text"
      placeholder="Descreva brevemente..." style="margin-top:6px"
      oninput="D.at_outros_desc=this.value">
  </div>

  <div class="aviso" style="margin-top:4px">
    <span>⚠</span>
    <span>Inclua apenas bens usados no negócio. Imóvel pessoal no CNPJ, carro para uso próprio ou investimentos separados não devem entrar aqui.</span>
  </div>

  <div class="res-box" id="ativos-box" style="display:none;margin-bottom:16px">
    <span class="res-lbl">Total de ativos</span>
    <span class="res-val" id="ativos-val">—</span>
  </div>
  <button class="btn" onclick="ir('t42')">CONTINUAR →</button>
  <button class="btn-sec" onclick="ir('t40')">← Voltar</button>
</div>

<!-- T42 PASSIVOS (pré-preenchido) -->
<div class="tela" id="t42">
  <div class="sec-label">Balanço</div>
  <div class="t-num">42</div>
  <div class="t-q">Passivos e obrigações</div>
  <div class="t-sub">Atrasadas e a vencer.</div>
  <div class="aviso" style="margin-bottom:16px">
    <span>ℹ</span>
    <span>Campos com ★ foram pré-preenchidos. Confira e corrija se necessário.</span>
  </div>
  <div class="inp-grid">
    <div><label class="inp-lbl">CONTAS A PAGAR FORNECEDORES ★</label>
      <input class="inp inp-money sm" id="i-pv-forn" type="text" placeholder="R$ 0"
        oninput="maskMoney(this);D.pv_forn=parseMasked('i-pv-forn');calcPassivos()"></div>
    <div><label class="inp-lbl">DÍVIDA FINANCEIRA (saldo) ★</label>
      <input class="inp inp-money sm" id="i-pv-fin" type="text" placeholder="R$ 0"
        oninput="maskMoney(this);D.pv_fin=parseMasked('i-pv-fin');calcPassivos()"></div>
  </div>
  <div class="inp-wrap">
    <label class="inp-lbl">OUTROS PASSIVOS</label>
    <input class="inp inp-money" id="i-pv-outros" type="text" placeholder="R$ 0"
      oninput="maskMoney(this);D.pv_outros=parseMasked('i-pv-outros');calcPassivos()">
    <input class="inp sm" id="i-pv-desc" type="text"
      placeholder="Descreva brevemente (opcional)..." style="margin-top:6px"
      oninput="D.pv_desc=this.value">
  </div>
  <div class="res-box rd" id="passivos-box" style="display:none;margin-bottom:16px">
    <span class="res-lbl">Total de passivos</span>
    <span class="res-val" id="passivos-val">—</span>
  </div>
  <button class="btn" onclick="ir('t43')">CONTINUAR →</button>
  <button class="btn-sec" onclick="ir('t41')">← Voltar</button>
</div>

<!-- T43b RESULTADO ESTIMADO — cruzamento com DRE calculada -->
<div class="tela" id="t43b">
  <div class="sec-label">Perspectiva</div>
  <div class="t-num">43b</div>
  <div class="t-q">Só pra checar — quanto você acha que sobra todo mês?</div>
  <div class="t-sub">Depois de pagar tudo — funcionários, aluguel, fornecedores, parcelas. O que fica no caixa de verdade.</div>
  <div class="inp-wrap" style="margin-bottom:10px">
    <label class="inp-lbl">SOBRA MENSAL ESTIMADA</label>
    <input class="inp inp-money" id="i-sobra-est" type="text" placeholder="R$ 0"
      oninput="maskMoney(this);D.sobra_estimada=parseMasked('i-sobra-est');D.tem_estimativa=true">
  </div>
  <div style="margin-bottom:18px">
    <button class="op chip" id="btn-sobra-nsei" onclick="pick(this,'sobra_modo','nao_sei');D.sobra_estimada=null;D.tem_estimativa=false;document.getElementById('i-sobra-est').value=''">
      <div class="op-check"></div>
      <div class="op-txt"><div class="op-lbl">Varia muito — não sei dizer</div></div>
    </button>
  </div>
  <button class="btn" onclick="ir('t44')">CONTINUAR →</button>
  <button class="btn-sec" onclick="ir('t43')">← Voltar</button>
</div>

<!-- T43 PLANO -->
<div class="tela" id="t43">
  <div class="sec-label">Perspectiva</div>
  <div class="t-num">43</div>
  <div class="t-q" id="t43-q">Você tem um plano para os próximos 12 meses?</div>
  <div class="ops">
    <button class="op" onclick="pick(this,'plano','metas');ir('t43b')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Metas definidas e documentadas</div></div></button>
    <button class="op" onclick="pick(this,'plano','informal');ir('t43b')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Informal — existe mas não está documentado</div></div></button>
    <button class="op" onclick="pick(this,'plano','nao');ir('t43b')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não tenho um planejamento formalizado</div></div></button>
  </div>
  <button class="btn-sec" onclick="ir('t42')">← Voltar</button>
</div>

<!-- T44 EXPECTATIVA DE VALOR -->
<div class="tela" id="t44">
  <div class="sec-label">Perspectiva</div>
  <div class="t-num">44</div>
  <div class="t-q">Por fim — quanto você acha que vale o seu negócio hoje?</div>
  <div class="t-sub">Pense no conceito de "porteira fechada" — um valor pelo qual alguém assumiria o negócio por completo, com todos ativos e passivos.</div>

  <!-- Campo de valor sempre visível -->
  <div class="inp-wrap" style="margin-bottom:10px">
    <label class="inp-lbl">SUA ESTIMATIVA</label>
    <input class="inp inp-money" id="i-expectativa" type="text" placeholder="R$ 0"
      onfocus="focusMoney(this)" onblur="blurMoney(this)" oninput="maskMoney(this);D.expectativa_val=parseMasked('i-expectativa');D.expectativa='tenho_ideia'">
  </div>
  <!-- Opção não sabe inline -->
  <div style="margin-bottom:18px">
    <button class="op chip" id="btn-nao-sei" onclick="pick(this,'expectativa','nao_sei');document.getElementById('i-expectativa').value='';D.expectativa_val=0">
      <div class="op-check"></div>
      <div class="op-txt"><div class="op-lbl">Não faço ideia — quero descobrir</div></div>
    </button>
  </div>

  <div class="sep"></div>
  <label class="inp-lbl" style="margin:12px 0 8px;display:block">SE A AVALIAÇÃO ATENDESSE SUA EXPECTATIVA, VOCÊ ESTARIA ABERTO A PROPOSTAS?</label>
  <div class="ops" style="margin-bottom:22px">
    <button class="op chip" onclick="pick(this,'abertura_venda','sim')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Claro</div></div></button>
    <button class="op chip" onclick="pick(this,'abertura_venda','talvez')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Quem sabe</div></div></button>
    <button class="op chip" onclick="pick(this,'abertura_venda','nao')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não penso em vender</div></div></button>
  </div>
  <button class="btn" onclick="ir('t45')">CONTINUAR →</button>
  <button class="btn-sec" onclick="ir('t43')">← Voltar</button>
</div>

<!-- T44b — tela intermediária de abertura (referenciada no voltar do t45) -->
<div class="tela" id="t44b">
  <div class="sec-label">Perspectiva</div>
  <div class="t-num">44b</div>
  <div class="t-q" id="t44b-q">E se a avaliação surpreender positivamente... estaria aberto a propostas?</div>
  <div class="ops" style="margin-bottom:22px">
    <button class="op chip" onclick="pick(this,'abertura_venda2','sim');ir('t45')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Sim, estaria aberto</div></div></button>
    <button class="op chip" onclick="pick(this,'abertura_venda2','talvez');ir('t45')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Talvez, depende do valor</div></div></button>
    <button class="op chip" onclick="pick(this,'abertura_venda2','nao');ir('t45')"><div class="op-check"></div><div class="op-txt"><div class="op-lbl">Não estou pensando em vender</div></div></button>
  </div>
  <button class="btn-sec" onclick="ir('t44')">← Voltar</button>
</div>

<!-- T45 CONTEXTO LIVRE -->
<div class="tela" id="t45">
  <div class="sec-label">Perspectiva</div>
  <div class="t-num">45</div>
  <div class="t-q" id="t45-q">Por fim, tem algo mais que devemos saber?</div>
  <div class="t-sub">Contexto adicional, situação especial, fato relevante a ser observado.</div>
  <div class="inp-wrap" style="margin-bottom:22px">
    <textarea class="inp" id="i-contexto" rows="5"
      placeholder="Contexto adicional, situação especial, pontos que devem entrar na avaliação..."
      oninput="D.contexto=this.value"></textarea>
  </div>
  <button class="btn" onclick="ir('t46')">CONTINUAR →</button>
  <button class="btn-skip" onclick="ir('t46')">Pular →</button>
  <button class="btn-sec" onclick="ir('t44b')">← Voltar</button>
</div>

<!-- T46 CONFIRMAÇÃO + WOW -->
<div class="tela" id="t46">
  <div class="sec-label">Confirmação</div>
  <div class="t-num">46</div>
  <div class="t-q">É isso mesmo?</div>
  <div class="t-sub">Revise os números antes de gerar a avaliação.</div>

  <!-- WOW stats -->
  <div class="wow-stat-grid" id="wow-grid"></div>

  <!-- Resumo identidade -->
  <div class="resumo-grid" id="resumo-grid"></div>

  <!-- ── DRE ── -->
  <div style="margin:20px 0 6px">
    <div style="font-family:'DM Mono',monospace;font-size:9px;letter-spacing:.18em;
      text-transform:uppercase;color:var(--ink4);margin-bottom:10px;
      display:flex;align-items:center;gap:8px">
      DRE Simplificada<span style="flex:1;height:1px;background:var(--line)"></span>
      <span style="font-size:8px;color:var(--ink4)">/ mês</span>
    </div>
  </div>
  <div id="dre-wrap" style="margin-bottom:20px"></div>

  <!-- ── BP ── -->
  <div style="margin:0 0 6px">
    <div style="font-family:'DM Mono',monospace;font-size:9px;letter-spacing:.18em;
      text-transform:uppercase;color:var(--ink4);margin-bottom:10px;
      display:flex;align-items:center;gap:8px">
      Balanço Patrimonial<span style="flex:1;height:1px;background:var(--line)"></span>
    </div>
  </div>
  <div id="bp-wrap" style="margin-bottom:24px"></div>

  <div style="background:var(--em-d);border:1px solid var(--em-l);border-radius:var(--r2);
    padding:14px 16px;margin-bottom:20px">
    <div style="font-size:10px;font-family:'DM Mono',monospace;letter-spacing:.12em;
      text-transform:uppercase;color:var(--em);margin-bottom:4px">Tudo certo?</div>
    <div style="font-size:12px;color:var(--ink2);line-height:1.65">
      Qualquer erro nos números muda o resultado. Se precisar corrigir, use o botão voltar.
    </div>
  </div>
  <button class="btn" onclick="gerarAvaliacao()">GERAR MINHA AVALIAÇÃO →</button>
  <button class="btn-sec" onclick="ir('t45')">← Voltar e corrigir</button>
</div>

</div></div><!-- /card /page -->

<script>
// ── STATE ──────────────────────────────────────────────────────────
const D = {
  tipo:[], online:[], canais:[], juridico_tipo:[], divida_natureza:[],
  at_nao_operacional:0, at_nao_op_desc:'', sobra_estimada:null, tem_estimativa:false
};
const TOTAL_TELAS = 50; // incluindo sub-telas t43b e t44b

const SECOES = {
  t1:'Início',t2:'Identidade',t3:'Identidade',t4:'Identidade',t5:'Identidade',
  t6:'Identidade',t7:'Identidade',t8:'Identidade',t9:'Identidade',t10:'Identidade',
  t11:'Identidade',t11b:'Identidade',t12:'Identidade',t13:'Identidade',t14:'Identidade',
  t15:'Identidade',t16:'Identidade',t17:'Faturamento',t18:'Faturamento',t19:'Faturamento',
  t20:'Faturamento',t21:'Faturamento',t22:'Comercial',t23:'Comercial',t24:'Comercial',
  t25:'Comercial',t26:'Comercial',t27:'Comercial',t28:'Comercial',t29:'Comercial',
  t30:'Custos',t31:'Custos',t32:'Equipe',t33:'Equipe',t34:'Gestão',t35:'Gestão',
  t36:'Gestão',t37:'Gestão',t38:'Custos fixos',t39:'Balanço',t40:'Balanço',
  t41:'Balanço',t42:'Balanço',t43:'Perspectiva',t43b:'Perspectiva',t44:'Perspectiva',t44b:'Perspectiva',
  t45:'Perspectiva',t46:'Confirmação',
};

const ORDEM = ['t1','t2','t3','t4','t5','t6','t7','t8','t9','t10',
  't11','t11b','t12','t13','t14','t15','t16','t17','t18','t19','t20',
  't21','t22','t23','t24','t25','t26','t27','t28','t29','t30','t31',
  't32','t33','t34','t35','t36','t37','t38','t39','t40','t41','t42',
  't43','t44','t45','t46'];

// Cidades por estado
const CIDADES = {
  SP:['São Paulo','Campinas','Guarulhos','São Bernardo do Campo','Ribeirão Preto'],
  RJ:['Rio de Janeiro','Niterói','Duque de Caxias','Nova Iguaçu','São Gonçalo'],
  MG:['Belo Horizonte','Uberlândia','Contagem','Juiz de Fora','Betim'],
  RS:['Porto Alegre','Caxias do Sul','Pelotas','Canoas','Santa Maria'],
  SC:['Florianópolis','Joinville','Blumenau','Balneário Camboriú','Chapecó'],
  PR:['Curitiba','Londrina','Maringá','Ponta Grossa','Foz do Iguaçu'],
  BA:['Salvador','Feira de Santana','Vitória da Conquista','Camaçari','Itabuna'],
  GO:['Goiânia','Aparecida de Goiânia','Anápolis','Rio Verde','Luziânia'],
  PE:['Recife','Caruaru','Olinda','Petrolina','Paulista'],
  CE:['Fortaleza','Caucaia','Juazeiro do Norte','Maracanaú','Sobral'],
  DF:['Brasília','Taguatinga','Ceilândia','Gama','Samambaia'],
  MT:['Cuiabá','Várzea Grande','Sinop','Rondonópolis','Tangará da Serra'],
  MS:['Campo Grande','Dourados','Três Lagoas','Corumbá','Ponta Porã'],
};

// ── HELPERS ────────────────────────────────────────────────────────
function fmtM(v){
  if(!v||v===0)return'R$ 0';
  const s = v < 0 ? '-' : '';
  const a = Math.abs(Math.round(v));
  return s+'R$ '+a.toLocaleString('pt-BR');
}
function maskMoney(inp){
  // Remove tudo exceto dígitos — trata ponto e vírgula como separadores visuais, não decimais
  let v=inp.value.replace(/[^0-9]/g,'');
  if(!v){inp.value='';return;}
  const num=parseInt(v,10);
  if(isNaN(num)){inp.value='';return;}
  inp.value='R$ '+num.toLocaleString('pt-BR',{minimumFractionDigits:0,maximumFractionDigits:0});
}
function focusMoney(inp){
  // Ao focar, mostra só o número puro para redigitação segura
  const num=parseInt(inp.value.replace(/[^0-9]/g,''),10)||0;
  inp.value=num>0?String(num):'';
  inp.select();
}
function blurMoney(inp){
  // Ao sair, aplica a máscara
  maskMoney(inp);
}
function parseMasked(id){
  const v=document.getElementById(id)?.value||'';
  // Remove tudo exceto dígitos — nunca interpreta ponto/vírgula como decimal
  const digits=v.replace(/[^0-9]/g,'');
  return parseInt(digits,10)||0;
}
function setMasked(id,val){
  // Preenche campo com máscara a partir de valor numérico (usado no carregamento da sessão)
  const el=document.getElementById(id);
  if(!el||!val) return;
  const num=parseInt(String(val).replace(/[^0-9]/g,''),10)||0;
  el.value=num>0?'R$ '+num.toLocaleString('pt-BR',{minimumFractionDigits:0,maximumFractionDigits:0}):'';
}
function mostrar(id){const e=document.getElementById(id);if(e)e.style.display='block';}
function esconder(id){const e=document.getElementById(id);if(e)e.style.display='none';}
function nn(s){return s||'—';}

// ── NAVEGAÇÃO ──────────────────────────────────────────────────────
let telaAtual='t1';
function ir(id){
  document.querySelectorAll('.tela').forEach(t=>t.classList.remove('ativa'));
  const el=document.getElementById(id);
  if(!el)return;
  el.classList.add('ativa');
  window.scrollTo({top:0,behavior:'smooth'});
  telaAtual=id;
  const idx=ORDEM.indexOf(id);
  const pct=Math.round(((idx+1)/ORDEM.length)*100);
  document.getElementById('prog-fill').style.width=pct+'%';
  document.getElementById('prog-txt').textContent=pct+'%';
  document.getElementById('tag-secao').textContent=SECOES[id]||'';
  // Side effects por tela
  if(id==='t23')verificarHintFranquia();
  if(id==='t38')preencherCF();
  if(id==='t41')preencherAtivos();
  if(id==='t42')preencherPassivos();
  if(id==='t46')buildResumo();
  // Autosave
  clearTimeout(window._saveTimer);
  window._saveTimer = setTimeout(async ()=>{
    await salvarSessao(id);
    mostrarSaveIndicator();
  }, 800);
}
function voltar_t9(){
  ir(D.local_tipo==='remoto'?'t7':'t8');
}

// ── SELEÇÃO ÚNICA ──────────────────────────────────────────────────
function pick(btn,campo,val){
  btn.closest('.ops,.artigo-group')?.querySelectorAll('.op,.artigo-btn').forEach(b=>b.classList.remove('sel'));
  btn.classList.add('sel');
  D[campo]=val;
  // ativar botões desabilitados da tela atual
  const tAtiva=document.querySelector('.tela.ativa');
  ['btn1','btn2','btn3','btn8','btn11','btn15','btn16','btn17','btn22','btn27','btn34'].forEach(id=>{
    const b=document.getElementById(id);
    if(b&&tAtiva?.contains(b))b.disabled=false;
  });
}

// ── SELEÇÃO MÚLTIPLA ───────────────────────────────────────────────
function toggleMulti(btn,campo,val){
  if(!Array.isArray(D[campo]))D[campo]=[];
  if(D[campo].includes(val)){D[campo]=D[campo].filter(v=>v!==val);btn.classList.remove('sel');}
  else{D[campo].push(val);btn.classList.add('sel');}
  const tAtiva=document.querySelector('.tela.ativa');
  ['btn3','btn16','btn22'].forEach(id=>{
    const b=document.getElementById(id);
    if(b&&tAtiva?.contains(b)&&D[campo].length>0)b.disabled=false;
  });
}

// ── PILLS ──────────────────────────────────────────────────────────
function selPill(btn,campo,val){
  btn.closest('.pills')?.querySelectorAll('.pill').forEach(b=>b.classList.remove('sel'));
  btn.classList.add('sel');
  D[campo]=val;
  const tAtiva=document.querySelector('.tela.ativa');
  const b=tAtiva?.querySelector('.btn:not(.btn-sec):not(.btn-skip)');
  if(b)b.disabled=false;
}

// ── ESTADO + CIDADES ───────────────────────────────────────────────
function selPillEstado(btn,uf){
  document.querySelectorAll('#estados-pills .pill').forEach(b=>b.classList.remove('sel'));
  btn.classList.add('sel');
  D.estado=uf;
  const cidades=CIDADES[uf];
  const c=document.getElementById('cidades-sugeridas');
  const cp=document.getElementById('cidades-pills');
  if(cidades){
    cp.innerHTML=cidades.map(city=>`<button class="pill" onclick="selCidade(this,'${city}')">${city}</button>`).join('');
    c.style.display='block';
  } else {c.style.display='none';}
  validarT8();
}
function selCidade(btn,city){
  document.querySelectorAll('#cidades-pills .pill').forEach(b=>b.classList.remove('sel'));
  btn.classList.add('sel');
  D.cidade=city;
  document.getElementById('i-cidade').value=city;
  validarT8();
}
function validarT8(){
  document.getElementById('btn8').disabled=!(D.cidade?.trim()||D.estado);
}

// ── T2: NOME + ARTIGO ──────────────────────────────────────────────
function atualizarArtigos(){
  const n=D.nome||'seu negócio';
  document.getElementById('ex-o').textContent='o '+n;
  document.getElementById('ex-a').textContent='a '+n;
  document.getElementById('c-artigo').style.display=n.length>1?'block':'none';
  // Atualizar T3 e demais com artigo
  atualizarTitulos();
}
function selArtigo(a){
  document.getElementById('art-o').classList.toggle('sel',a==='o');
  document.getElementById('art-a').classList.toggle('sel',a==='a');
  validarT2();
  atualizarTitulos();
}
function validarT2(){
  // Se nome veio da URL (pré-preenchido), habilita mesmo sem artigo selecionado ainda
  document.getElementById('btn2').disabled=!(D.nome?.trim());
}
function atualizarTitulos(){
  const oa=D.artigo?.toUpperCase()||'O/A';
  const a=D.artigo||'o/a';
  const n=D.nome||'o negócio';
  const an=a+' '+n;
  const AN=oa+' '+n.toUpperCase();
  const ids={
    't3-q':`O que melhor representa ${an}?`,
    't5-q':`Descreva ${an} em poucas palavras`,
    't7-q':`${an.charAt(0).toUpperCase()+an.slice(1)} está em um local fixo?`,
    't10-q':`${D.nome?.split(' ')[0]||'Você'}, você tem sócios?`,
    't12-q':`Qual o regime tributário atual d${a} ${n}?`,
    't13-q':`${an.charAt(0).toUpperCase()+an.slice(1)} precisa de alguma licença especial para operar?`,
    't16-q':`${an.charAt(0).toUpperCase()+an.slice(1)} tem presença online?`,
    't22-q':`Por onde ${an} vende?`,
    't32-q':`Qual o tamanho do time d${a} ${n}?`,
    't43-q':`${D.nome?.split(' ')[0]||'Você'}, você tem um plano para ${an} nos próximos 12 meses?`,
    't44b-q':`${D.nome?.split(' ')[0]||'Você'}, e se a avaliação surpreender positivamente... estaria aberto a propostas?`,
    't45-q':`${D.nome?.split(' ')[0]||'Por fim'}, tem algo mais que devemos saber?`,
    't1-q':`${D.nome_usuario?D.nome_usuario+', conta':' Conta'} pra gente — o que te motiva a avaliar o negócio hoje?`,
  };
  Object.entries(ids).forEach(([id,txt])=>{
    const el=document.getElementById(id);
    if(el)el.textContent=txt;
  });
}

// ── T9: ANOS ───────────────────────────────────────────────────────
function atualizarAnos(v){
  D.anos_num=parseInt(v);
  document.getElementById('anos-val').textContent=v>=30?'30+ anos':v+' ano'+(v>1?'s':'');
}
function atualizarOrigemAnos(v){
  D.origem_anos_num=parseInt(v);
  document.getElementById('origem-anos-val').textContent=v>=30?'30+ anos':v+' ano'+(v>1?'s':'');
}

// ── T17: FATURAMENTO ───────────────────────────────────────────────
function atualizarFatDisplay(){
  const m=D.fat_mensal||0;
  const a=D.fat_anual||0;
  const d=document.getElementById('fat-display');
  const v=document.getElementById('fat-display-val');
  if(m>0||a>0){
    d.style.display='flex';
    v.textContent=D.fat_modo==='mensal'?fmtM(a)+' por ano':fmtM(m)+' por mês';
    document.getElementById('btn17').disabled=false;
  }
}

// ── T20: RECORRÊNCIA ───────────────────────────────────────────────
function atualizarRecorrenciaVal(){
  const pct=parseFloat(D.pct_recorrencia)||0;
  const m=D.fat_mensal||0;
  const box=document.getElementById('recorrencia-val-box');
  const val=document.getElementById('recorrencia-val');
  if(pct>0&&m>0){
    box.style.display='flex';
    val.textContent=fmtM(m*pct/100)+' / mês';
  }
}

// ── T23: HINT FRANQUIA ─────────────────────────────────────────────
function verificarHintFranquia(){
  if(D.franquia==='sim'&&D.mkt_franquia>0){
    document.getElementById('mkt-hint-franquia').style.display='block';
  }
}

// ── T26: COMISSÃO VALOR ────────────────────────────────────────────
function calcComissaoValor(){
  const pct=parseFloat(D.pct_comissao)||0;
  const m=D.fat_mensal||0;
  const box=document.getElementById('comissao-val-box');
  const val=document.getElementById('comissao-val');
  if(pct>0&&m>0){
    const v=m*pct/100;
    D.comissao_val=v;
    box.style.display='flex';
    val.textContent=fmtM(v)+' / mês';
  }
}

// ── T28: CONCENTRAÇÃO HINT ─────────────────────────────────────────
function atualizarConcHint(){
  const v=D.concentracao_pct||0;
  const hint=document.getElementById('conc-hint');
  if(v>=70)hint.textContent='⚠ Concentração muito alta — risco relevante para o valuation';
  else if(v>=50)hint.textContent='Concentração elevada — será destacada no laudo';
  else if(v>=30)hint.textContent='Concentração moderada';
  else hint.textContent='Boa diversificação de clientes';
}

// ── MODE TOGGLE ────────────────────────────────────────────────────
function selMode(btn,campo,val,togId){
  document.querySelectorAll('#'+togId+' .mode-btn').forEach(b=>b.classList.remove('sel'));
  btn.classList.add('sel');
  D[campo]=val;
  // atualizar hint de royalty
  if(campo==='royalty_tipo'){
    document.getElementById('hint-royalty').textContent=val==='pct'?'Ex: 5% sobre o faturamento':'Ex: R$ 2.000 fixo por mês';
    document.getElementById('i-royalty').placeholder=val==='pct'?'5':'2000';
  }
}

// ── T30: CMV ───────────────────────────────────────────────────────
function calcCMV(){
  const compra=parseMasked('i-compra');
  const venda=parseMasked('i-venda');
  if(venda>0&&compra>=0){
    const margem=((venda-compra)/venda*100).toFixed(1);
    D.margem_bruta=parseFloat(margem);
    D.cmv_pct=100-D.margem_bruta;
    document.getElementById('calc-cmv-val').textContent=margem+'%';
    document.getElementById('calc-cmv-res').style.display='flex';
  }
}

// ── T32: FOLHA ─────────────────────────────────────────────────────
function calcFolha(){
  const t=(D.clt_folha||0)+(D.pj_custo||0);
  const box=document.getElementById('folha-total-box');
  if(t>0){box.style.display='flex';document.getElementById('folha-total-val').textContent=fmtM(t);}
  D.folha_total=t;
}

// ── T6: ROYALTIES e MKT FRANQUIA ─────────────────────────────────
function atualizarInputRoyalty(tipo){
  const el=document.getElementById('i-royalty');
  const hint=document.getElementById('hint-royalty');
  if(tipo==='pct'){
    el.classList.remove('inp-money');
    el.placeholder='Ex: 5';
    el.value='';
    hint.textContent='Percentual sobre o faturamento bruto';
    el.oninput=function(){D.royalty_val=parseFloat(this.value)||0;D.royalty=D.fat_mensal?(D.fat_mensal*D.royalty_val/100):0;};
  } else {
    el.classList.add('inp-money');
    el.placeholder='R$ 0';
    el.value='';
    hint.textContent='Valor fixo por mês';
    el.oninput=function(){maskMoney(this);D.royalty=parseMasked('i-royalty');};
  }
}
function atualizarInputMkt(tipo){
  const el=document.getElementById('i-mkt-franquia');
  if(tipo==='pct'){
    el.classList.remove('inp-money');
    el.placeholder='Ex: 2';
    el.value='';
    el.oninput=function(){D.mkt_franquia_val=parseFloat(this.value)||0;D.mkt_franquia=D.fat_mensal?(D.fat_mensal*D.mkt_franquia_val/100):0;};
  } else {
    el.classList.add('inp-money');
    el.placeholder='R$ 0';
    el.value='';
    el.oninput=function(){maskMoney(this);D.mkt_franquia=parseMasked('i-mkt-franquia');};
  }
}
function inputRoyalty(el){
  if(D.royalty_tipo==='pct'){
    D.royalty_val=parseFloat(el.value)||0;
    D.royalty=D.fat_mensal?(D.fat_mensal*D.royalty_val/100):0;
  } else {
    maskMoney(el);
    D.royalty=parseMasked('i-royalty');
  }
}

// ── T41: ATIVOS (pré-preenchimento) ───────────────────────────────
function preencherAtivos(){
  const preenche=(id,val)=>{
    const el=document.getElementById(id);
    if(el&&val>0&&!parseMasked(id)){el.value=formatarMoney(val);}
  };
  preenche('i-at-caixa', D.caixa||0);
  preenche('i-at-cr', D.contas_receber||0);
  preenche('i-at-estoque', D.estoque||0);
  D.at_caixa=D.caixa||0;
  D.at_cr=D.contas_receber||0;
  D.at_estoque=D.estoque||0;
  calcAtivos();
}

// ── T42: PASSIVOS (pré-preenchimento) ─────────────────────────────
function preencherPassivos(){
  const preenche=(id,val)=>{
    const el=document.getElementById(id);
    if(el&&val>0&&!parseMasked(id)){el.value=formatarMoney(val);}
  };
  preenche('i-pv-forn', D.contas_pagar||0);
  preenche('i-pv-fin', D.saldo_devedor||0);
  D.pv_forn=D.contas_pagar||0;
  D.pv_fin=D.saldo_devedor||0;
  calcPassivos();
}

function formatarMoney(v){
  if(!v||isNaN(v)) return 'R$ 0';
  return 'R$ '+Number(v).toLocaleString('pt-BR',{minimumFractionDigits:0,maximumFractionDigits:0});
}

// ── T38: CUSTOS FIXOS (pré-preenchimento) ─────────────────────────
function calcCF(){
  const t=(D.aluguel||0)+(D.folha_total||0)+(D.prolabore||0)+(D.facilities||0)+(D.terceirizados||0)+(D.outros_cf||0);
  const box=document.getElementById('cf-total-box');
  if(t>0){box.style.display='flex';document.getElementById('cf-total-val').textContent=fmtM(t);}
  D.cf_total=t;
}
function preencherCF(){
  // Calcular folha_total se não calculado ainda
  if(!D.folha_total){
    const clt = D.folha_clt_bruta || 0;
    const pj  = D.folha_pj || 0;
    D.folha_total = clt + pj;
  }
  // Aluguel
  if(D.aluguel>0){
    const el=document.getElementById('i-cf-aluguel');
    if(el&&!parseMasked('i-cf-aluguel')){el.value=formatarMoney(D.aluguel);}
  }
  // Pessoal CLT+PJ
  if(D.folha_total>0){
    const el=document.getElementById('i-cf-pessoal');
    if(el&&!parseMasked('i-cf-pessoal')){el.value=formatarMoney(D.folha_total);}
  }
  // Pró-labore
  if(D.prolabore>0){
    const box=document.getElementById('cf-prolabore-box');
    if(box) box.style.display='block';
    const el=document.getElementById('i-cf-prolabore');
    if(el&&!parseMasked('i-cf-prolabore')){el.value=formatarMoney(D.prolabore);}
  }
  calcCF();
}

function calcAtivos(){
  const t=(D.at_caixa||0)+(D.at_cr||0)+(D.at_estoque||0)+(D.at_imovel||0)+(D.at_equip||0)+(D.at_outros||0);
  const box=document.getElementById('ativos-box');
  if(t>0){box.style.display='flex';document.getElementById('ativos-val').textContent=fmtM(t);}
  D.total_ativos=t;
}
function calcPassivos(){
  const t=(D.pv_forn||0)+(D.pv_fin||0)+(D.pv_outros||0);
  const box=document.getElementById('passivos-box');
  if(t>0){box.style.display='flex';document.getElementById('passivos-val').textContent=fmtM(t);}
  D.total_passivos=t;
}

// ── T46: RESUMO + WOW + DRE + BP ──────────────────────────────────
function buildResumo(){
  const campos_preenchidos = Object.keys(D).filter(k=>D[k]&&D[k]!==''&&(!Array.isArray(D[k])||D[k].length>0)).length;
  const fat_anual = D.fat_anual || (D.fat_mensal||0)*12;
  const pl = (D.total_ativos||0)-(D.total_passivos||0);

  // WOW
  // Calcular dados de mercado usados (campos sem resposta do cliente)
  const campos_mercado = [
    !D.facilities && 'facilities',
    !D.terceirizados && 'contabilidade/terceiros',
    D.cmv_modo==='servico' && 'CMV zerado (serviço)',
  ].filter(Boolean);
  const n_mercado = campos_mercado.length;

  // WOW stats básicos
  document.getElementById('wow-grid').innerHTML=[
    {n:campos_preenchidos,l:'informações coletadas'},
    {n:n_mercado>0?n_mercado:'0',l:'estimativas de mercado usadas'},
    {n:fmtM(fat_anual),l:'faturamento anual'},
    {n:fmtM(pl>=0?pl:-pl),l:pl>=0?'patrimônio líquido':'passivo líquido'},
  ].map(w=>`<div class="wow-stat"><div class="wow-n">${w.n}</div><div class="wow-l">${w.l}</div></div>`).join('');
  
  if(n_mercado>0){
    const aviso = document.getElementById('wow-grid');
    const nota = document.createElement('div');
    nota.style.cssText='grid-column:1/-1;background:var(--am-d);border:1px solid var(--am-l);border-radius:var(--r);padding:10px 12px;font-size:10px;color:var(--ink3);line-height:1.55;margin-top:4px';
    nota.innerHTML='<span style="color:var(--am);font-family:DM Mono,monospace;font-size:9px;letter-spacing:.1em;text-transform:uppercase">Estimativas de mercado usadas</span><br>'+campos_mercado.join(' · ')+' — valores baseados na média do seu setor. Você pode voltar e preencher para maior precisão.';
    aviso.appendChild(nota);
  }

  // ── INDICADORES IMPLÍCITOS ─────────────────────────────────────
  // Calculados a partir dos dados coletados — preview do que tem no laudo completo
  const ind = [];

  // Ticket médio implícito
  if(fat_anual>0 && (D.cli_12m||D.cli_total)>0){
    const cli = D.cli_12m||D.cli_total;
    const ticket = Math.round(fat_anual/cli);
    ind.push({
      ico:'R$', cor:'var(--em)',
      titulo:'Ticket médio anual por cliente',
      valor: fmtM(ticket),
      detalhe: `Calculado sobre ${cli.toLocaleString('pt-BR')} clientes · ${fmtM(Math.round(ticket/12))}/mês por cliente`
    });
  }

  // Ponto de equilíbrio
  if(fat>0 && ro>0){
    const cf = total_desp;
    const mc_pct = mb/fat_liq;
    const pe = mc_pct>0 ? cf/mc_pct : 0;
    if(pe>0){
      const folga = ((fat_liq - pe)/fat_liq*100);
      ind.push({
        ico:'⚖', cor: folga>20?'var(--em)':folga>5?'var(--am)':'var(--rd)',
        titulo:'Ponto de equilíbrio mensal',
        valor: fmtM(Math.round(pe)),
        detalhe: `Você opera ${folga.toFixed(0)}% acima do ponto mínimo para não perder dinheiro`
      });
    }
  }

  // Comprometimento de caixa por parcelas
  if(ro>0 && parcelas>0){
    const comp = Math.round(parcelas/ro*100);
    ind.push({
      ico:'🔒', cor: comp>70?'var(--rd)':comp>40?'var(--am)':'var(--em)',
      titulo:'Comprometimento do resultado por dívidas',
      valor: comp+'%',
      detalhe: `${fmtM(parcelas)}/mês em parcelas sobre ${fmtM(ro)}/mês de resultado — ${comp>70?'alto: o dinheiro some antes de sobrar':comp>40?'moderado: atenção ao fluxo':'dentro do aceitável'}`
    });
  }

  // Cobertura de custo fixo em dias
  if(fat>0 && total_desp>0){
    const dias_cf = Math.round(total_desp/fat*30);
    ind.push({
      ico:'📅', cor: dias_cf>20?'var(--rd)':dias_cf>15?'var(--am)':'var(--em)',
      titulo:'Dias trabalhando para pagar a estrutura',
      valor: dias_cf+'d / mês',
      detalhe: `Você trabalha ${dias_cf} dias por mês só para cobrir custos fixos — ${30-dias_cf} dias sobram para o resultado`
    });
  }

  // Folha vs benchmark
  if(fat>0 && (clt_folha+desp_pj)>0){
    const folhaPct = Math.round((clt_folha+encargos_clt+provisoes_clt+desp_pj)/fat*100);
    const benchF = {alimentacao:22,varejo:18,saude:28,educacao:35,tech:30,construcao:20,outro:25};
    const ref = benchF[D.setor]||25;
    if(Math.abs(folhaPct-ref)>3){
      ind.push({
        ico:'👥', cor: folhaPct>ref+5?'var(--rd)':folhaPct>ref?'var(--am)':'var(--em)',
        titulo:'Custo de pessoal vs. setor',
        valor: folhaPct+'% do faturamento',
        detalhe: `Referência para ${D.setor||'seu setor'}: ${ref}% · você está ${folhaPct>ref?folhaPct-ref+'pp acima':ref-folhaPct+'pp abaixo'} do esperado`
      });
    }
  }

  if(ind.length>0){
    const grid = document.getElementById('wow-grid');
    const sep = document.createElement('div');
    sep.style.cssText='grid-column:1/-1;border-top:1px solid var(--line);padding-top:12px;margin-top:4px';
    sep.innerHTML=`
      <div style="font-family:'DM Mono',monospace;font-size:8px;letter-spacing:.14em;text-transform:uppercase;color:var(--ink4);margin-bottom:10px">
        Indicadores implícitos · calculados dos seus dados
      </div>
      <div style="display:flex;flex-direction:column;gap:8px">
        ${ind.map(i=>`
          <div style="display:flex;align-items:flex-start;gap:10px;background:var(--bg2);border:1px solid var(--line2);border-radius:var(--r2);padding:10px 12px">
            <div style="font-family:'Syne',sans-serif;font-size:18px;font-weight:800;color:${i.cor};flex-shrink:0;min-width:60px;text-align:center;line-height:1.1">${i.valor}</div>
            <div>
              <div style="font-size:11px;color:var(--ink);font-weight:600;margin-bottom:2px">${i.titulo}</div>
              <div style="font-size:10px;color:var(--ink3);line-height:1.5">${i.detalhe}</div>
            </div>
          </div>`).join('')}
      </div>
      <div style="margin-top:10px;background:var(--pu-d);border:1px solid var(--pu-l);border-radius:var(--r2);padding:9px 12px;font-size:10px;color:var(--ink3);line-height:1.6">
        <span style="color:var(--pu);font-family:'DM Mono',monospace;font-size:8px;letter-spacing:.1em;text-transform:uppercase">Laudo completo · R$ 99</span><br>
        Ciclo financeiro, GAO, liquidez corrente, concentração de receita, CAC implícito e muito mais — disponíveis no laudo completo.
      </div>`;
    grid.appendChild(sep);
  }

  // Resumo identidade
  document.getElementById('resumo-grid').innerHTML=[
    {k:'Negócio',v:(D.artigo?D.artigo.toUpperCase()+' ':'')+D.nome},
    {k:'Setor',v:D.setor?.replace(/_/g,' ')},
    {k:'Localização',v:[D.cidade,D.estado].filter(Boolean).join(', ')},
    {k:'Tempo',v:D.anos_num?D.anos_num+'+ anos':'—'},
    {k:'Regime',v:D.regime},
    {k:'Equipe',v:((D.clt_qtd||0)+(D.pj_qtd||0))+' pessoas'},
  ].map(i=>`<div class="res-item"><div class="res-k">${i.k}</div><div class="res-v">${i.v||'—'}</div></div>`).join('');

  // ── DRE MENSAL — estrutura conforme Notion (03 · Estrutura da DRE Ajustada) ──
  const fat = D.fat_mensal || 0;

  // L2 — Impostos por regime
  function calcImpostos(fat, regime){
    if(!fat) return 0;
    const f = fat * 12;
    if(regime === 'mei') return fat * 0.05;
    if(regime === 'simples'){
      if(f <= 180000) return fat * 0.06;
      if(f <= 360000) return fat * 0.112;
      if(f <= 720000) return fat * 0.135;
      if(f <= 1800000) return fat * 0.16;
      return fat * 0.21;
    }
    if(regime === 'presumido') return fat * (0.15*0.32 + 0.09*0.32 + 0.0065 + 0.03);
    if(regime === 'real') return fat * 0.15;
    return fat * 0.08;
  }
  const impostos = calcImpostos(fat, D.regime);

  // L3 — Comissões
  const desp_comissao = D.comissao_val_direto || D.comissao_val || (fat*(D.pct_comissao||0)/100);

  // L4 — Taxas processadores
  const desp_proc = fat * (D.pct_proc||0)/100 * (parseFloat(D.taxa_proc)||0)/100;

  // L5/L6 — Royalties e fundo % (somente se % do faturamento)
  const royalty_pct   = (D.franquia==='sim' && D.royalty_tipo==='pct') ? (D.royalty||0) : 0;
  const mkt_frq_pct   = (D.franquia==='sim' && D.mkt_franquia_tipo==='pct') ? (D.mkt_franquia||0) : 0;

  const fat_liq = fat - impostos - desp_comissao - desp_proc - royalty_pct - mkt_frq_pct;

  // L7 — CMV aplicado sobre Receita Líquida
  const cmv = fat_liq * (D.cmv_pct||0) / 100;
  const mb  = fat_liq - cmv;

  // L8 — Folha CLT bruta
  const clt_folha = D.clt_folha || 0;
  // L9 — Encargos CLT: 29,2% (Simples) ou 58% (Presumido/Real)
  const encargos_pct = (D.regime==='presumido'||D.regime==='real') ? 0.58 : 0.292;
  const encargos_clt = clt_folha * encargos_pct;
  // L10 — Provisões CLT: 13º (8,3%) + férias+1/3 (11,1%) + rescisão (3%) = 22,4%
  const provisoes_clt = clt_folha * 0.224;
  // L11 — PJ/freela
  const desp_pj = D.pj_custo || 0;
  // L12 — Aluguel
  const desp_aluguel = D.aluguel || 0;
  // L13/L14/L15 — Facilities, terceirizados, outros CF
  const desp_facilities   = D.facilities || 0;
  const desp_terceirizados= D.terceirizados || 0;
  const desp_outros_cf    = D.outros_cf || 0;
  // L16/L17 — Royalties e fundo FIXO
  const royalty_fixo  = (D.franquia==='sim' && D.royalty_tipo==='fixo') ? (D.royalty||0) : 0;
  const mkt_frq_fixo  = (D.franquia==='sim' && D.mkt_franquia_tipo==='fixo') ? (D.mkt_franquia||0) : 0;
  // L18 — Marketing pago
  const desp_mkt = D.mkt_valor || 0;

  const total_desp = clt_folha + encargos_clt + provisoes_clt + desp_pj
    + desp_aluguel + desp_facilities + desp_terceirizados + desp_outros_cf
    + royalty_fixo + mkt_frq_fixo + desp_mkt;

  const ro = mb - total_desp; // Resultado Operacional — base do valuation

  // Abaixo do RO — informativo
  const parcelas  = D.parcelas_mensais || 0;
  // Pró-labore fica ABAIXO do RO (comprador pode remunerar diferente)
  const prolabore = D.prolabore || 0;
  const pot_caixa = ro - parcelas - prolabore;

  function dreRow(lbl, val, pct, indent, bold, color, dim){
    const vStr = fmtM(val);
    const pStr = (pct && fat > 0) ? (typeof pct === 'string' ? pct : Math.abs(val/fat*100).toFixed(1)+'%') : '';
    const c = color || (bold ? (val>=0?'var(--em)':'var(--rd)') : 'var(--ink3)');
    const fs = bold ? '12' : (dim ? '10' : '11');
    const fw = bold ? '700' : '400';
    return `<div style="display:flex;justify-content:space-between;align-items:center;
      padding:${bold?'9px':'5px'} 0 ${bold?'9px':'5px'} ${indent?'14px':'0'};
      ${bold?'border-top:1px solid var(--line2);margin-top:2px':''}">
      <span style="font-size:${fs}px;color:${dim?'var(--ink4)':c};font-weight:${fw};opacity:${dim?'.7':'1'}">${lbl}</span>
      <div style="display:flex;gap:8px;align-items:center">
        ${pStr?`<span style="font-size:9px;font-family:'DM Mono',monospace;color:var(--ink4)">${pStr}</span>`:''}
        <span style="font-family:'DM Mono',monospace;font-weight:${bold?'700':'400'};
          font-size:${bold?'13':'11'}px;color:${dim?'var(--ink4)':c}">${vStr}</span>
      </div>
    </div>`;
  }

  const pct = v => fat>0 ? Math.abs(v/fat*100).toFixed(1)+'%' : '';

  document.getElementById('dre-wrap').innerHTML=`
    <div style="background:var(--bg1);border:1px solid var(--line2);border-radius:var(--r2);padding:4px 14px 8px">
      ${dreRow('Faturamento bruto', fat, '100%', false, true, 'var(--ink)')}
      ${impostos>0?dreRow('(−) Impostos · '+(D.regime||'estimado'), -impostos, pct(impostos), true, false):''}
      ${desp_comissao>0?dreRow('(−) Comissões', -desp_comissao, pct(desp_comissao), true, false):''}
      ${desp_proc>0?dreRow('(−) Taxas de processamento', -desp_proc, pct(desp_proc), true, false):''}
      ${royalty_pct>0?dreRow('(−) Royalties (% fat.)', -royalty_pct, pct(royalty_pct), true, false):''}
      ${mkt_frq_pct>0?dreRow('(−) Fundo propaganda (% fat.)', -mkt_frq_pct, pct(mkt_frq_pct), true, false):''}
      ${dreRow('= Receita Líquida', fat_liq, pct(fat_liq), false, true)}
      ${cmv>0?dreRow('(−) CMV / CPV', -cmv, pct(cmv), true, false):''}
      ${dreRow('= Margem Bruta', mb, pct(mb), false, true)}
      ${clt_folha>0?dreRow('(−) Folha CLT bruta', -clt_folha, pct(clt_folha), true, false):''}
      ${encargos_clt>0?dreRow('(−) Encargos CLT ('+Math.round(encargos_pct*100)+'%)', -encargos_clt, pct(encargos_clt), true, false):''}
      ${provisoes_clt>0?dreRow('(−) Provisões CLT (22,4%)', -provisoes_clt, pct(provisoes_clt), true, false):''}
      ${desp_pj>0?dreRow('(−) Equipe PJ / freela', -desp_pj, pct(desp_pj), true, false):''}
      ${desp_aluguel>0?dreRow('(−) Aluguel', -desp_aluguel, pct(desp_aluguel), true, false):''}
      ${desp_facilities>0?dreRow('(−) Facilities', -desp_facilities, pct(desp_facilities), true, false):''}
      ${desp_terceirizados>0?dreRow('(−) Terceirizados', -desp_terceirizados, pct(desp_terceirizados), true, false):''}
      ${desp_outros_cf>0?dreRow('(−) Outros custos fixos', -desp_outros_cf, pct(desp_outros_cf), true, false):''}
      ${royalty_fixo>0?dreRow('(−) Royalties (fixo)', -royalty_fixo, pct(royalty_fixo), true, false):''}
      ${mkt_frq_fixo>0?dreRow('(−) Fundo propaganda (fixo)', -mkt_frq_fixo, pct(mkt_frq_fixo), true, false):''}
      ${desp_mkt>0?dreRow('(−) Marketing pago', -desp_mkt, pct(desp_mkt), true, false):''}
      ${dreRow('= Resultado Operacional ⭐', ro, pct(ro), false, true)}
      <div style="margin-top:8px;padding-top:6px;border-top:1px dashed var(--line2)">
        <div style="font-family:'DM Mono',monospace;font-size:8px;letter-spacing:.12em;text-transform:uppercase;color:var(--ink4);margin-bottom:4px">Linhas informativas — não entram no valuation</div>
        ${parcelas>0?dreRow('(−) Parcelas de dívidas', -parcelas, '', true, false, 'var(--ink4)', true):''}
        ${prolabore>0?dreRow('(−) Retiradas dos sócios', -prolabore, '', true, false, 'var(--ink4)', true):''}
        ${dreRow('= Potencial de geração de caixa', pot_caixa, '', false, false, pot_caixa>=0?'var(--ink2)':'var(--rd)', false)}
      </div>
    </div>`;

  // ── BP ──────────────────────────────────────────────────────────
  const at_circ   = (D.at_caixa||0) + (D.at_cr||0) + (D.at_estoque||0);
  const at_fixo   = (D.at_imovel||0) + (D.at_equip||0) + (D.at_outros||0) + (D.taxa_franquia||0);
  const total_at  = at_circ + at_fixo;
  const pv_cp     = (D.pv_forn||0) + (D.contas_pagar||0);
  const pv_lp     = (D.pv_fin||0) + (D.saldo_devedor||0);
  const pv_outros = D.pv_outros||0;
  const total_pv  = pv_cp + pv_lp + pv_outros;
  const pl_calc   = total_at - total_pv;

  function bpRow(lbl, val, indent, bold, color){
    const c = color || (bold ? 'var(--ink)' : 'var(--ink2)');
    return `<div style="display:flex;justify-content:space-between;align-items:center;
      padding:${bold?'9px':'6px'} 0;
      ${bold?'border-top:1px solid var(--line);':''}
      ${indent?'padding-left:14px;':''}">
      <span style="font-size:${bold?'12':'11'}px;color:${bold?'var(--ink2)':'var(--ink3)'};
        font-weight:${bold?'700':'400'}">${lbl}</span>
      <span style="font-family:'Syne',sans-serif;font-weight:${bold?'800':'600'};
        font-size:${bold?'14':'12'}px;color:${c}">${fmtM(val)}</span>
    </div>`;
  }

  document.getElementById('bp-wrap').innerHTML=`
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px">
      <!-- ATIVO -->
      <div style="background:var(--bg1);border:1px solid var(--line2);border-radius:var(--r2);padding:4px 14px 8px">
        <div style="font-family:'DM Mono',monospace;font-size:8px;letter-spacing:.16em;
          text-transform:uppercase;color:var(--ink4);padding:10px 0 6px">ATIVO</div>
        <div style="font-size:9px;font-family:'DM Mono',monospace;color:var(--ink4);
          letter-spacing:.1em;text-transform:uppercase;padding-bottom:4px">Circulante</div>
        ${bpRow('Caixa e aplic.', D.at_caixa||0, true, false)}
        ${bpRow('Contas a receber', D.at_cr||0, true, false)}
        ${bpRow('Estoques', D.at_estoque||0, true, false)}
        <div style="font-size:9px;font-family:'DM Mono',monospace;color:var(--ink4);
          letter-spacing:.1em;text-transform:uppercase;padding:8px 0 4px;
          border-top:1px solid var(--line)">Permanente</div>
        ${bpRow('Equipamentos', D.at_equip||0, true, false)}
        ${bpRow('Imóveis / Veículos', D.at_imovel||0, true, false)}
        ${D.taxa_franquia>0?bpRow('Taxa de franquia', D.taxa_franquia, true, false):''}
        ${D.at_outros>0?bpRow('Outros ativos', D.at_outros, true, false):''}
        ${bpRow('TOTAL ATIVO', total_at, false, true, 'var(--em)')}
      </div>
      <!-- PASSIVO + PL -->
      <div style="background:var(--bg1);border:1px solid var(--line2);border-radius:var(--r2);padding:4px 14px 8px">
        <div style="font-family:'DM Mono',monospace;font-size:8px;letter-spacing:.16em;
          text-transform:uppercase;color:var(--ink4);padding:10px 0 6px">PASSIVO + PL</div>
        <div style="font-size:9px;font-family:'DM Mono',monospace;color:var(--ink4);
          letter-spacing:.1em;text-transform:uppercase;padding-bottom:4px">Curto prazo</div>
        ${bpRow('Fornecedores', pv_cp, true, false)}
        <div style="font-size:9px;font-family:'DM Mono',monospace;color:var(--ink4);
          letter-spacing:.1em;text-transform:uppercase;padding:8px 0 4px;
          border-top:1px solid var(--line)">Longo prazo</div>
        ${bpRow('Dívida financeira', pv_lp, true, false)}
        ${pv_outros>0?bpRow('Outros passivos', pv_outros, true, false):''}
        ${bpRow('TOTAL PASSIVO', total_pv, false, true, 'var(--rd)')}
        <div style="font-size:9px;font-family:'DM Mono',monospace;color:var(--ink4);
          letter-spacing:.1em;text-transform:uppercase;padding:8px 0 4px;
          border-top:1px solid var(--line)">Patrimônio</div>
        ${bpRow('Patrimônio Líquido', pl_calc, true, true, pl_calc>=0?'var(--em)':'var(--rd)')}
      </div>
    </div>`;
}

// ── AUTOSAVE ───────────────────────────────────────────────────────
const SUPA_URL = 'https://dbijmgqlcrgjlcfrastg.supabase.co/functions/v1/diagnostico-sessao';

async function salvarSessao(tela){
  if(!D.codigo_lead) return; // só salva se tiver slug
  try{
    await fetch(`${SUPA_URL}?action=save&slug=${D.codigo_lead}`, {
      method:'POST',
      headers:{'Content-Type':'application/json'},
      body: JSON.stringify({ tela_atual: tela, dados: D, concluido: tela==='fim' })
    });
  }catch(e){ console.warn('autosave falhou:', e); }
}

async function carregarSessao(slug){
  try{
    const r = await fetch(`${SUPA_URL}?action=load&slug=${slug}`);
    const j = await r.json();
    return j.found ? j : null;
  }catch(e){ return null; }
}

// Modal de sessão existente
function mostrarModalSessao(sessao){
  const dias = Math.round((Date.now() - new Date(sessao.atualizado_em).getTime()) / 86400000);
  const tempoStr = dias === 0 ? 'hoje' : dias === 1 ? 'ontem' : `há ${dias} dias`;
  const nome = sessao.nome ? sessao.nome.split(' ')[0] : '';
  const negocio = sessao.nome_negocio || 'seu negócio';

  const modal = document.createElement('div');
  modal.id = 'modal-sessao';
  modal.style.cssText = `
    position:fixed;inset:0;z-index:999;
    background:rgba(8,13,10,.92);backdrop-filter:blur(16px);
    display:flex;align-items:center;justify-content:center;padding:20px`;
  modal.innerHTML = `
    <div style="background:var(--bg2);border:1px solid var(--em-l);border-radius:20px;
      padding:32px;max-width:400px;width:100%;text-align:center">
      <div style="font-family:'DM Mono',monospace;font-size:9px;letter-spacing:.18em;
        text-transform:uppercase;color:var(--em);margin-bottom:12px">Sessão encontrada</div>
      <div style="font-family:'Syne',sans-serif;font-weight:800;font-size:22px;
        letter-spacing:-.02em;color:var(--ink);margin-bottom:6px;line-height:1.2">
        ${nome ? nome+', você' : 'Você'} já começou<br>esta avaliação
      </div>
      <div style="font-size:12px;color:var(--ink3);margin-bottom:6px;line-height:1.65">
        <strong style="color:var(--ink)">${negocio}</strong><br>
        Salva ${tempoStr} · tela <strong style="color:var(--em)">${sessao.tela_atual?.replace('t','')}</strong> de 46
      </div>
      ${sessao.concluido ? `
        <div style="background:var(--em-d);border:1px solid var(--em-l);border-radius:10px;
          padding:10px 14px;margin:12px 0;font-size:11px;color:var(--em)">
          ✓ Avaliação concluída — você pode visualizar o laudo
        </div>` : ''}
      <div style="display:flex;flex-direction:column;gap:8px;margin-top:20px">
        ${sessao.concluido ? `
          <button onclick="verLaudo()" style="width:100%;background:var(--em);color:#080d0a;
            border:none;border-radius:12px;padding:14px;font-family:'DM Mono',monospace;
            font-size:11px;letter-spacing:.12em;cursor:pointer;font-weight:700">
            VER MEU LAUDO →
          </button>` : `
          <button onclick="retomar()" style="width:100%;background:var(--em);color:#080d0a;
            border:none;border-radius:12px;padding:14px;font-family:'DM Mono',monospace;
            font-size:11px;letter-spacing:.12em;cursor:pointer;font-weight:700">
            CONTINUAR DE ONDE PAREI →
          </button>`}
        <button onclick="recomecar()" style="width:100%;background:transparent;
          border:1px solid var(--line2);color:var(--ink3);border-radius:12px;padding:12px;
          font-size:12px;cursor:pointer;font-family:'Cabinet Grotesk',sans-serif;font-weight:600">
          Começar do zero
        </button>
      </div>
    </div>`;
  document.body.appendChild(modal);
}

function retomar(){
  document.getElementById('modal-sessao')?.remove();
  const tela = D._tela_retomada || 't1';
  delete D._tela_retomada;
  // Preencher campos de texto
  if(D.nome){ const el=document.getElementById('i-nome'); if(el){el.value=D.nome;atualizarArtigos();} }
  if(D.cidade){ const el=document.getElementById('i-cidade'); if(el) el.value=D.cidade; }
  if(D.descricao){ const el=document.getElementById('i-descricao'); if(el) el.value=D.descricao; }
  // Preencher campos monetários com máscara
  const moneyFields = [
    ['i-fat-anual',D.fat_anual],['i-fat-mensal',D.fat_mensal],
    ['i-aluguel',D.aluguel],['i-mkt',D.mkt_valor],
    ['i-clt-folha',D.clt_folha],['i-pj-custo',D.pj_custo],
    ['i-prolabore',D.prolabore],['i-caixa',D.caixa],
    ['i-cr',D.contas_receber||D.at_cr],['i-cp',D.contas_pagar||D.pv_forn],
    ['i-estoque',D.estoque||D.at_estoque],['i-saldo-dev',D.saldo_devedor||D.pv_fin],
    ['i-parcelas',D.parcelas_mensais],['i-comissao-val',D.comissao_val_direto||D.comissao_val],
    ['i-mkt-franquia',D.mkt_franquia],['i-taxa-franquia',D.taxa_franquia],
    ['i-cf-aluguel',D.aluguel],['i-cf-pessoal',D.folha_total||D.clt_folha],
    ['i-cf-prolabore',D.prolabore],['i-facilities',D.facilities],
    ['i-at-caixa',D.at_caixa||D.caixa],['i-at-cr',D.at_cr||D.contas_receber],
    ['i-at-estoque',D.at_estoque||D.estoque],['i-at-equip',D.at_equip],
    ['i-at-imovel',D.at_imovel],['i-at-outros',D.at_outros],
    ['i-pv-forn',D.pv_forn||D.contas_pagar],['i-pv-fin',D.pv_fin||D.saldo_devedor],
    ['i-pv-outros',D.pv_outros],
  ];
  moneyFields.forEach(([id,val])=>{ if(val) setMasked(id,val); });
  ir(tela);
}

function recomecar(){
  document.getElementById('modal-sessao')?.remove();
  // Limpar dados mas manter slug e nome_usuario
  const slug = D.codigo_lead;
  const nomeu = D.nome_usuario;
  Object.keys(D).forEach(k=>{if(k!=='tipo'&&k!=='online'&&k!=='canais'&&k!=='juridico_tipo'&&k!=='divida_natureza')delete D[k];});
  D.tipo=[]; D.online=[]; D.canais=[]; D.juridico_tipo=[]; D.divida_natureza=[];
  D.codigo_lead=slug; D.nome_usuario=nomeu;
  ir('t1');
}

function verLaudo(){
  document.getElementById('modal-sessao')?.remove();
  window.location.href='/laudo?dados='+encodeURIComponent(JSON.stringify(D));
}

// ── INDICADOR AUTOSAVE ─────────────────────────────────────────────
function mostrarSaveIndicator(){
  let ind = document.getElementById('save-ind');
  if(!ind){
    ind = document.createElement('div');
    ind.id = 'save-ind';
    ind.style.cssText = `
      position:fixed;bottom:16px;right:16px;z-index:200;
      background:var(--bg2);border:1px solid var(--line2);
      border-radius:20px;padding:5px 12px;
      font-family:'DM Mono',monospace;font-size:8px;letter-spacing:.12em;
      text-transform:uppercase;color:var(--ink4);
      transition:opacity .3s;opacity:0`;
    document.body.appendChild(ind);
  }
  ind.textContent = '✓ salvo';
  ind.style.opacity='1';
  setTimeout(()=>{ ind.style.opacity='0'; }, 2000);
}

// ── GERAR AVALIAÇÃO ───────────────────────────────────────────────
async function gerarAvaliacao(){
  const btn = document.querySelector('#t46 .btn');
  if(btn){ btn.disabled=true; btn.innerHTML='<span class="spin"></span> GERANDO AVALIAÇÃO...'; }

  Object.assign(D, {concluido: true});

  // 1. Salvar no sessionStorage como fallback
  try{ sessionStorage.setItem('1neg_diagnostico', JSON.stringify(D)); }catch(e){}

  // 2. Salvar no Supabase (dados completos para o laudo ler)
  if(D.codigo_lead){
    try{
      await fetch('https://dbijmgqlcrgjlcfrastg.supabase.co/functions/v1/diagnostico-sessao?action=save&slug='+D.codigo_lead, {
        method:'POST',
        headers:{'Content-Type':'application/json'},
        body: JSON.stringify({ tela_atual: 'fim', dados: D, concluido: true })
      });
    }catch(e){ console.warn('save falhou:', e); }
  }

  // 3. Redirecionar — com lead se tiver, senão sessionStorage
  setTimeout(()=>{
    if(D.codigo_lead){
      window.location.href = '/laudo?lead=' + D.codigo_lead;
    } else {
      // Fallback: passar dados comprimidos só se não tiver lead
      window.location.href = '/laudo';
    }
  }, 1200);
}

// Handler global para campos monetários — garante redigitação limpa
document.addEventListener('DOMContentLoaded', ()=>{
  document.querySelectorAll('.inp-money').forEach(inp=>{
    if(!inp.dataset.moneyBound){
      inp.dataset.moneyBound='1';
      inp.addEventListener('focus', ()=>focusMoney(inp));
      inp.addEventListener('blur',  ()=>blurMoney(inp));
    }
  });
});
// ir() já inclui autosave e side effects — sem overrides adicionais

// ── INIT ────────────────────────────────────────────────────────────
(async function(){
  const p = new URLSearchParams(window.location.search);
  const nomeParam   = p.get('nome');
  const negParam    = p.get('negocio');
  const leadParam   = p.get('lead');

  if(nomeParam){
    D.nome_usuario = nomeParam;
    const primeiroNome = nomeParam.split(' ')[0];
    const t1q = document.getElementById('t1-q');
    if(t1q) t1q.textContent = `${primeiroNome}, conta pra gente — o que te motiva a avaliar o negócio hoje?`;
  }
  if(negParam){
    D.nome = negParam;
    const el = document.getElementById('i-nome');
    if(el){ el.value=negParam; atualizarArtigos(); }
  }
  if(leadParam) D.codigo_lead = leadParam;

  // T17: abrir com média mês pré-selecionado
  D.fat_modo = 'mensal';
  mostrar('c-fat-mensal');
  esconder('c-fat-anual');

  // T6: royalties=% e mkt=fixo por padrão
  D.royalty_tipo = 'pct';
  D.mkt_franquia_tipo = 'fixo';

  // T30: calculadora pré-selecionada
  D.cmv_modo = 'calc';

  // Sliders
  atualizarAnos(5);
  atualizarOrigemAnos(3);

  // Concentração
  D.concentracao_pct = 20;
  atualizarConcHint();

  // ── Tentar carregar sessão existente ──────────────────────────────
  const slug = leadParam || D.codigo_lead;
  if(slug){
    const sessao = await carregarSessao(slug);
    if(sessao && sessao.found && sessao.tela_atual && sessao.tela_atual !== 't1'){
      // Mesclar dados salvos
      const dadosSalvos = sessao.dados || {};
      Object.assign(D, dadosSalvos);
      D._tela_retomada = sessao.tela_atual;
      D.codigo_lead = slug;
      mostrarModalSessao(sessao);
      return; // não navega — modal decide
    }
  }

  // Sem sessão — começar normal na T1
  ir('t1');
})();

</script>
</body>
</html>
