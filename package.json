{
  "name": "1negocio-tests",
  "private": true,
  "description": "Test tooling + @vercel/og pro render server-side de criativos P5. Demais endpoints /api/* continuam sem dep de npm.",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "dependencies": {
    "satori": "^0.10.13",
    "@resvg/resvg-js": "^2.6.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
